# リファクタリング計画：umoria

## 前提

- テスト実行コマンド: `make -f makefile.test` （未作成。項目 #0 で整備する）
- 着手前のテスト件数: **0**（`tests/minunit.h` の疎通確認のみ済み）
- 着手前のコミット: `488db72`
- 調査日: 2026-08-26
- 規模: 62 ファイル / 約 30,000 行 / C17

### ビルドについて（重要）

`make` は **ncurses.h 不在で失敗する**。ただし `curses.h` を include するのは
`io.c`, `platform.c`, `render_ncurses.c`, `input_ncurses.c` の 4 ファイルのみで、
ゲームロジック層は ncurses 非依存でコンパイルできることを確認済み。

→ **ncurses を導入せずにロジック層のテストは書ける。** これが本計画の前提。

### テスト可能性の実証（済）

`monster_name_indefinite` に実際にテストを書き、リンクとレッド報告まで確認した。

```
FAIL: monster_name_indefinite(out, &c) == "XXX": actual "an orc", expected "XXX"
```

「実装に答えを教えてもらう」手順が機能することを確認済み。個別 `.c` を
必要な分だけリンクし、足りない依存はスタブ化する方式で成立する。

### UI 層のモック差しこみ（可能）

`render.h:32-69` の `struct RenderBackend`（関数ポインタ 18 個）と
`input.h:68-80` の `struct InputBackend`（5 個）が vtable 形式。
差しこみ口は `render_init(RenderBackend*)` / `input_init(InputBackend*)`。
高レベル関数は全て `current_backend->fn` 経由で NULL ガード付きなので、
部分実装のモックでも安全に動く。

制約: `platform_init()` が ncurses backend をハードコードしているので、
テストでは `platform_init` を通さず `render_init(&mock)` を直接呼ぶ。
また `io.c` の不要な `curses.h` include を外さないとリンクに ncurses が必要
（→ 項目 #2 が最優先の理由）。

## 検出結果の内訳

| 分類 | 件数 | うち P1 | P2 | P3 | 棚上げ |
|---|---|---|---|---|---|
| 重複コード | 8 | 2 | 2 | 3 | 1 |
| 長すぎるメソッド | 6 | 0 | 1 | 2 | 3 |
| 肥大化クラス／モジュール | 5 | 2 | 1 | 2 | 0 |
| 不適切な責務配置 | 3 | 1 | 1 | 1 | 0 |
| 名前が意図を表さない | 3 | 1 | 1 | 1 | 0 |
| 複雑な条件分岐 | 4 | 0 | 1 | 1 | 2 |
| 強すぎる依存関係 | 3 | 1 | 0 | 1 | 1 |
| データの散在 | 3 | 0 | 1 | 1 | 1 |
| 不要なコード | 7 | 5 | 2 | 0 | 0 |
| 理解しづらいロジック | 4 | 1 | 1 | 2 | 0 |

## 作業台帳（P1：今回やる）

着手順に並べる。優先度順ではなく「保護しやすく効果が大きいもの」順。

| # | 状態 | 分類 | 場所 | 内容 | 手法 | 影響度 | コスト |
|---|---|---|---|---|---|---|---|
| 0 | **検証済み** | （足場） | tests/, makefile.test | テスト実行の makefile ターゲットを整備 | — | High | Low |
| 1 | **検証済み** | 理解しづらいロジック | misc1.c:217 `distance` | 依存ゼロの純関数。テスト第一号の足場 | テスト追加のみ | High | Low |
| 2 | **検証済み** | 強すぎる依存関係 | io.c:13,20-22 | 不要な `curses.h`/`signal.h`/`sys/select.h`/`termios.h` include。該当API使用0件（`wrefresh` はコメントアウト済） | include 削除 | High | Low |
| 3 | **棚上げ** | ~~不要なコード~~ → 未接続の基盤 | view_observer.c/.h 全体 | 下記「棚上げ」#34 に移動 | — | — | — |
| 4 | **棚上げ** | ~~不要なコード~~ → 未接続の基盤 | game_state.c/.h 全体 | 下記「棚上げ」#35 に移動 | — | — | — |
| 5 | **棚上げ** | ~~不要なコード~~ → 未接続の基盤 | render.h:61-63 | 下記「棚上げ」#36 に移動 | — | — | — |
| 6 | **検証済み** | 不要なコード | io.c:26,617-630 / signals.c:138-151 / misc1.c:58-62 | `error_abort`, `ignore_signals`/`default_signals`/`restore_signals`, `check_time` が参照0件。git 履歴で「基盤」ではないことを確認済み | デッドコード削除 | Medium | Low |
| 7 | **検証済み** | 重複コード | staffs.c:35-46, wands.c:42-56 | 魔法道具の成功判定が2箇所で丸ごと同一。差異は staffs の `-5` のみ → `src/device.c` に抽出。penalty を引数化（STAFF=5 / WAND=0） | Extract Method → `device_use_chance()` | High | Low |
| 8b | **検証済み** | 重複コード | staffs.c:154-167, wands.c:152-165 | **#8 と同じ `ident` ブロックがさらに 2 箇所**（2026-08-27 に判明）。どちらも `i_ptr` 再代入がコメントアウト済み（`// NOTE: this is never read after this`）なので、`scrolls.c` と同型。`learn_item_effect()` の呼びだしに置きかえるだけで済む。**すでに `item_ident.c` が存在し、テスト 21 件で保護されているので着手コストは Low** | 抽出済み関数の呼びだしに置きかえ | High | Low |
| 8 | **検証済み** | 重複コード | potions.c:319-331, eat.c:190-203, scrolls.c:465-480 | 「効果が判明したら経験値加算 → identify、判明しなければ sample」の `ident` ブロックが3ファイルで実質完全一致（差異は `scrolls.c` が `i_ptr` 再代入を省くのみ）。経験値の加算式 `m_ptr->exp += (i_ptr->level + (m_ptr->lev >> 1)) / m_ptr->lev;` は3箇所で同一 | Extract Method | High | Medium |
| 9 | **完了** | 重複コード | render_ncurses.c:274-297 vs :96-101 | `suspend()` が端末モード設定5行（`cbreak`/`noecho`/`nonl`/`intrflush`/`keypad`）を `ncurses_init` からコピペ | Extract Method | High | Low |
| 40 | **完了** | 肥大化クラス／モジュール | misc3.c:257-327, 658-702 → `src/stats.c`（新設、2026-09-13） | 能力値の補正表 7 関数（`stat_adj` `chr_adj` `con_adj` `tohit_adj` `toac_adj` `todis_adj` `todam_adj`）と `lookup_stat_bonus` ＋ テーブル 7 個、計 **116 行**（記録の 118 は境界の数えまちがい）。**単体コンパイルで未解決シンボルは `py` の 1 個だけ** | モジュール分割（切り取って貼る。中身は変えない） | Medium | Low |
| 41 | **完了** | 肥大化クラス／モジュール | misc3.c:1649-1716 → `src/str_insert.c`（新設、2026-09-13） | `insert_str` / `insert_lnum`、**67 行**（記録の 69 は境界の数えまちがい。行番号も #40 の移動で 118 行ずれていた）。**umoria のシンボルへの依存ゼロ**（標準ライブラリのみ）。~~`misc4.c:95` の `concat` も同じ性質なので一緒に寄せる~~ → **外した**（`concat` は保護ゼロ。→ P3 #47）。名前は `strings.c` にしない（`-Isrc` が `<strings.h>` を横取りしうる） | モジュール分割（切り取って貼る。中身は変えない） | Medium | Low |

**#40・#41 は 2026-09-13 に追加。** #0〜#9 とは前提が違い、**着手時点で保護済み**（#40 は `stat_bonus_test`、#41 は `objdes_test` が実体を検証している）。移動だけの変更なので、ステップ A で確かめるのは「意図的に壊してレッドが出るか」の 1 点。

**ただしこの「保護済み」は正確ではなかった。** 着手順の再検討（下記）で測ると、#40 の
7 関数のうち `con_adj` はテスト 0 件、#41 の `concat` は保護ゼロだった。**#40 は
`con_adj` に 12 件足すところから始めた**（→ 下記「#40」の節）。

**#0〜#9 について：すべて「保護」欄は無（テスト0件）。** #1 でテストの足場を作り、以後は各項目の対象に
テストを足してから着手する。#2〜#6 のデッドコード削除は参照0件を grep で裏取り済みなので、
削除後にビルドが通ることが実質的な検証になる（`patterns.md` の「不要なコード」の手順）。

### 着手順の理由

- **#0〜#1 を最初に置く理由。** テストが 1 件も動いていない状態では、他のどの項目も
  「壊したかどうか」を確認できない。まず依存ゼロの純関数（`distance`）で
  「テストが書ける・動く・レッドが出る」ことを確立する。
- **#2 を早い位置に置く理由。** `io.c` の不要な include を外すと、io.c のテストに
  ncurses リンクが不要になる。UI 層（最も変更が活発な領域）のテスト可能性が開けるので、
  後続の項目すべてのコストを下げる。
- **#3〜#6 のデッドコード削除を前に出す理由。** 合計約 600 行が消える。読む量が減って
  以後の調査が速くなるうえ、削除は最も安全な操作（参照0件を確認済み）。
- **#7〜#9 は重複の中でも差異が 1 つ以下のもの。** 抽出しても引数が増えすぎず、
  素直に関数化できる。

### #8 の抽出範囲（2026-08-27 に精査）

**`ident` ブロックだけを抽出する。その後の処理は含めない。**

検出時は「効果判明後の後処理」全体が重複していると報告されたが、実際に 3 ファイルを
突きあわせると、共通なのは `ident` ブロック（経験値加算 → identify、あるいは sample）
までで、その後は各ファイルで異なる。

| ファイル | `ident` ブロックの後 |
|---|---|
| potions.c | `add_food` → `desc_remain` → `inven_destroy` |
| eat.c | `add_food` → **`PY_WEAK\|PY_HUNGRY` クリア** → **`prt_hunger`** → `desc_remain` → `inven_destroy` |
| scrolls.c | **`if (used_up)` で囲んで** `desc_remain` → `inven_destroy` |

差異が 3 つあるので、後処理まで含めて共通化すると引数だらけの関数になる
（`patterns.md`「差異が 3 つ以上になったら抽出をやめる」）。`ident` ブロックに
限定すれば差異は 1 つ（`i_ptr` 再代入の有無）なので素直に抽出できる。

テスト保護の難所: `identify()` がグローバル `inventory` を直接操作し、
`prt_experience()` が画面描画を行う。純粋関数として切りだせるのは経験値の
加算式のみ。#7 と同じ「テスト側に写す」方式か、`identify`/`prt_experience` を
差しかえ可能にするかの判断が必要。

### #9 を保留した理由（2026-08-27）

`render_ncurses.c` は **ncurses 不在の環境ではコンパイルできない**。

重複自体は明白（端末モード設定 5 行が `ncurses_init` と `suspend` に完全一致）で、
抽出も単純だが、**変更後にコンパイルが通ることを確認できない**。
「テストで保護してから変更する」原則の前提が満たせないので着手しない。

着手できる条件は次のいずれか。

- `libncurses-dev` を導入して本体のビルドを通せるようにする（環境の変更なのでユーザーの判断）
- ncurses を持つ別の環境で作業する

なお #2 の成果により、この 4 ファイル（`io.c` は除外済みなので
`platform.c`, `render_ncurses.c`, `input_ncurses.c`）以外は
ncurses 非依存で検証できる。制約はこの 3 ファイルに閉じている。

### 同じ理由で保留した項目（2026-08-27）

| # | 場所 | 内容 |
|---|---|---|
| 9 | `render_ncurses.c:274-297` vs `:96-101` | 端末モード設定 5 行の重複 |
| 13 | `render_ncurses.c:47-50,107-110,271-298` | SIGTSTP の責務が `signals.c` と分裂 |
| 15 | `render_ncurses.h`, `input_ncurses.h`, `backend_ncurses.h` | 同じ 2 関数宣言を持つヘッダが 3 つ |

**この 3 項目は `render_ncurses.c` / `input_ncurses.c` を触るため、
ncurses なしでは変更後の検証ができない。** 重複や責務の分裂は明白で、
修正方法も単純だが、「テストで保護してから変更する」原則の前提
（変更後にビルドが通ることを確認できる）が満たせない。

### 実際に動作することを確認（2026-08-28）

**ncurses 導入により、はじめてゲームを起動して確かめられた。**

```
$ make            → Build complete: umoria
$ cd data && TERM=xterm ../umoria
  → キャラクター作成画面（種族選択メニュー）まで到達
```

これは**単体テスト 269 件では確認できない領域**の検証だ。テストが守るのは
個々の関数のふるまいで、「画面制御を含めてアプリケーションが起動するか」は
別の問題。抽出した 3 ファイル（`device.c` / `item_ident.c` / `abilities.c`）を
含むビルドで正常に動くことが確かめられた。

**テストがグリーンでも、動くとは限らない。** 逆に、動いてもテストがなければ
壊したかどうかは分からない。両方が必要で、役割が違う。

### 保留の解除（2026-08-28）：ncurses 導入により再評価

`libncurses-dev` が導入され、**`make` が成功するようになった**。3 ファイルすべて
`gcc -fsyntax-only` が通り、`umoria` の実行ファイルが生成される
（私たちが追加した `device.o` / `item_ident.o` / `abilities.o` も正しくリンクされた）。

**テスト可能性も実証した。** `ncurses.h` は関数をマクロ化せず実関数として宣言している
ため、**リンクシームが効く**。`cbreak` / `noecho` / `nonl` / `intrflush` / `keypad` /
`initscr` / `endwin` / `stdscr` / `LINES` / `COLS` などの代役を書き、`-lncurses` を
付けずにリンクできることを確認済み（必要な代役は 75 個。既存の
`tests/misc3_stubs.c` が 57 個なので同程度の作業量）。

再評価の結果、3 項目の判定が分かれた。

| # | 判定 | 理由 |
|---|---|---|
| **15** | **着手可（最優先）** | 宣言は文字単位で同一（差異 0）。ヘッダの移動だけなので**コンパイラ自身が保護**になる。リスクほぼゼロ |
| **9** | **着手可** | 端末モード設定 5 行が完全一致（差異 0）。代役に呼びだし回数カウンタを持たせれば `ncurses_init` と `suspend` の等価性を検証できる |
| **13** | **棚上げに変更** | テストは書けるが、**ふるまいの正解が未定**。`nosignals()` の `SIG_IGN` と `suspend` 登録が競合する設計上の不整合（→ **B15**）を含み、責務を `signals.c` に寄せる前にその不整合をどう扱うかの仕様判断が必要。リファクタリングではなく設計変更 |

### #12 の精査（2026-08-27）

**差異は 3 つある。** 検出時の報告（「配列名以外まったく同一」）より多い。

| # | 差異 | 内容 |
|---|---|---|
| 1 | 配列名 | `comment2a`/`comment2b` vs `comment3a`/`comment3b` |
| 2 | 要素数 | `comment2b[16]` vs `comment3b[15]`（`a` 側はどちらも 3） |
| 3 | **呼びだし側の引数順** | `prt_comment2(last_offer, cur_ask, ...)` vs `prt_comment3(cur_ask, last_offer, ...)` |

3 番目が要注意。両関数のパラメータ名はどちらも `(offer, asking)` だが、
**呼びだし側が渡す順序が逆**になっている。

- 購入時（`store2.c:571`）：`prt_comment2(last_offer, cur_ask, ...)` — 自分の提示額、店の要求額
- 売却時（`store2.c:773`）：`prt_comment3(cur_ask, last_offer, ...)` — 店の提示額、自分の要求額

売買で役割が入れかわるので、`offer`/`asking` という名前は購入視点で付けられており、
売却では実質的に意味が逆になっている。**メッセージ内の `%A1`/`%A2` の埋めこみ順序が
入れかわると表示が壊れる**ので、統合時にここを取りちがえないことが最重要。

差異 3 つは `patterns.md` の「差異が 3 つ以上になったら抽出をやめる」の境界だが、
差異 1・2 は「配列とその要素数」で 1 つの組として扱えるうえ、差異 3 は呼びだし側に
残る（統合する関数の中には入らない）ので、実質的な引数は
「配列, 要素数, offer, asking, final」の 5 つに収まる。抽出は妥当と判断する。

## 作業台帳（P2：P1 のあと、時間・合意しだい）

| # | 分類 | 場所 | 内容 | 手法 | 影響度 | コスト |
|---|---|---|---|---|---|---|
| 10 | **検証済み** | 重複コード | misc3.c:1162-1181 と 1242-1281 | `inven_check_num` と `inven_carry` のスタック可否判定（6条件）が同一。コメントで "must be identical" と自認しており、不一致はアイテム消失バグ直結 → `items_can_stack(existing, incoming)` に抽出。**副産物として実バグ B8/B9 を発見** | Extract Method | High | Medium |
| 11 | **検証済み** | 重複コード | misc3.c:970-988, files.c:232-249 | 能力値算出の9式（`xbth`, `xbthb`, `xfos`, `xsrh`, `xstl`, `xdis`, `xsave`, `xdev`, `xinfra`）が画面版とファイル版で二重化。**2026-08-27 に diff で照合し、9式の本体が完全一致（コメントまで同一）と確認済み。** 差異は出力先（`put_buffer` か `fprintf`）のみ | Extract Method → `calc_abilities()` | Medium | Low |
| 12 | **検証済み** | 重複コード | store2.c:123-135 vs 137-150 | `prt_comment2`/`prt_comment3` が同型。**2026-08-27 精査：差異は配列名だけでなく「要素数」も違う**（`comment2b[16]` vs `comment3b[15]`、`comment2a`/`comment3a` はどちらも 3）。配列ポインタと要素数を引数にとれば統合できる（差異 2 つ） | 引数化して統合 | Medium | Low |
| 13 | **棚上げ** | 不適切な責務配置 | render_ncurses.c:47-50,107-110,271-298 | Render backend が SIGTSTP を直接 `signal()` 登録（`:109`）。`signals.c:98` は `// SIGTSTP is handled by the rendering system` とコメントで済ませており、シグナル責務が2ファイルに分裂 | Move（signals.c へ寄せる） | High | Medium |
| 14 | **検証済み** | 名前が意図を表さない | creature.c:75-86 `movement_rate` | 速度>0 なら移動回数、速度<=0 なら比較の結果を返す。単位が2種類混在で名前と乖離 → `moves_this_turn` に改名し、`? 1 : 0` で「回数を返す」ことを明示。`if/else` の対称性は保持。**ふるまいは不変**（等価性を1,081万通りで実証） | Rename + 戻り値の明示 | Medium | Low |
| 15 | **完了** | 重複コード | render_ncurses.h, input_ncurses.h, backend_ncurses.h | 同じ2関数宣言を持つヘッダが3つ。実利用者 platform.c は backend_ncurses.h のみ使う | 宣言の一元化 | Medium | Low |
| 16 | **完了** | 理解しづらいロジック | io.c:207-224 | `wait_for_more_confirmation` が `goto inkey` + switch。**読みにくさの原因は `case` の列挙ではなく、ラベル名（関数 `inkey()` と同名）と `default` の位置だった。** ラベルを `retry` に改め、`default` を後置してコメントを補った。`case` で受理文字を並べる形は「集合の宣言」として読めるので残した | ラベル改名 + 分岐の並べ替え | Medium | Low |
| 17 | **検証済み** | マジックナンバー | misc3.c:256-275, 679-808 | `stat_adj`/`tohit_adj`/`toac_adj`/`todis_adj`/`todam_adj` が 4/7/17/18/94/117/118 等の閾値をif連鎖で直書き。同じ境界値が5関数に散在 | テーブル化 | Medium | Low |
| 18 | データの散在 | `externs.h` の 67 個（着手時 112。misc3.c:369-540, dungeon.c:580-780 ほか） | プレイヤー状態値が py.misc / PY_* ビット / 画面座標 に分散。**2026-09-09 に全体像を計測 → [GLOBALS_INVENTORY.md](GLOBALS_INVENTORY.md)** | 区分ごとに分割して着手（下記）。**9 区分のうち 6 つ完了** —— オプション 11 個・メッセージ 4 個・パネル 10 個・店 2 個・持ち物 4 個（区分に残るのは `object_ident` `sorted_objects` の 2 個）・セーブ／スコア／進行メタ 13 個（区分に残るのは `highscore_fp` 1 個。**その 1 個も 2026-09-16 に `death.c` の 2 関数の局所変数にしたが、`init_scorefile()` のために `externs.h` には残る** → バグ候補 B19）。7 区分目「プレイヤー状態 13 個」に着手し、**2026-09-23 に居場所 2 個（`char_row` `char_col`）を `src/player_pos.c` へ**（→ #18-6）、**2026-09-24 に 4 群 6 個**（勝ちと最高得点 → `score_death.c`、レベルごとの HP 表 → `hp_table.c`、明かりの有無 → `player_light.c`、重さ 2 個 → `burden.c`。→ #18-7）、**同日に呪文 4 個を `src/spells_known.c` へ**（→ #18-8）。**区分に残るのは `py` 1 個**（552 参照。1 本の module にはならず、問い単位の窓口で別に進める） | High | 区分ごと |
| 42 | 未着手 | 肥大化クラス／モジュール | misc3.c の残る **10 塊**（→ 上記「#33 の精査」） | 配置 161 行／描画 475 行／能力値の変更 115 行／名前 50 行／持ち物 210 行／呪文 525 行／経験値 82 行／wizard 18 行／戦闘 120 行／移動 118 行 | モジュール分割（1 塊 1 コミット。終点は misc3.c が無くなるところ） | High | High |

### #18 の全体像（2026-09-09 に計測）

`externs.h` の 112 個を `scripts/globals.py` で数えあげた。詳細は
[GLOBALS_INVENTORY.md](GLOBALS_INVENTORY.md)。計画に効く点だけ抜くと:

- **前提が消えた。** この行はもともと「`view_observer.h` に分散」を理由に
  #3 の削除待ちとしていたが、`view_observer.c`（129 行）は `externs.h` の
  112 個を **1 個も参照していない**。#18 と `view_observer` は切り離して
  進められる。
- **1 個の課題ではなく 11 個の課題。** 参照 3147 のうち 47% が
  `py` `cave` `inventory` `char_row` `char_col` `t_list` の 6 個に集まる。
  一括では終わらないので、区分ごとに別項目として扱う。
- **着手順は「安い順」。** オプション 11 個（`misc2.c:856` に既に登録表が
  あり、表の外で書くのは `save.c` と `main.c` の 3 箇所だけ）→ メッセージ
  4 個 → パネル 10 個 → 店・持ち物 → プレイヤー状態 →
  ダンジョン（`cave` の別名 145、単独で 1 プロジェクト）。
  **オプションとメッセージは 2026-09-11、パネルと店は 2026-09-13 に完了
  （下の #18-1 〜 #18-4）。店の別名 23 は `&store[n]` が 22 でアクセサ 1 つで
  足りた。次は持ち物 6 個。`inventory` の別名 105 は種類がばらけており
  （`inven_type *` を渡す形・配列ごと渡す形・`inven_ctr` との不変条件）、
  ここから先は「窓口を作って参照を置きかえる」だけでは済まない。**
- **#18 に混ぜないもの。** `free_turn_flag`（書きこみ 67／14 ファイル）は
  変数の置き場の問題ではなく戻り値にすべき制御フロー。別項目にする。
  実質定数 16 個は `const` 化の話で「散在」ではない。
  逆に「定数表に見えて起動時に書きかわる 7 個」（`colors` 系と
  `object_list`）は `const` 化すると壊れる。
- **`game_state.c` は真実の源ではない。** `game_state_init()` は 40 個ほどの
  グローバルを `state->` に写しとるだけで、しかも `game_state.c` の外から
  呼ばれていない（参照 0 件、→ B3）。#18 は「`GameState` を作る」ではなく
  「この雛形を実体にして写しを消す」作業になる。

#### #18-1 オプション 11 個（2026-09-11 完了）

`src/options.c` の 1 つの表に「表示名・値の置き場・セーブファイルのビット・
既定値」を集め、設定画面（`misc2.c`）・セーブとロード（`save.c`）・
起動（`main.c`）が同じ表を読むようにした。**着手前は 3 箇所（misc2.c と
save.c の書く側・読む側）が同じことを知っていて、一致を保証するものが何も
なかった。** 一致していなければセーブファイルが壊れる。

| | 着手前 | 現在 |
|---|---|---|
| この区分の参照 | 120 | **87** |
| この区分の書きこみ | 26 | **3** |
| `save.c` の if 文 | 22（書く 11・読む 11） | **0**（2 行の呼びだし） |

残る 3 件の書きこみはいずれも表に持たせるものではない。`main.c:185` の
`-r`/`-o`（キー配列の指定。セーブファイルの読みこみより後に効かせる必要が
ある）と、`save.c` の 5.2.2 以前の互換 2 件（そのビットが無い形式なので
on に固定する）。

保護のとりかたは #7 と同じ「テスト側に写す」方式をとった。書きこみ・
読みこみの実体は `sv_write()` / `rd_*` の中に直に並んだ if 文で関数として
呼べないため、「先にテストを書いて本体無変更でグリーン」の形がとれない。
変更前の if 文の並びを `tests/options_test.c` に写しとり、新しい実体と
**2^11 = 2048 通りすべてで突きあわせた**（11 件のテスト）。
#### #18-2 メッセージ表示 4 個（2026-09-11 完了）

`src/messages.c` を作り、`old_msg[22]` `last_msg` `msg_flag`
`wait_for_more` の 4 個を `static` にした。**この区分は `externs.h` から
まるごと消えた**（112 → 108 個。#18 で項目が実際に減ったのはここが最初）。

この区分の本体はリングバッファの**回りかた**だった。同じ規則を 3 箇所が
別々に持っていて、片方だけ直すと履歴が飛ぶかセーブファイルが壊れる。

| 知っていた場所 | 何を知っていたか | 現在 |
|---|---|---|
| `io.c:191` | 次の枠へ進める（+1 が 22 なら 0） | `msg_history_push()` |
| `dungeon.c:1115` | `^P` で古いほうへ辿る（0 の 1 つ前は 21） | `msg_history_recent(back)` |
| `save.c:212,682` | 枠をそのまま並べる（セーブファイルの形式） | `msg_history_slot(i)` |

窓口を 2 系統に分けたのが設計上の要点。読む側が欲しいのは「何個前か」、
セーブファイルが欲しいのは「枠そのもの」。**セーブファイルの形式は変えられ
ないので、生の並びを見る窓口は残さざるをえない**。消すのではなく、用途を
名前で分けて、片方だけがヘッダに「手で回すな」と書いてある状態にした。

`msg_flag` と `wait_for_more` は名前も変えた（`msg_pending()` /
`msg_at_more_prompt()`）。変数名が何の状態かを言っていなかったので、
`store2.c` には「clear the msg flag just like we do in dungeon.c」という
**他所を見にいかせるコメント**が付いていた。そこには理由（次のコマンドを
訊く直前だから `-more-` は要らない）を書いた。

保護は #18-1 と同じ「テスト側に写す」方式（`tests/messages_test.c`、19 件）。
辿りかたは `dungeon.c` の case 文の中で外から呼べないので、変更前の並べかたを
テストに写し、押しこみ 0〜66 回 × 辿り 0〜21 個の全組みあわせで突きあわせた。
`^P` の表示は書きかえで添字の向きが逆になる（行番号と枠を同時に減らす形から、
行番号から「何個前か」を求める形へ）ので、行数 2〜22 × 押しこみ 0〜44 回の
全組みあわせで (行, メッセージ) の対応が変わらないことも確かめた。
意図的に壊してレッドになることを 5 通りで確認している。

`tests/messages_test.c` は `externs.h` を include しない。閉じたことが
テスト側の依存に現れている。

**寄せた効き目が 1 つ出た（#18-2E）。** セーブファイルに書かれた「いまの枠」の
添字は誰も検査していない（`rd_short()` の 0〜65535 がそのまま入る）。変更前は
その値で `old_msg[j]` を直に引いていたので、添字 40000 のセーブファイルでは
`^P` が配列の外を読み、次の `msg_print()` が配列の外へ書いていた。輪への入口が
1 箇所になったので、そこで範囲へ畳むだけで済む。正しい値（0〜21）は同じ値に
畳まれるので、まともなセーブファイルのふるまいは変わらない。**変更前は入口が
3 箇所あったので、どこで検査すべきかを決められなかった。**


#### #18-3 パネル 10 個（2026-09-13 完了）

`src/panel.c` を作り、`panel_row` `panel_col` とそこから決まる 6 個、
それにパネル数 2 個を `static` にした。**この区分も `externs.h` から
まるごと消えた**（108 → 98 個、参照 3073 → 2974）。

この区分の本体は**添字 2 個から座標 6 個が決まる関係**だった。6 個が同じ
`externs.h` に並んでいたので、4 ファイルがそれぞれ計算に加わっていた。

| 知っていた場所 | 何を知っていたか | 現在 |
|---|---|---|
| `misc1.c:153` | 6 個の導出（`panel_bounds`） | `panel.c` の `static` |
| `misc1.c:165` | 動かすかどうかの判定（`get_panel`） | `panel_move_to()` |
| `io.c:134,143` | `*_prt` を引いて画面座標にする | `panel_screen_row/col()` |
| `generate.c:1260` | 4 辺を導出せずに 0 にする | `panel_forget_bounds()` |
| `generate.c:1274,1282` | パネル数の式（同じ 4 行が 2 回） | `panel_set_dungeon_size()` |
| `spells.c` ほか | 4 辺の間を歩く | `panel_top_row()` ほか |

**数の重複が 2 つ消えた。** パネル数の式（町とダンジョンの枝に同じ 4 行）と、
地図の左上の位置。後者は `panel_bounds` の中の 1 と 13、そして `prt_map()` が
自分で数えていた行番号（`k = 0` から 1 行ずつ）と `erase_line(k, 13)` の 13 が
同じものを別々に持っていた形で、いまは `PANEL_MAP_TOP_ROW` /
`PANEL_MAP_LEFT_COL` と `panel_screen_row()` に寄っている。

**副作用は呼びだし側に残した。** `get_panel()` は動いたときに `end_find()` を
呼ぶ（走っている足を止める）。この 1 行だけを `misc1.c` に残したので、
`panel.c` はダンジョンにも移動処理にも依存しない。`tests/panel_test.c` が
`panel.c` 1 本だけをリンクできているのはその結果で、閉じたことがテスト側の
依存に現れている（`messages_test` と同じ形）。

**「まだ見ていない」に名前が付いた。** 変更前は 2 つの不正な状態でそれを
表していた —— `dungeon.c` の `panel_row = panel_col = -1` と、
`generate_cave()` の 4 辺 0 埋め。どちらも「次に必ず地図を引きなおさせる」
ための値で、`panel_forget_position()` / `panel_forget_bounds()` になった。
2 つを 1 つにまとめてはいない。呼ぶ場所と時期が違い、まとめるとその間に
挟まる処理（`town_gen()` / `cave_gen()`）の見る状態が変わる。

保護は #18-1 / #18-2 と同じ「テスト側に写す」方式（`tests/panel_test.c`、
12 件）。変更前の `panel_bounds` / `get_panel` / `panel_contains` を写しとり、
出発点 28 通り（正しいパネル位置すべてと「忘れた」形 3 通り）× ダンジョン
全域の外 4 マスまでの座標 × `force` の両方で、戻り値と座標 6 個すべてを
突きあわせた。町（1 パネル）とダンジョン（5x5 パネル）の両方。意図的に
壊してレッドになることを 4 通りで確認している。

**テストの出発点は窓口だけで作る（ステップ C）。** 10 個が `static` に
なった時点で生の代入ができなくなったので、出発点は窓口越しの手順書で作り、
写しの側にも同じ順で同じ操作をあてる形にした。その過程で、変更前の写しに
**本体がなりようのない状態**（4 辺を `-1` のパネルから導出した形）が
混ざっていたことがわかった。運べる状態しか出発点にしないので、いまは
「本体が実際になりうる状態」だけを見ている。


#### #18-4 店 2 個（2026-09-13 完了）

`src/stores.c` を作り、6 軒の記録 `store[MAX_STORES]` を `static` にした。
値切りの途中の入力 `last_store_inc` は `store2.c` の `static`。**この区分も
`externs.h` からまるごと消えた**（98 → 96 個、参照 2974 → 2886）。

**この区分には計算が無かった。** パネル（#18-3、添字から座標が決まる関係）や
メッセージ（#18-2、輪の回りかた）と違って、`store` は 6 軒ぶんの記録
そのもので、導出される値も丸めの規則も持たない。窓口を作っても隠せる計算が
無いので、得られたのは「置き場の持ち主が 1 つになる」ことに尽きる。
区分ごとに得るものが違う、という例として残す。

そのかわり同じ 2 行が 22 か所にあった。

| 知っていた場所 | 何を知っていたか | 現在 |
|---|---|---|
| `store1.c` 10 箇所 | `store_type *s_ptr = &store[n]`（在庫の出し入れと値づけ） | `store_at(n)` |
| `store2.c` 9 箇所 | 同じ 2 行（店に入って買う・売る） | `store_at(n)` |
| `save.c` 3 箇所 | 同じ 2 行＋「6 軒ぶん並べて読み書きする」 | `store_at(i)` / `store_count()` |
| `game_state.c:40` | 配列の先頭をそのまま控える | `store_at(0)` |

別名 23 のうち 22 がこの形で、「`store_type` へのアクセサが要る」という
当初の見立ては当たっていた（`store_at()` 1 つで足りた）。

**もう 1 つ数の重複が消えた（#18-4C）。** セーブデータの店 1 軒の並び
（項目 6 つと、在庫のある枠だけ値段と品物）が `save.c` の 3 か所にあり、
読む 2 か所は字下げをのぞいて同一の 17 行だった（いまの形式と 5.1.3 より前の
形式が同じ並びなので、同じものが 2 度書かれていた）。品物 1 つは `wr_item` /
`rd_item` という関数になっていたのに店 1 軒はなっていなかったので、同じ形に
揃えて `wr_store()` / `rd_store()` にした。在庫数の妥当性の判定
（`> STORE_INVEN_MAX` なら読みこみを投げだす）も関数の中に入り、投げだす
合図だけが戻り値になっている。

`last_store_inc` には 15 年前から「なぜ `extern` なのか、`store2.c` しか
使っていない」という FIXME が付いていた。値切りの 3 関数だけが使う値で、
店の在庫とは別のもの（「+50」のような増分入力を覚えておき、次に空行を
打ったら同じ増分をもう一度使う）。区分の名前が「店」なので同じ引きだしに
入っていたが、運ぶ先は違った。

保護のとりかたは 2 通りに分かれた。**置き場そのものには写す相手がいない**
ので、`tests/stores_test.c`（8 件）は「置き場としてのふるまい」を押さえて
いる —— 軒数、添字ごとに別の記録であること、記録が重なっていないこと、
同じ添字がいつでも同じ記録を指すこと、書いたものが残ること、在庫 24 枠が
最後まで収まること、走りだしが空であること。「配列だから当然」だったことが
窓口越しでは当然でなくなる、というのが要点で、実際、写しを返す実装にすると
4 件落ちる。

セーブデータのほうは #18-1 / #18-3 と同じ「テスト側に写す」方式
（`tests/store_save_test.c`、7 件）。変更前の 3 か所を写しとり、写しと実体が
書いたバイト列が 1 バイトも違わないこと、写しが書いたものを実体が読んで
記録が丸ごと戻ること（およびその逆）、在庫数がありえない値なら断り満杯なら
断らないことを見ている。記録の比べかたは `memcmp` で丸ごと —— 読む前に 0 で
埋めておくので、復元しそこねた項目が 1 つでもあれば落ちる。

在庫の出し入れ（`store1.c`）と値切り（`store2.c`）は動かしていない。前者は
`t_list` や `object_list` を、後者は画面と入力を連れてくるので、一緒に運ぶと
`stores.c` が 1 本きりでリンクできる潔さが保てない。

#### #18-5 持ち物 4 個（2026-09-14 完了）

`src/inventory.c` を作り、`inventory[INVEN_ARRAY_SIZE]` `inven_ctr`
`inven_weight` `equip_ctr` の 4 個を `static` にした（**96 → 92 個、参照
2886 → 2527**）。この区分は 6 個のうち **2 個**（`object_ident`
`sorted_objects`）が残る —— どちらも持ち物 4 個と独立で、鑑定と床の物
（`t_list`）の側につながっている（実測は上記「#18-5（持ち物 6 個）の実測」）。

| | 着手前 | 完了後 |
|---|---:|---:|
| `externs.h` の global | 96 | **92** |
| この区分の個数 | 6 | **2** |
| 直接参照（`src/*.c`） | 354 | **0** |
| 窓口の呼びだし（`src/*.c`） | — | **354** |
| テスト件数（全体） | 403 | **428** |
| 実行形式 | 17 | **18** |
| 警告 | 0 | 0 |

コミットは **48 本**（`develop..HEAD`）。#18 の 5 区分のうちいちばん大きい。

**窓口は 3 系統・13 個。** 持ち物と装備に割ったのはユーザーの判断（上記
「窓口を持ち物と装備に割る」）。実体は 1 本の配列で、割ったのは窓口だけ。

| 系統 | ヘッダ | 窓口（呼びだし件数） | 計 |
|---|---|---|---:|
| 持ち物 0..21 | `src/inventory.h` | `inventory_at` 65 / `inventory_count` 63 / `inventory_weight` 18 / `inventory_set_weight` 13 / `inventory_slot_count` 5 / `inventory_set_count` 5 | **169** |
| 装備 22..33 | `src/equipment.h` | `equipment_at` 119 / `equipment_count` 22 / `equipment_first_slot` 12 / `equipment_end_slot` 6 / `equipment_set_count` 4 | **163** |
| 跨ぎ | `src/inventory.h`（跨ぎ専用と明記） | `inventory_and_equipment_at` 15 / `inventory_and_equipment_slot_count` 7 | **22** |

**跨ぎ 22 件（6%）・10 ファイル。割ったままにする。** 着手前の見立ては 16 件で、
実測はそれより多い。それでも全体から見れば例外の位置にとどまった。
`inventory_and_equipment_at` という長い名前が「ここは添字空間の全域を
走っている」という警告として働き、跨ぎが名前で目立つ形になっている。1 本に
まとめ直すと 354 件すべてが同じ顔になり、この情報が消える。

**ステップ A（`0b96757`、1 コミット）。** `src/inventory.c` 85 行と
`inventory.h` 55 行・`equipment.h` 45 行を置き、`tests/inventory_test.c`
338 行・25 件で「置き場としてのふるまい」を押さえた（#18-4 の
`stores_test.c` と同じ形。この区分にも隠せる計算が無いので、写す相手がいない）。
本体は 1 箇所も呼ばない＝ふるまい不変。**実体は `treasure.c` に残したまま**で、
窓口は `extern` 越しにそれを読み書きする。足場は
`tests/inventory_fixture.c` 31 行（ステップ C で消えた）。

**ステップ B（42 コミット `76e5f5f..646a52c`、6 群）。** 呼びだし側 354 箇所を
窓口越しに書きかえた。着手前の計画は「ファイル単位で 5 分割」だったが、
実測で 6 群になった。

| 群 | 対象 | 件数 | 跨ぎ | コミット |
|---|---|---:|---:|---|
| 1 | `moria1.c` | 98 | 1 | B1-B6 `76e5f5f..f6b813c` |
| 2 | `misc3.c` | 41 | 1 | B7-B11 `b15ce9d` `0f78f3c` `97931d4` `c8372b5` `9c67e02` |
| 3 | `scrolls.c` 46 ＋ `desc.c` 13 | 59 | 1 | B12-B15 `e5595ae` `bb5597b` `aac2842` `11038d3` |
| 4 | `moria4.c` 24 ＋ `moria2.c` 9 ＋ `moria3.c` 6 | 39 | 0 | B16-B20 `fbb5392..154daed` |
| 5 | `save.c` `dungeon.c` `files.c` `death.c` `main.c` | 46 | 10 | B21-B30 `0dbaf85..5a3affc`（10 本） |
| 6 | 残り 11 ファイル ＋ `game_state.c` | 36 | 10 | B31-B42 `56bef1d..646a52c`（12 本） |

**6 群に分けた理由は「跨ぎの寄りかた」だった。** 群 1〜4（237 件）で跨ぎは
3 件しか出ず、22 件のうち 20 件が群 5（直列化と全域ループ）と群 6（残り）に
寄っていた。件数の多いファイルを先に片づけると、機械的に置きかえられる
ものが先に終わり、判断が要る跨ぎが後半に集まる。群 5 を 10 コミットに、
群 6 を 12 コミットに割ったのはこのため（1 コミット 1 ファイルを基本にした）。

**ステップ C（3 コミット）。** `e96f504` で実体 4 個を `treasure.c` から
`inventory.c` へ移して `static` にし、`externs.h` の 4 行を消した。
`b028c17` で `tests/inventory_fixture.c` 31 行と `creature_stubs.c` の代役
4 個を削除。`31c29d7` で `scripts/globals.py` の `GROUPS` から 4 個を外した。
`treasure.c` は **564 → 565 行**——定義 4 行を消し、行き先を指すコメント 4 行を
置いたので 1 行増えている。

**実体を残置してから C で移した二段構えには理由がある。** ステップ A で実体を
`inventory.c` へ移してしまうと、生の名前を使っている 354 箇所が同時に
コンパイルできなくなる ——「窓口を作る」「呼びだし側を書きかえる」「実体を
移して閉じる」が 1 つのコミットに固まる。残置すれば、B の 42 コミットが
どれも単体でグリーンな小さい変更になり、`treasure.c` をリンクする 7 実行形式の
リンク行も B の間は 1 つも変えなくて済む。**閉じるのは「直接参照が 0 になった
後」で、そこが C。**

**意図的な壊しは 3 通り・すべてレッド**（ステップ C。3 通りとも復元して
グリーン復帰を確認）。

| 壊しかた | レッド件数 | 内訳 |
|---|---:|---|
| `inventory_at()` を `&inventory[index + 1]` に | **7** | `item_ident_test` 4・`inventory_test` 3。`inven_stack_test` は **0 件** |
| `equipment_first_slot()` を 22 → 23 に | **3** | `inventory_test` |
| `inventory_set_count()` を空実装に | **16** | `inven_stack_test` 14・`inventory_test` 2 |

**1 つめで `inven_stack_test` が 0 件だったのは穴ではない。** あの 40 件は
「スタックできるか」を枠の中身どうしで判定するので、**全体が同じだけずれると
区別できない**。ずれを捕まえるのは絶対の位置を見るテスト（`inventory_test` の
「添字ごとに別の枠」「同じ添字はいつでも同じ枠」）で、そこは 3 件レッドに
なっている。

#### リンク単位の変化（#38 の指標）

| 実行形式 | #18-5 前 | 完了後 | 何が変わったか |
|---|---:|---:|---|
| `inventory_test`（新設） | — | **2** | `tests/inventory_test.c` ＋ `src/inventory.c`。**代役 0**（ステップ C で `inventory_fixture.c` が不要になり 3 → 2） |
| `movement_rate_test` | 2 | **3** | `creature.c` が窓口を呼ぶので `src/inventory.c` を足した。代役に `inven_weight` と `equip_ctr` の 2 行を足すだけで済み、`externs.h` は引かない |
| `item_ident_test` | — | +1 | `src/inventory.c` を追加（群 3） |
| `save_bool_test` / `store_save_test` | — | +1 | 同（群 5） |
| `treasure.c` を引く 7 本 | — | **±0** | **リンク行が 1 つも変わらずに済んだ**（`treasure.c` の変更はコメントだけ） |

**#41 で見えた二面性がここでも出た。** 新設の `inventory_test` は 2 単位・
代役 0（`str_insert_test` と並ぶ最小構成）で済んだが、既存の 4 本は
`src/inventory.c` を足すぶん 1 本ずつ重くなっている。ただし #41 のときと違い、
**代役が減る側にも動いた** —— `inventory_fixture.c` 31 行と
`creature_stubs.c` の 4 個の代役が消えた。窓口が `static` な実体を持つと、
足場で偽の定義を置く必要が無くなるためで、**分割（#42）ではなく
カプセル化（#18）のほうがこの指標に効く**。

#### 予測と実測のずれ（「記録された数字を前提にせず測りなおす」の実例）

着手前の再計測（2026-09-13、上記「#18-5（持ち物 6 個）の実測」）でも 6 箇所
直したが、実際に手を入れると**さらに 6 箇所ずれた**。

| 台帳・偵察の予測 | 実測 |
|---|---|
| 参照 379 件（装備 112・持ち物 64・跨ぎ 16） | 窓口の呼びだし **354 件**（装備 163・持ち物 169・跨ぎ **22**） |
| `desc.c` 21 件 | **19 件**。うち `object_ident` の 6 件は窓口の対象外なので、置きかえは **13 件** |
| 別名は `&inventory[x]` の 1 形に収束 | **正しかった。** 6 群すべてで機械的に置きかえられた |
| `save.c` の直列化は跨ぎ | **誤り。** 持ち物と装備で 2 本の別ループだったので分けられた（跨ぎ **0**） |
| `moria4.c` の中枢は `inven_command` | **誤り。** `inven_command` は `moria1.c` 側。`moria4.c` の 24 件は `tunnel` / `facts` / `throw_object` / `py_bash` に散在 |
| `src/` の直接参照が 0 なら `static` 化できる | **不足。** `tests/` に **41 件**残っていた（`inven_stack_test.c` 22・`item_ident_test.c` 6・`misc3_stubs.c` 3・`fixture.c` 2、`creature_stubs.c` の重複定義 4）。**ステップ B の完了判定を `src/*.c` に限って測っていた見落とし** |

最後の 1 件は地雷として `HANDOVER.md` 第 7 節に入れた。**`static` な実体と、
別の翻訳単位にある代役の非 `static` な定義は multiple definition にならない**
——リンカは黙って両方を置くので、足場を消し忘れると「窓口越しの読み書きが
届かない代役」が生き残り、テストは通るのに何も検証しない状態になる。今回は
`undefined reference` 41 件が先に出たので気づけたが、順序が違えば見逃していた。

#### `save.c` の 1 件だけは字面の置きかえにならなかった

`rd_short((uint16_t *)&inven_ctr)` は窓口に設定関数しか無く、`rd_short` が
アドレスを取るので、そのままでは置きかえられない。局所の `uint16_t` に
読んでから `inventory_set_count((int16_t)tmp)` へ渡す形にした（同じ関数内の
先例 `msg_history_set_newest_slot()` と同じ形。#18-2E / #18-3B でも同じ
キャストを外している）。**0..65535 の全域でビット直書きと `(int16_t)`
キャストの一致を独立に確認した（mismatch=0）。** 副産物として、変更前に
あった型を越えたアドレス操作（strict aliasing 違反）が消えた。**セーブ形式
（順序・個数・型）は不変。**

#### 実動作の突きあわせ

変更前の実行形式を別に作り、同じ入力列で町まで降りて `i`（持ち物一覧）を
押した。**`Rations of Food` / `Wooden Torch` / `Dagger` / `Cloak` と
`"You are carrying"` の行が一致**した（差は能力値のロール＝乱数の種のみ）。
テストは置き場としてのふるまいを押さえるが、354 箇所の置きかえで添字を
1 つ取りちがえていないことは実際に持ち物を表示させるのがいちばん早い。

#### #19 セーブ／スコア／進行メタ 13 個（2026-09-15 完了）

`src/progress.c` `src/score_death.c` `src/save_state.c` の 3 本を作り、14 個の
うち **13 個**を `static` にした（**92 → 79 個、参照 2527 → 2253**）。残る
1 個は `highscore_fp` で、これは**そもそも global である必要が無い** 1 個
（`display_scores()` と `highscores()` が 1 つの関数の中で `fopen` して
`fclose` する）。**2026-09-16 にその 2 関数を局所変数にした**（→ 下の
「#19-5-1」。`externs.h` には `init_scorefile()` のために残るので個数は 79 の
まま）。

| | 着手前 | 完了後 |
|---|---:|---:|
| `externs.h` の global | 92 | **79** |
| この区分の個数 | 14 | **1**（`highscore_fp`） |
| 直接参照（`src/*.c`、13 個分） | 277 | **0** |
| 窓口の呼びだし（`src/*.c`） | — | **171** |
| テスト件数（全体） | 428 | **514** |
| 実行形式 | 18 | **21** |
| 警告 | 0 | 0 |

コミットは **11 本**（うち 1 本はバグ候補 B18 の記録のみ）。置きかえた 277 件が
窓口の呼びだし 171 件に減っているのは、`if` 文に直に書かれていた式を述語 1 つに
畳んだぶん（3 つの旗を読む式が 1 回の呼びだしになる）と、`savefile` の
1 箇所で複数回参照していた行があるため。

**区分の名前が 3 つの用途を束ねていたので、module も 3 本に割った。**
`GLOBALS_INVENTORY.md` の見立て（「1 つの module に全部入れる前に行き先を
3 つに分けて考える」）どおりで、割ってよいかを実測で確かめてから決めた
——**`src/` 全体で 3 群を混ぜる式は 1 つだけ**（`signals.c:199` の
`!death && !character_saved && character_generated`）。共有の芯を 3 本の下に
置くと呼びだし 1 箇所のための層になるので、**跨ぐ 2 つの問いを
`save_state.c` に置いて名前を付けた**（`save_state.c` が `progress.h` と
`score_death.h` を読む。逆向きは無いので矢印は一方向）。

| module | 記録 | 窓口の呼びだし（`src/*.c`） |
|---|---|---:|
| `src/progress.c` | `turn` `randes_seed` `town_seed` `wizard` `to_be_wizard` | **47**（`progress_turn` 13・`progress_wizard_mode` 9・`progress_set_turn` 6 ほか） |
| `src/score_death.c` | `death` `died_from` `birth_date` `noscore` | **75**（`death_cause` 19・`player_is_dead` 17・`score_disqualifications` 17 ほか） |
| `src/save_state.c` | `savefile` `character_generated` `character_saved` `panic_save` | **49**（`save_file_path` 22・`save_state_character_is_in_play` 6 ほか） |

**述語 3 つが名前を得た。** 変更前は `if` 文の中に直に書かれていた式で、
関数として呼べないので #7（`device.c`）・#18-1（`options.c`）と同じ
「テスト側に写す」方式で保護した（写した式と入力の全通りで突きあわせ。
旗 2 つは 4 通り、3 つは 8 通りで総当たり）。

| 窓口 | 変更前の字面 | 呼びだし |
|---|---|---:|
| `save_state_has_unsaved_character()` | `character_generated && !character_saved`（3 箇所に同じ字面） | 3 |
| `save_state_has_live_character()` | `!death && !character_saved && character_generated` | 1 |
| `save_state_character_is_in_play()` | `turn >= 0`（5 箇所） | 6 |

**`turn` の符号は 2 種類の境目を持つ。** `turn >= 0`（キャラクターが場にいるか）と
`signals.c:170` の `turn > 0` は `turn == 0` で答えが分かれる。走りだしの `-1` は
turn ではなく、主ループが最初の周で 0 に上げるので **0 は本物の turn**。
まとめると壊れるので、境目そのものをテストで固定した（`save_state_test` の
`turn_zero_counts_as_being_in_play`）。

#### `save.c` の 55 箇所と、そこで消えた 4 件のキャスト

着手前の別名 8 件のうち 2 件は `globals.py` の誤検出（→ P3 #45）、**残る 6 件は
すべて `save.c` の読みこみの受け先**という見立ては**正しかった**。窓口には
設定関数しか無く `rd_*` にアドレスを渡せないので、局所変数に読んでから
設定関数へ渡す形にした（先例 `inventory_set_count()`・
`msg_history_set_newest_slot()`）。

    rd_long((uint32_t *)&turn)        → uint32_t saved_turn 経由
    rd_long(&randes_seed) / (&town_seed) → 局所の種 2 つ経由
    rd_bool(&panic_save)              → bool 経由
    rd_short((uint16_t *)&noscore)    → uint16_t 経由
    rd_long((uint32_t *)&birth_date)  → uint32_t 経由

**型を越えたポインタのキャストが 4 件消えた**（`&turn` `&noscore`
`&birth_date` と、`bool` に 2 バイト書いていた `panic_save`）。
`rd_string(died_from)` はアドレスを返す窓口なのでそのまま渡せる。
**セーブファイルの形式（順序・個数・型）は 1 バイトも変えていない。**

#### 保護の限界を正直に書く（→ P3 #49）

`save.c` の 55 箇所（#19B4）では、**意図的な壊し 4 件が 514 件グリーンのまま
生き残った**（書きだしの `turn` を 0 にする／読みこんだ `turn` を捨てる／
種 2 つを入れちがえる／`character_is_saved()` の判定を落とす）。
`save_bool_test` が見ているのは `wr_short` / `rd_bool` という**部品**、
`store_save_test` が見ているのは**店の塊**だけで、`sv_write()` の 150 件と
`get_char()` の 151 件の**項目の並びを見るテストは 1 本も無い**。
**これは #19 が作った穴ではなく、もとから空いていた穴**なので P3 #49 に記録した。

代わりに実物のセーブファイルで両方向を確かめた。(1) 変更後の binary が作った
セーブを 1 つ前の binary が読める、(2) その逆も読める、(3) 確認に歯があることの
証明として `wr_short(noscore)` を 1 行落とした mutant のセーブは
「Error during reading of file.」で弾かれる。**pty 越しにキー列
`am ESC a Probe RET SPC ^X` を流すだけで再現できる**（tmux は要らない）。

#### ステップ C は #18-5C より静かだった

`src/` と `tests/` の直接参照を**両方 0 にしてから**実体を移したので、
`static` 化でリンカのエラーが **1 件も出なかった**（#18-5C1 では
`undefined reference` が 41 件出て、そこで初めて `tests/` の残りに気づいた）。
**足場 3 本（`tests/*_fixture.c` 88 行）は二重定義にもならずに残った**
——`static` は翻訳単位の中だけの名前なので、足場の非 `static` な同名は
「誰も参照しない別の記録」になる。実測でも確かめた（足場の `turn` を
`-1` → `12345` にしても failed=0。本体側を同じように壊すと 1 件レッド）。
**#18-5 の地雷（`HANDOVER.md` 第 7 節）が、今度は静かな側に出た形。**

| 実行形式 | #19 前 | 完了後 | 何が変わったか |
|---|---:|---:|---|
| `progress_test`（新設） | — | **2** | テスト 1 本＋`src/progress.c`。**代役 0** |
| `score_death_test`（新設） | — | **2** | 同上 |
| `save_state_test`（新設） | — | **4** | テスト 1 本＋module 3 本。**跨ぐ述語 2 つの代金**として隠さずに数える |
| `save_bool_test` / `store_save_test` | 12 | **15** | 3 本を足した（`variable.c` の本物を引いているので足場は不要だった） |
| `inven_stack_test` / `put_misc3_test` / `objdes_test` | 12 | **12** | ステップ B で +3、ステップ C で足場 −3 |
| `haggle_comment_test` | 15 | **15** | 同（+3 → −3） |
| `movement_rate_test` | 3 | **4** | `progress.c` と `score_death.c` を足し、足場 −1 |
| `item_ident_test` | 8 | **8** | `progress.c` を足し、足場 −1 |

**#41 で見えた二面性の 3 例目。** 新設 3 本のうち 2 本は最小構成（2 単位・
代役 0）で済み、既存 6 本はステップ B で重くなったぶんをステップ C の足場削除で
取りもどした。**カプセル化（#18・#19）はこの指標に効く**という #18-5 の所見が
再現した。

#### #19-5-1 `highscore_fp` 1 個（2026-09-16 完了）

区分の 14 個目。**窓口を作る話ではなく「global である必要が無い」話**だった。
3 通りの行き先（`HANDOVER.md` 第 5 節に並べた）のうちユーザーが選んだのは
**1 番 —— `death.c` の 2 関数だけ局所変数にし、`externs.h` からは消さない**。

| | 着手前 | 完了後 |
|---|---:|---:|
| `externs.h` の global | 79 | **79**（減らない） |
| `highscore_fp` の参照 | 43 | **7** |
| 書きこみ | 3 | **1**（`files.c:29` のみ） |
| 参照の合計（全 global） | 2253 | **2215** |

`display_scores()`（`"rb"`）と `highscores()`（`"rb+"`）はどちらも 1 つの関数の
中で `fopen` して `fclose` する。外から読む人はいない（`rd_highscore()` は
`set_fileptr()` 越しに受けとる）ので、38 箇所を `FILE *score_fp` にした。
**名前を変えたのは `-Wshadow` が global と同名の局所変数を拾うから** ——
`highscore_fp` のままだと警告 0 を保てない。

**個数が減らないのに意味がある変更だった。** 残った書きこみが
`init_scorefile()`（`files.c:28`）の 1 箇所だけになったことで、**setuid の権限が
あるうちに開いた handle が誰にも読まれず閉じられもしない**ことが字面に出た。
以前は 2 関数が同じ global を上書きしていたので「開いたものが捨てられている」
と読めたが、いまは上書きする人すらいない。**意図（権限のあるうちに開く）は
最初から果たされていなかった**——権限を落としたあとに 2 関数が開きなおすので、
権限の要る環境で困るのは `highscores()` 側。バグ候補 **B19** として記録し、
直していない（この 1 個を `externs.h` から消すのは、その判断とセットになる）。

**単体テストが 1 件も届かない場所だった。** `death.c` をリンクする実行形式が
無いので、テスト 514 件は 1 件もこの経路を通らない（`makefile.test` を見れば
分かる。「グリーンだから安全」ではない —— P3 #49 と同じ穴の別の形）。
かわりに**実物の `scores.dat` を変更前後の binary で両方向に読みかえた**：

| 走らせたこと | 結果 |
|---|---|
| 変更前が 1 人ぶん書く → 両方の `-s` で表示 | **76 バイト**、出力が 1 バイトも違わない |
| そこへ変更後が追記 → 両方の `-s` で表示 | **149 バイト**、2 行とも一致 |
| 逆順（変更後が書いて変更前が追記） | 同じく 76 → 149 バイト、表示も一致 |

`git worktree` で変更前（`3aa8ed8`）を建て、pty ごしにキャラクタを作って `^K`
で終わらせるところまで走らせている。**手順に 2 つ罠があった**ので
`HANDOVER.md` 第 7 節に写した ——（1）キーをまとめて流しこむと端末の用意が
終わる前に食われて種族選択の手前で止まる（1 文字ずつ間をあける）、
（2）**終了キーは `^K` で `Q` ではない**（既定の割りあてでは
`original_commands()` が `^K` を `'Q'` に訳す側なので、`Q` を直に送っても
効かない）。

#### #18-6 プレイヤー状態のうち居場所 2 個（2026-09-23 完了）

区分「プレイヤー状態 13 個」の 1 群目。`src/player_pos.c` を作り、
`char_row` と `char_col` を `static` にした（**79 → 77 個、参照 2215 → 1913**）。

| | 着手前 | 完了後 |
|---|---:|---:|
| `externs.h` の global | 79 | **77** |
| 参照の合計（全 global） | 2215 | **1913** |
| 区分「プレイヤー状態」 | 13 個・960 参照・書き 167・別名 106 | **11 個・658・157・100** |
| `src/` の直接参照 | 302 | **0**（module の `static` 2 個だけ） |
| テスト | 514 件・21 実行形式 | **531 件・22 実行形式** |

**`py` に寄せるかどうかはユーザーの判断だった。** 着手前に 3 通り
（`py.misc` に入れる／位置だけの module を作る／`py` 全体の窓口に混ぜる）を
並べて問い合わせ、**「位置だけの module」**を選んでもらった。これで
**`py` の粒度の決定と独立に進められた** —— 区分に残る `py`（552 参照）は
**#19 方式の「問い単位の窓口」**で別に進める。着手前の見立て
（[GLOBALS_INVENTORY.md](GLOBALS_INVENTORY.md) の表）は「`py` に寄せるのが
最も効く一手」だったが、**効いたのは `py` に寄せることではなく「読みを
1 つの問いに畳むこと」だった。**

**中身は「読むだけの 284 箇所」だった。** 302 箇所のうち読みが 284 件
（`cave[char_row][char_col]` や `los(char_row, char_col, y, x)` のように
「いまの居場所」をそのまま引数にしているだけ。計算は 1 つも無い）、
書きは **5 組 10 件**で、**行と列を別々に動かす箇所は 1 つも無かった**。
だから窓口は 4 つで足りた：

| 窓口 | 呼びだし | 役 |
|---|---:|---|
| `player_row()` / `player_col()` | **284** | いまの居場所を読む |
| `player_place(row, col)` | 7 | 行と列をまとめて置く |
| `player_pos_forget()` | 1 | 階を作る前に控えを捨てる |

**名前は呼びだし側の読みやすさで決めた。** module の接頭辞にそろえるなら
`player_pos_row()` だが、284 箇所が `los(player_pos_row(), …)` になる。
**読みだけプレイヤーを主語にした短い名前**にし、その理由を
`src/player_pos.h` に英語で書いた（書きこむ側は接頭辞を保っている——
誰がプレイヤーを動かすのかを名前で探せるようにするため）。

**`generate.c` の `-1` は番人ではなかった。** 新しい階を作る前に
`char_row = -1; char_col = -1;` と置く箇所があるが、**`src/` 全体で位置を
`-1` や `0` と比べる箇所は 1 つも無い**。書いた値が読まれないので、窓口も
`player_pos_forget()` という名前で「忘れる」だけを表し、「どこにもいない
状態」を作らなかった。

**セーブの別名 2 件でポインタのキャスト 2 件が消えた。**
`rd_short((uint16_t *)&char_row)` は型を偽っていたので、局所変数に読んで
から `player_place()` に渡す形にした（#19B4 でやったのと同じ字面）。
**セーブ形式は 1 バイトも変えていない。**

**実物で突きあわせるときは「乱数を引かない経路」を選ぶ。** この区分で
手順の限界が分かった。歩く・探索する・セーブを読みこむ経路は決定的なので、
`git worktree` で建てた変更前の binary と画面が 1 バイトも違わないことを
確かめられる（B2・B4・B5・C1 でそうした）。一方**ウィザードモードの
テレポートは同じ binary で 2 回流しても画面が違う**（種を時計から取る）。
**セーブファイル同士のバイト比較も使えない** —— 4 バイト目が XOR の種で、
中身が同じでも全バイトが違って見える。比べるのは復元した画面のほう。

**#38 の二面性、4 例目。** 新設 1 本は最小構成（**2 単位・代役 0**）だが、
既存 7 本は窓口をリンクするぶん 1 単位ずつ重くなった：

| 実行形式 | 着手前 | 完了後 |
|---|---:|---:|
| `player_pos_test`（新設） | — | **2**（代役 0） |
| `haggle_comment_test` | 16 | **17** |
| `save_bool_test` / `store_save_test` | 15 | **16** |
| `inven_stack_test` / `put_misc3_test` / `objdes_test` | 14 | **15** |
| `movement_rate_test` | 5 | **6** |

**今回は足場を引いて取りもどせなかった。** #18-5 や #19 では代役の定義を
消せたぶんで相殺できたが、`tests/creature_stubs.c` は他の代役が残るので
ファイルごと消せず、消えたのは 2 行だけ（説明のコメントに置きかえた）。
**窓口の数が少ない群ほど、この指標では損に見える**ということ。

#### #18-7 プレイヤー状態の残り 4 群 6 個（2026-09-24 完了）

区分「プレイヤー状態」の 2〜5 群目。ユーザーの指示で **`py` 以外を 1 本の
ブランチにまとめて**進めた（`refactor/18-7-player-rest`、26 コミット）。

| | 着手前 | 完了後 |
|---|---:|---:|
| `externs.h` の global | 77 | **71** |
| 参照の合計（全 global） | 1913 | **1850** |
| 区分「プレイヤー状態」 | 11 個・658 参照・書き 157・別名 100 | **5 個・595・139・97** |
| テスト | 531 件・22 実行形式 | **612 件・27 実行形式** |

| 群 | 個数 | 行き先 | 覚えていること |
|---|---:|---|---|
| #18-7-1 | 2 | 既にあった `src/score_death.c` | 勝ったか（`total_winner`）・最高得点（`max_score`） |
| #18-7-2 | 1 | 新設 `src/hp_table.c` | レベルごとの HP の段（`player_hp`、1 起点で読む表） |
| #18-7-3 | 1 | 新設 `src/player_light.c` | 明かりを持っているか（`player_light`） |
| #18-7-4 | 2 | 新設 `src/burden.c` | 武器が重すぎるか（`weapon_heavy`）・荷が速度を何段落としているか（`pack_heavy`） |

どの群も **A（窓口とテスト。呼びだし側も実体も動かさないのでふるまい不変）→
B（呼びだし側）→ C1（実体を `static` に移す）→ C2（足場を消す）→
C3（`globals.py` の区分から外す）** の順。

**4 個は「計算すれば出るのに覚えている値」だったが、置き場のまま残した。**
呼びだし側が必要としているのはいまの値ではなく**変わり目**だから
（`check_strength()` は速度を `change_speed(新しい段数 − 覚えた段数)` という差で
動かし、`dungeon()` は明るい／暗いの変わり目でモンスターを出しなおす）。
窓口のヘッダに “This is storage and only storage” と書き、範囲を検査しない
ことも明記した（古い代入も検査していなかった）。詳しくは
[GLOBALS_INVENTORY.md](GLOBALS_INVENTORY.md) の所見 16。

**テストが 1 件も届いていない関数 2 本を、窓口に替える前に押さえた。**
`calc_hitpoints`（13 件）と `check_strength`（24 件）。どちらも戻り値が無く、
結果が message・速度・画面にしか出ないので、代役（`tests/misc3_stubs.c`）に
渡された値を記録させて `tests/fixture.h` の窓口から読む形にした
（`change_speed` と `calc_bonuses` に記録を足した。`msg_print` に対して
既にやっていた手の延長）。**本体を 1 行も変えない状態で先に通す**のが要点。

**実物で作れない状態は実物で確かめなかった。** 重い荷（上限は STR 17・体重 166 で
2376）に届かせるには店で 2000 単位ぶんの鉄杭を買う必要があり、盲打ちのキー列では
組めなかった。その 4 通りの分岐は単体テストで押さえ、実物は「ふつうの 1 回の
プレイが踏む道が変更前と同じ」ことの確認に使った（装備 → 掘る → 保存・読みこみで
**2981 バイト**が一致）。窓口がその道で生きていることは実物の変異で見た。

**足場は 3 つ作って 3 つ消した**（`hp_table_fixture.c`・`player_light_fixture.c`・
`burden_fixture.c`）。残すと窓口越しの読み書きが届かない器が生き残る。

残りは `py`（552 参照。**問い単位の窓口**で別に進める）と呪文 4 個
（`spell_learned` `spell_worked` `spell_forgotten` `spell_order`）。

### #18-8 覚えている呪文 4 個を `src/spells_known.c` に閉じた（2026-09-24 完了）

`spell_learned`（17 参照）`spell_worked`（8）`spell_forgotten`（9）
`spell_order`（9）の 4 個。**行き先（1 本の module にまとめる）とビット演算の
扱い（窓口の内側に隠す）はユーザーに問い合わせて決めた。**

| | 着手前 | 完了後 |
|---|---:|---:|
| `externs.h` の global | 71 | **67** |
| 参照の合計（全 global） | 1850 | **1808** |
| 区分「プレイヤー状態」 | 5 個・595 参照・書き 139・別名 97 | **1 個・552・126・88** |
| テスト | 612 件・27 実行形式 | **685 件・30 実行形式**（新設 3 本 —— `calc_spells_test`・`gain_spells_test`・`spells_known_test`） |

| 段 | 何をしたか |
|---|---|
| A1 `502a348` | `calc_spells`（`misc3.c:1220`）にテスト 17 件。**単体テストが 1 件も届いていなかった** |
| A2 `0b8555d` | `gain_spells`（`misc3.c:1374`）にテスト 13 件。同じく 0 件 |
| A3 `90f5d07` | `src/spells_known.c` ＋ `.h` ＋ テスト 43 件（足場 `tests/spells_known_fixture.c`） |
| B1 `b6be3b4` | `misc3.c` の外の 5 ファイル（`magic.c` `prayer.c` `main.c` `moria3.c` `save.c`）13 行 |
| B2 `73bd5b0` | `misc3.c` 25 行とテスト 2 本。**`src/` から直接参照が消えた** |
| C `dd50206` | 実体 4 行を `player.c` から `static` に移し、`externs.h` の 4 行と足場を削除。`globals.py` の区分から 4 個を外した |

**窓口は「global の数」でも「操作の数」でもなく、不変条件の単位で切った。**
この 4 個には、別々の global なのに片方だけ動かしてはいけない組み合わせが
2 つあった —— 忘れるときの「覚えた印を降ろす＋忘れた印を立てる」
（`calc_spells` に 2 か所、思いだす側にも 1 か所）と、覚えるときの
「覚えた印を立てる＋覚えた順に積む」（`gain_spells` に 2 か所）。どちらも
たまたま 2 行が隣にあるだけで、**対であることはコードのどこにも
書かれていなかった。** `spell_forget()` `spell_remember()` `spell_learn()` の
3 つにまとめたので、片方だけ動かす書きかたが存在しなくなった。詳しくは
[GLOBALS_INVENTORY.md](GLOBALS_INVENTORY.md) の所見 17。

**呼びだし側から消えた雑音**: 覚えた順に積む位置を探す 6 行（`last_known`）／
「99 でシフトすると結果が決まらない」ための 6 行 × 2 か所／32 回まわして
覚えている数を数える 5 行／同じ `1L << i` を書いていた 3 か所。

**テストが 1 件も届いていない関数 2 本を、窓口に替える前に押さえた**
（#18-7 と同じ手。`calc_spells` 17 件・`gain_spells` 13 件）。どちらも戻り値が
無く、結果が覚えた印・message・`py.flags.new_spells`・魔力にしか出ない。
`gain_spells` は「どれを覚えるか」をキー入力で選ぶので、代役の `get_com` に
テストから並べたキーを返させる窓口（`fixture_set_get_com_keys`）を足した。

**見立ては外れていた。** `GLOBALS_INVENTORY.md` にはこの 4 個の集まりを
「`magic.c` `prayer.c` `save.c`」と書いていたが、43 参照のうち **25 行が
`misc3.c`** で、`magic.c` と `prayer.c` は 2 行ずつだった。

**意図して古い形と違えたところが 3 つ**（どれもまともなゲームでは起きない道すじ。
理由は英語のコメントに書いた）。覚えた順が満杯なら積みを落とす（古い形は
`spell_order[32]` に書いた）／`spell_learned_nth(負)` は `SPELL_NONE` を返す
（古い形は配列の手前を読んだ）／走りだしの 0 埋めは**そのまま**にした
（`main.c` が `SPELL_NONE` で埋め、セーブから戻すときは 4 つとも上書きされる）。

**変異で測った**（A3 で 8 通り、C で 1 通り）。C の検証では**実体を
`0xFFFFFFFF` にしても全件グリーン**になった —— どのテストも最初に置き場を
空にするので初期値は観測できない。**実体の幅を狭める**（`uint32_t` →
`uint16_t`）ほうに替えて、module のテストと `calc_spells_test` の両方が
落ちることを確かめた（→ `HANDOVER.md` 第 7 節）。

**これで区分「プレイヤー状態」は 13 個のうち 12 個が片づき、残るのは `py` 1 個**
（552 参照・別名 88）。1 本の module にはならず、問い単位の窓口で別に進める。

### #16 で浮上した課題：`wait_for_more` グローバルフラグ（2026-08-28）

`wait_for_more`（`variable.c:102`）は「`-more-` プロンプト表示中に `^C` が押されたら、
キャンセル後にプロンプトを描き直す」ためのフラグ。`io.c:209` で立て、
`signals.c:176` が読む。

**構造上はグローバルにする理由がある。** `wait_for_more_confirmation()` が
`inkey()` でブロック中に `^C` が来ても、`handle_pending_signals()` は
メインループまで戻ってから呼ばれる（`dungeon.c:90`, `main.c:198`）。
つまりフラグを立てた関数のフレームは既に抜けているので、ローカル変数では渡せない。

検討した代替案。

| 案 | 内容 | 評価 |
|---|---|---|
| 画面内容から復元 | `RenderBackend` に画面を読む操作がない | **不可** |
| 復元処理を関数ポインタで渡す | グローバルが `bool` から関数ポインタに変わるだけ | 改善にならない |
| **`get_check` で画面を保存・復元** | `io.c` の `save_screen()`/`restore_screen()` を使えばフラグ不要になり、`-more-` に限らずどんな画面でも正しく戻る | **正しい方向。ただし着手は保留** |

3 番目を今回着手しない理由。

1. `save_screen`/`restore_screen` は ncurses 実装に依存し、**現環境では変更後の検証ができない**
2. `get_check` の呼びだし元が多数あり、全経路で画面保存が正しく働くかの確認が必要
3. 「`-more-` だけ復元」から「画面全体を復元」への変更は**ふるまいの変更**（仕様変更）

**グローバル変数の削除という大きな枠組みで扱うべき事項。** `externs.h` には
約 90 個の裸グローバルがあり（→ 棚上げ #32、#35）、`wait_for_more` はその一部。
個別に外すより、状態の持ち方全体を設計しなおす作業に含めるのが筋。

**→ 2026-09-11（#18-2）。** グローバルではなくなった。`src/messages.c` の
`static` になり、窓口は `msg_at_more_prompt()` /
`msg_set_at_more_prompt(bool)`。**ただし上の話は解決していない。**
「フラグを立てた関数のフレームは既に抜けているので、ローカル変数では渡せない」
という構造は変わっておらず、`-more-` の状態を module が持つことは正しい。
3 番目の案（`get_check` で画面を保存・復元してフラグを要らなくする）は
そのまま残る。今回変わったのは「誰でも書ける裸のグローバル」から
「`messages.c` が持ち、`signals.c` は読むだけ」になったこと。

### #17 の精査（2026-08-28）：5関数はテーブル化できる

`stat_adj` / `tohit_adj` / `toac_adj` / `todis_adj` / `todam_adj` は
いずれも「能力値 → 段階的な補正値」の写像だが、書き方が揃っていない。

| 関数 | 参照する能力値 | 比較演算子 | 段数 | 戻り値 |
|---|---|---|---|---|
| `stat_adj` | 引数で指定 | `>`（**降順**） | 8 | 0..7 |
| `tohit_adj` | A_DEX **+** A_STR | `<`（昇順） | 9+8 | 2つを合算 |
| `toac_adj` | A_DEX | `<` と **`==` 混在** | 10 | -4..5 |
| `todis_adj` | A_DEX | `<` と **`==` 混在** | ? | -8.. |
| `todam_adj` | A_STR | `<`（昇順） | 9 | -2..6 |

**書き方の不統一は解消できることを実測で確認した。**

- `toac_adj` の `==` は `<` に置きかえても等価（`s = -100..300` の全域で一致）。
  `< 4`, `== 4`, `== 5`, `== 6`, `< 15` は連続する境界を `==` で書いただけ
- `stat_adj` の `>` 降順は `<` 昇順に書きかえても等価（同様に全域で確認）

したがって 5 関数すべてを「**境界値の昇順リスト → 対応する補正値**」という
同一の形に揃えられる。テーブル化すれば、同じ境界値（4/7/17/18/94/117/118）が
5 箇所に散っている状態も 1 箇所にまとまる。

**着手の方針。** ただし段数と境界値は関数ごとに違うので、共通化するのは
「テーブルを引く仕組み」だけにする。テーブルの中身は各関数が持つ。
`tohit_adj` は 2 つの能力値の合算なので、テーブルを 2 回引いて足す形になる。

### #17 の再評価（2026-08-28）：境界値はほとんど共通していない

ステップ A で全境界値が判明したので、当初の見立て（「同じ境界値が 5 箇所に散在」）を
検証した。**結果は想定と違った。**

```
境界値: 0 4 5 6 7 8 13 15 16 17 18 59 68 69 88 94 108 109 117 118

関数            0   4   5   6   7   8  13  15  16  17  18  59  68  69  88  94 108 109 117 118
stat_adj        ●   ·   ·   ·   ·   ●   ·   ●   ·   ·   ●   ·   ●   ·   ●   ·   ●   ·   ·   ●
todam_adj       ●   ●   ●   ·   ·   ·   ·   ·   ●   ●   ●   ·   ·   ·   ·   ●   ·   ●   ●   ·
toac_adj        ●   ●   ●   ●   ●   ·   ·   ●   ·   ·   ●   ●   ·   ·   ·   ●   ·   ·   ●   ·
todis_adj       ●   ●   ●   ●   ●   ●   ●   ·   ●   ·   ●   ●   ·   ·   ·   ●   ·   ·   ●   ·
tohit(DEX)      ●   ●   ·   ●   ·   ●   ·   ·   ●   ●   ●   ·   ·   ●   ·   ·   ·   ·   ·   ●
tohit(STR)      ●   ●   ●   ·   ●   ·   ·   ·   ·   ·   ●   ·   ·   ·   ·   ●   ·   ●   ●   ·

全6リストに共通: 0, 18 のみ
4つ以上で共通:   0, 4, 5, 18, 94, 117
```

**20 個の境界値が 6 リストにばらばらに分散している。** 共通するのは `0` と `18` だけ
（`0` は下限なので実質は `18` のみ）。「同じマジックナンバーが 5 箇所に散在している」
という当初の検出は**不正確だった**。

さらに次の不整合がある（いずれも意図が不明）。

- **最上段が揃っていない。** `stat_adj` と `tohit_adj`(A_DEX) は 118、他 4 つは 117。
  `tohit_adj` は同一関数内で 118 と 117 が混在
- **`todis_adj` だけ戻り値が 1 刻みでない。** 2 の次が 4（3 が欠番）、6 の次が 8（7 が欠番）。
  負側も -8/-6/-4/-2/-1 と不規則
- **`stat_adj` だけ負を返さない**（最小 0）。他 4 関数は -6/-4/-8/-2 まで返す

これらはゲームバランスの調整結果かもしれず、**統一すべきものかどうかを外から判断できない。**

### 方針の変更：テーブル化はするが、共通化はしない

**当初の「マジックナンバーを 1 箇所にまとめる」は達成できない。** 境界値が共通して
いないので、まとめる対象がない。

それでもテーブル化する価値は残る。

- `if` 連鎖（最大 12 段）より、境界値と戻り値の対応表のほうが読みやすい
- 段の抜け（`todis_adj` の 3 と 7）が表として見えるようになる
- 比較演算子の不統一（`>` 降順、`==` 混在）が解消される

したがって**各関数が自分のテーブルを持つ形**にする。共有するのは「昇順の境界リストを
引く」関数だけ。`use_stat` は `uint8_t`（`types.h:183`）で 0..255 なので、
負の入力を考慮する必要はない。

### 代役の点検（2026-08-28）：保護の穴を1件塞いだ

全体の再評価で、**代役が保護の穴を作っていた**ことが判明した。

**塞いだ穴：`distance()` の代役が常に 0 を返していた**（`3017ea7` で修正）

```c
/* 修正前 */
int distance(int y1, int x1, int y2, int x2) { return 0; }
```

これは次の経路で誤った結果を「正しい」と固定する危険があった。

- `misc3.c:2103` の `teleport()` が `while (distance(...) > dis)` でループする
  → 常に偽になりループの意味を失う
- `creature.c:1033,1532` の `m_ptr->cdis = distance(...)`
  → 常に 0 で「全モンスターが隣接している」状態

`distance` は純粋関数なので本物の実装を写した（`misc1.c` 全体をリンクすると
ダンジョン生成への依存が芋づるで付くため）。**写しが本物と文字単位で一致する
ことを確認済み**で、`tests/distance_test.c` が本物のふるまいを 10 件で固定して
いるので、乖離すればそちらで気づける。

**残る「常に 0/false を返す」代役**（現時点では無害だが、将来テストが及ぶと危険）

| 代役 | 使われる場所 | 現状の影響 |
|---|---|---|
| `bit_pos` | `creature.c` 1 箇所 | なし（`moves_this_turn` は使わない） |
| `in_bounds` | `misc3.c` 1、`creature.c` 2 箇所 | なし（テスト対象の経路外） |
| `damroll` | `creature.c` 4 箇所 | なし（同上） |
| `test_hit` | `creature.c` **23 箇所** | なし（同上）。**戦闘判定の中核なので、`make_attack` にテストを及ばせるなら必ず見直す** |

**教訓：リンカが列挙した未解決シンボルを機械的に埋めると、危険な代役が混ざる。**
リンカは「足りない」ことしか教えてくれず、何を返すべきかは判断が必要。
純粋な計算関数や、判定・分岐に使う値を返す関数は代役にしてはいけない。

### #17b：`chr_adj` / `con_adj`（2026-08-28 に追加）

全体の再評価で、**#17 と同型の関数が台帳から漏れていた**ことが判明した。

| 関数 | 場所 | 構造 | テーブル化 |
|---|---|---|---|
| `chr_adj` | `misc3.c:295-346` | `if` 連鎖 5 段 + **`switch` 17 ケース** | **できる**（22 段。全域で等価を実証済み） |
| `con_adj` | `misc3.c:349-364` | `if` 連鎖 6 段 | **できない** |

**`con_adj` を対象外とした理由。**

```c
if (con < 7) {
    return (con - 7);   /* ← 定数ではなく計算値 */
}
```

`con < 7` の枝が **`con` の値から計算した結果**を返す（-7..-1 の範囲で連続的に変わる）。
テーブルは「境界値 → 定数」の写像なので、この形は表せない。
6 段に個別のエントリを並べれば表せるが（`{0,-7},{1,-6},...,{6,-1}`）、
**計算式を定数の列に展開するのは意味の劣化**にあたる。読み手は
「なぜ -7 から始まって 1 ずつ増えるのか」を表から読み取れない。
`con - 7` のままが正確に意図を伝えている。

**`chr_adj` はテーブル化できる。** `if` 連鎖と `switch` の混在という
最も読みにくい形で、22 段の昇順テーブルに置きかえられることを
0..255 の全域で確認した（不一致 0 件）。`switch` の `default: return 100` は
`charisma < 3` の場合で、テーブルでは先頭 `{0, 100}` に対応する。

## コンパイル警告の解消（2026-08-28 に追加、2026-09-07 に完了）

**警告は「コンパイラが機械的に検出した臭い」。** 人間の目による検出より確実で、
見落としがない。着手時は `make` で 8,338 件出ていた。

### 結果

`make` の警告は **8,338 件 → 0 件**。テストは 285 件 → 310 件でグリーン。
`makefile.test` 側も抑制なしで 0 件。

作業の単位は「警告の種類ごと」。件数の多さではなく、**危険度**（実際の不具合を
示しているか）の順に扱った。

| 種類 | 着手時 | 現在 | 直しかた |
|---|---|---|---|
| `-Wstrict-prototypes` | 5,530 | 0 | `externs.h` の 141 関数の `()` 宣言に引数を書いた |
| `-Wdiscarded-qualifiers` | 2,433 | 0 | 読みとり専用の文字列を `const char *` にそろえた |
| `-Wold-style-definition` | 177 | 0 | 定義側の `()` も同様 |
| `-Wpedantic` | 111 | 0 | `externs.h` ほか |
| `-Wformat-overflow=` | 36 | 0 | 書きこみ先を `msgtype`（`types.h`）にし `snprintf` にした |
| `-Wmissing-prototypes` | 27 | 0 | 宣言を足した／`static` にした |
| 個別の警告 | 12 | 0 | 下記 |
| `-Warray-bounds=` | 3 | 0 | 逆順走査を包みなおし、`bool` に 2 バイト読むのをやめた |
| `-Wformat-nonliteral` | 2 | 0 | 書式を変数で渡す `sprintf`/`sscanf` をやめた |

### 警告の陰にあった実際の不具合

警告を消す作業の副産物ではなく、**これが目的だった**もの。

| 場所 | 内容 |
|---|---|
| `signals.c:90` | `sigsetmask` の宣言がない（POSIX で廃止）。暗黙宣言は型検査が効かない |
| `signals.c:219`, `render_ncurses.c:292` | `kill` の宣言がない（`<signal.h>` の include 漏れ） |
| `game_state.c:66,67` | `strncpy` で 79 バイトを 79 バイトに写し、終端の NUL が入らない |
| `wizard.c:367` | `sscanf` に `"%lx"`（`unsigned long *` を要求）を渡しつつ受け手が `int32_t *`。64 bit 環境では 4 バイトの変数に 8 バイト書いていた |
| `death.c:164` | `print_tomb()` が表示のためにグローバルの `died_from` を書きかえ、死因が 78 文字以上だと配列外（`died_from[79]`, `[80]`）に書いていた |
| 文字列の埋めこみ 32 箇所 | `bigvtype` のアイテム説明を同じ大きさの `bigvtype` に埋めこんでいた。説明が長いと文飾りの分だけ溢れる |
| `dungeon.c:705,712`, `misc3.c:463` | `int` を `char[8]` / `char[16]` に書きこんでいた |
| `save.c:768-769` | `rd_short((uint16_t *)&panic_save)` が 1 バイトの `bool` に 2 バイト読みこみ、隣の変数を書きつぶしうる |
| `monsters.c:709` | `monster_creature_rend()` が `c_list - 1`（配列の直前）を返す。終端の 1 つ先は合法だが先頭の 1 つ前は未定義動作 |

### 最後に残った 2 件の直しかた

どちらも「テストで先に押さえて、ふるまいを変えずに直す」で通せた。着手時は
ふるまいが変わると見ていたが、変わらない直しかたがあった。

**`monsters.c` の逆順走査。** `std::reverse_iterator` と同じ持ちかたにした。
反復子（`types.h` の `creature_rev_iterator`）が持つのは指したい要素ではなく
「その 1 つ先」なので、先頭を指す状態でも配列の外を指すポインタが現れない。
`creature_type *` を裸で返さず構造体に包んだので、1 つずれた値をうっかり
`->` で読むことがコンパイルエラーになる。添字ではなくポインタを包んでいる
ため、反復子は `c_list` というグローバルに結びつかず対象を持ちはこぶ。
先に `tests/creature_iterator_test.c` で「どの要素をどの順にたどるか」を
固定し、走査の書きかたはテスト内の 1 箇所（`collect_reverse_walk`）に
閉じこめたので、API を変えても期待値は動かさずに済んだ。

**`save.c` の真偽値。** `uint16_t` でいったん受けてから `bool` に詰める。
セーブファイルの形式（真偽値 1 つが 2 バイト）と値の対応（0 → false、
1 → true）を `tests/save_bool_test.c` で先に固定した。本体が書くのは 0 か 1
だけで、リトルエンディアンでは下位バイトが同じ値になっていたので、
既存のセーブファイルの読みこみ結果は変わらない。static 関数なので、
テストが `src/save.c` を `#include` して実体ごと取りこんでいる。

### なぜこれを臭いとして扱うか

- `-Wstrict-prototypes` は「引数の型検査が効いていない」ことを意味する。
  `()` 宣言の関数は**どんな引数で呼んでも警告が出ない**
- `-Wdiscarded-qualifiers` は「変更しないデータを変更可能として扱っている」。
  読み手は「この関数は引数を書きかえるのか」を実装を読まないと判断できない。
  実際、`const` を付けたことで `death.c` の二役の変数（読みとり専用のリテラルと
  書きかえるグローバル）が露出し、上記の配列外書きこみが見つかった
- `-Wformat-*` と `-Warray-bounds` と `-Wstringop-truncation` は**実際の不具合**。
  臭いではなくバグ

### 抑制を残さない

作業中は `makefile.test` に本体由来の警告の抑制（`SUPPRESS_UPSTREAM` と各
ターゲットの `-Wno-format-overflow`）を置いていた。**事情が消えたら抑制も消す。**
残しておくと新しく同じ種類の警告が出ても気づけない。

### #33 の精査（2026-09-13）：misc3.c は 6 責務ではなく 11 責務

**棚上げを解除する。** 棚上げの理由（「一括分割はしない」）は今も正しいが、
**段階的に進めるなら今すぐ着手できる塊が 2 つある**ことが計測で分かった。
リファクタリング用スキルに「肥大化クラス」をモジュール（C では `.c` 1 つ）
単位で見る手順を足して再適用した結果（→ `HANDOVER.md`）。

記録も 2 箇所ずれていた。**行数は 2212 ではなく 2090**（それ以降の作業で減った）、
**責務は 6 ではなく 11**。

#### 名前が何も約束していないファイルは 8 個・8307 行（`src/*.c` 28458 行の 29%）

| ファイル | 行数 | 冒頭コメント |
|---|---:|---|
| misc1.c | 807 | Misc utility and initialization code, magic objects code |
| misc2.c | 916 | （misc1.c と**同一**） |
| misc3.c | 2090 | Misc code for maintaining the dungeon, printing player info |
| misc4.c | 108 | （misc3.c と**同一**） |
| moria1.c | 1722 | Misc code, mainly handles player movement, inventory, etc |
| moria2.c | 597 | （moria1.c と**同一**） |
| moria3.c | 1038 | Misc code, mainly to handle player commands |
| moria4.c | 1029 | （moria3.c と**同一**） |

**連番のペアは冒頭コメントまで同一。** 責務で分けた結果ではなく、1 ファイルが
大きくなったのでコンパイル単位を足しただけだ。だから中身は連番のあいだで自由に
散っている（`misc4.c` の 5 関数は「書きこみ」「視界」「文字列連結」の 3 責務）。

対して、これまでに切りだした module は冒頭コメントが 1 文で書けている
（`rnd.c` "Random number generator"／`panel.c` "The panel: the part of the
dungeon the screen is showing"／`options.c` "The player's boolean options, and
the one table that describes them"）。**1 文で言えるかどうかが、そのまま
分割済みかどうかの判定になっている。**

#### misc3.c の 87 関数を責務ごとに分けた結果

| 塊 | 行数 | 関数 | 行き先の候補 | 既存の保護 |
|---|---:|---:|---|---|
| A ダンジョンへの物の配置 | 161 | 7 | `place.c`（新設。`generate.c` は地形担当） | 無 |
| B ステータス行・キャラ画面の描画 | 475 | 35 | `char_screen.c`（新設）→ #23 と同じ対象 | `put_misc3_test` が一部 |
| ~~**C 能力値の補正表**~~ **→ 完了** | **116** | **8** | **`src/stats.c`（2026-09-13 に移した。→ #40）** | **`stat_bonus_test` 129 件** |
| C' 能力値の変更 | 115 | 6 | 同上。ただし下記のとおり切り目が違う | 無 |
| D キャラクタ名の入力 | 50 | 2 | `char_screen.c` に同居させるか別 | 無 |
| E 持ち物の出し入れ | 210 | 10 | `inventory.c`（新設） | `inven_stack_test` が一部（#10） |
| F 呪文の習得と選択 | 525 | 6 | `spellbook.c`（新設。`spells.c` は効果担当） | 無 |
| G 経験値・レベル・HP | 82 | 3 | `player_level.c`（新設） | 無 |
| **H 文字列への差しこみ** | **69** | **2** | **`strings.c`（新設）** | **`objdes_test` が実体を使用** |
| I wizard モードへの移行 | 18 | 1 | **既存の `wizard.c` へ合流** | 無 |
| J 打撃・ダメージ・耐性判定 | 120 | 4 | `combat.c`（新設） | 無 |
| K 移動とテレポート | 118 | 3 | `movement.c`（新設） | 無 |

87 関数・2061 行すべてを分類済み（残り 29 行はファイル冒頭の定数と空行）。

#### 切り目の良し悪しは未解決シンボルの数で測れる

`.c` を単体でコンパイルしてリンカに聞いた実測値。

| 対象 | 未解決シンボル |
|---|---|
| `misc3.c` 全体 | **83 個**（うち標準ライブラリ 7）|
| 塊 C + C'（能力値まるごと） | 8 個 — `py` `randint` `prt_stat` `calc_bonuses` `calc_hitpoints` `calc_mana` `calc_spells` |
| **塊 C だけ（補正表 7 関数＋テーブル）** | **1 個 — `py` のみ**→ #40 で `src/stats.c` へ（2026-09-13 完了） |
| **塊 H（`insert_str` / `insert_lnum`）** | **0 個**（標準ライブラリのみ）→ #41 で `src/str_insert.c` へ（2026-09-13 完了） |

**`makefile.test` の「57 個」という記述と矛盾しない。** あちらは
`desc.c` `tables.c` `player.c` などを本物としてリンクしたあとに残った数で、
83 は `misc3.c` 単体の数。

**この計測が切り目を教えている。** 能力値を丸ごと動かすと `prt_stat`（描画）と
`calc_hitpoints`/`calc_mana`/`calc_spells`/`calc_bonuses`（派生値の再計算）が
付いてくる。**純粋な補正表（C）と、値を書きかえる側（C'）のあいだが本当の
境界**で、C だけなら `py` 1 個で独立する。

#### 着手順の判断

**塊 C と塊 H を先に出す。** どちらも既にテストが実体を検証しており（#17 で
テーブル化した対象がそのまま塊 C）、外部シンボル依存が 1 個と 0 個。
`patterns.md`「保護しやすく効果が大きいものから」に最もよく当てはまる。
→ P1 の #40 と #41。

**残る 10 塊は #42 として P2。** 塊 B は #23（描画が計算と同居）と同じ対象なので、
UI 抽象化の進行中作業と歩調を合わせる必要がある。

**「9 塊」は誤りだった（2026-09-14 に数えなおした）。** 上の表は 12 行あり、
完了は塊 C（#40）と塊 H（#41）の 2 つなので、**残りは 10 塊**。9 と書いていたのは
**「11 責務」から 2 を引いた**もので、その 11 は塊 C と C' を「能力値」1 責務として
数えた数。ところが #40 が出したのは**補正表（C）だけ**で、値を書きかえる側
（C'・115 行）は `misc3.c` に残っている。**責務で数えるか塊で数えるかが混ざると
1 ずれる**ので、以後は表の行と同じ「塊」で数える。#42 の行に並べた 10 個の内訳
（161+475+115+50+210+525+82+18+120+118 = 1874 行）が現在の `misc3.c` 1906 行から
冒頭の定数・空行・#18-5 で置いたコメントを引いた残りと合う。

**終点を先に決める。** `misc3.c` が無くなるところまで。名前が何も約束しない
ファイルが残っているかぎり、次の人はそこに置く（実際に `misc4.c` がそうやって
できている）。**溢れたものを新しい連番に移すのは禁止。**

### 着手順の再検討（2026-09-13）：#40・#41 と #18-5 を測りなおした

`HANDOVER.md` 第 5 節は「次は #18-5 持ち物 6 個」と書いているが、その後に
P1 へ #40・#41 が入った（上記「#33 の精査」）ため、資料のあいだで次の一手が
食いちがっていた。**スキルの「保護しやすく効果が大きいものから」に照らして
測りなおした結果**を記録する。数字はすべてこの再検討で実測したもので、
**台帳側の記録と 3 箇所ずれていた**（下記）。

#### #40（能力値の補正表 → `stats.c`）の実測

| 項目 | 記録 | 実測 |
|---|---|---|
| 行範囲・行数 | `257-328` `659-704`／118 行 | **`257-327` `658-702`／116 行**（境界が 2 行ずれ。末尾の空行と次関数のコメントを含んでいた） |
| 未解決シンボル | `py` の 1 個 | **`py` の 1 個**（標準ライブラリ由来も 0）。`config.h` `constant.h` `types.h` ＋ `extern player_type py;` の 4 行でコンパイルでき、`externs.h` も `headers.h` も要らない |
| 保護 | `stat_bonus_test` が実体を検証 | **本物リンク・117 件全通。ただし 7 関数のうち `con_adj` はテスト 0 件** |
| 塊の独立性 | （記載なし） | `py` 以外に `misc3.c` の static 変数・static 関数を 1 つも使わない。`misc3.c` 側からの呼びだしは 5 箇所、`src` 全体で 10 ファイル 30 箇所（すべて宣言経由なので呼びだし側は無変更） |
| ビルド定義 | （記載なし） | `makefile` 3 行 ＋ `makefile.test` 最大 14 行 ＋ **`makefile.win` 3 行**。16 実行形式のうち `misc3.o` を引くのは 7 個 |

**新しく分かった効き目。** 抽出した塊 ＋ `py` の定義 ＋ `fixture_reset` の 3 つだけで
`stat_bonus_test` がリンクできる——いま引いている `misc3.c` `desc.c` `tables.c`
`treasure.c` `player.c` `abilities.c` の **6 コンパイル単位が不要になる**。
これは #38（`makefile.test` の芋づるリンク）を数字で 1 つ減らす最初の実例。

**危ないところ 3 つ。** (1) `con_adj` はテスト 0 件で、しかも 7 関数のうち唯一
テーブルを持たない if/else 連鎖（`:311-327`）——移動だけでも壊れたら誰も
気づかないので、**動かす前に数件で固定する**。(2) `makefile.win` を忘れると
Windows ビルドだけリンクできない。(3) `tests/put_misc3_test.c:82,89` のコメントが
指す行番号は既に古く、移動でさらに嘘になる。

#### #41（文字列への差しこみ → 新モジュール）の実測

| 項目 | 記録 | 実測 |
|---|---|---|
| 行範囲・行数 | `1767-1835`／69 行 | **`1767-1833`／67 行**（`insert_str` 34 ＋ `insert_lnum` 32）。`concat` は `misc4.c:95-108` |
| 依存 | umoria のシンボルへの依存ゼロ | **リンクシンボルは 0**（標準ライブラリ 7 個のみ）。ただし `insert_lnum` は `vtype`（`types.h:17`）**型に依存**する。`concat` は型依存も 0 |
| 保護 | `objdes_test` が実体を使用 | `objdes_test`（16 件）が通すのは **`insert_str` だけ**、しかも `~` を含む雛形は食料の 2 件のみで**複数形の分岐は未通過**。`insert_lnum` の保護は `haggle_comment_test`（25 件、台帳に未記載）。**`concat` を通るテストは無い**（リンクされる場面ではすべて `tests/creature_stubs.c:166` の代役に置きかわる） |
| 被参照 | （記載なし） | `insert_str` 3（`desc.c`）／`insert_lnum` 2（`store2.c`）／`concat` 直接 0・`CONCAT` マクロ 88（`creature.c` 40・`spells.c` 34・`moria3.c` 7・`moria4.c` 7） |
| ビルド定義 | （記載なし） | `makefile` 3 ＋ `makefile.win` 3 ＋ `makefile.test` 10 行。`insert_str` を引く実行形式は 5 個 |

**名前は `strings.c` にしない。** `-Isrc` があるので `src/strings.h` は
`#include <strings.h>` を横取りしうる（`src/curses.h` で同じ横取りが既に
起きている前例がある）。

**`CONCAT` マクロは `externs.h` に残す。** `vtype` の複合リテラルを使うので、
マクロまで新モジュールへ移すと「新しい module は依存を持たせない」に反する。

**`concat` を一緒に寄せるかは別判断。** 台帳は「同じ性質なので一緒に」と
書いているが、実測では**保護が無い**（代役に置きかわるので既存テストは
本物を 1 度も通らない）。#41 に混ぜると「保護済みの移動」ではなくなる。

#### テストの足場の実測（#37・#38 の数字を測りなおした）

**16 実行形式のリンク構成**（`make -f makefile.test -n -B` の実コマンドから）。
`src/*.c` を何本引くかで並べると、足場の重さがそのまま出る。

| 引く本数 | 実行形式 |
|---:|---|
| 0 | `distance_test`（テスト内に写し）、`movement_rate_test`（`creature.c` を `#include`） |
| 1 | `device_chance_test` `creature_iterator_test` `messages_test` `panel_test` `stores_test` |
| 2 | `options_test` |
| 4 | `item_ident_test` |
| 6 | `inven_stack_test` `put_misc3_test` `stat_bonus_test` `objdes_test`（6 連鎖） |
| 8 | `haggle_comment_test` |
| 9 | `save_bool_test` `store_save_test`（9 連鎖） |

**新しいテストを 1 本足すコストは 4 行**（`TESTS` に 1 語 ＋ ルール 3 行）。
ただしそれは**窓口方式で作った module に対してのみ**——手本は
`makefile.test:336-338`（`stores_test`）で、代役は 1 つも要らない。

**#37 の重複は 19 ではなく 20。** `creature_stubs.c` 184 行・83 シンボル、
`misc3_stubs.c` 248 行・64 シンボル。重複 20 個のうち 18 個が関数、2 個
（`cave` `wizard`）がデータ。`distance` の写し 2 つは本物（`src/misc1.c:172-184`）
と `diff` で完全一致。

**#38 の `misc3.c` 未解決シンボルは 83 ではない。** リンカ実測 **76 個**
（`main` を除くと 75、すべてプロジェクト内）。`nm -u` だと 85 個（libc 10 込み）。
記録の 83 は `nm -u` から 2 個引いた値で、リンカ基準と食いちがう。さらに
**`makefile.test:107` 付近のコメントは「57 個」と書いており、3 つの数字が全部
ちがう**。以後この数を成果指標に使うなら、**リンカ基準（76）に統一する**。

**新しく見つかった保護の穴（→ P3 #44）。** 代役 4 ファイルに、**引数を無視して
定数を返す非 void の代役が 55 個**ある。`in_bounds` は常に真、`los` は常に偽、
`known2_p` `bit_pos` `damroll` `delete_object` は常に 0、`tfopen` は NULL。
`refactoring-loop.md` が挙げる典型そのもので、**この代役に守られた範囲は
「グリーンだが保護されていない」**。とくに `tests/creature_stubs.c:182-184` の
観測窓口が常に `""` / `0` を返すので、**この構成ではメッセージや画面を観測する
テストは書いても中身を検証できない**。

**`misc3.c` から切りだすときの衝突源。** `misc3.c` を本物としてリンクするのは
5 実行形式（`inven_stack` `put_misc3` `haggle_comment` `stat_bonus` `objdes`）で、
切りだすだけならリンク行に `.o` を 1 つ足せば済む。ただし
**`tests/creature_stubs.c` は `misc3.c` の実体 8 個を代役として持っている**
（`dec_stat` `find_range` `inven_destroy` `mmove` `player_saves` `prt_cmana`
`prt_experience` `prt_gold`）。`movement_rate_test` は `src/*.c` を 1 本も
リンクしないので今は緑だが、**切りだした新 module にこれらが入り、かつ
`movement_rate_test` 側に足すと multiple definition になる**。#40 の 8 関数と
#41 の 3 関数はこの 8 個に含まれないので、**#40・#41 では起きない**。

#### #18-5（持ち物 6 個）の実測

`HANDOVER.md` 第 5 節の偵察結果を測りなおしたところ、**6 箇所ずれていた**。
うち 2 つは道具の側の誤りで、着手順の判断を変える。

| 項目 | 記録 | 実測 |
|---|---|---|
| 実体の置き場 | `variable.c`（4 個）／`treasure.c`（2 個） | **6 個すべて `treasure.c:558,561-563`**（集計自体は `globals.py` の `HOME_FILES` に `treasure.c` があるので正しかった） |
| `inventory` の別名 | 105 | **真の address-of は 85。** 差 20 件は `&&` とビット AND の誤検出（`moria1.c` 8 件・`scrolls.c` 12 件）。**`scripts/globals.py` の alias 正規表現に `&&`／`|` の除外が無い**（→ 道具の欠陥） |
| 触るファイル | 27 | **28**（実コードの参照だけなら 26。`options.c:30` と `variable.c:92` は文字列とコメント） |
| ファイル別（6 個合計） | `moria1.c` 64・`scrolls.c` 45・`misc3.c` 24・`moria4.c` 21 | **`moria1.c` 117・`misc3.c` 52・`scrolls.c` 46・`moria4.c` 24**（記録は `inventory` 単独の件数に近い数字で、`moria1.c` と `misc3.c` が食いちがう） |
| 既存の保護 | `inven_stack_test` がスタック可否判定を保護 | **重量の不変条件も既に見ている。** ただし `equip_ctr` は `tests/` に 1 度も現れず、**装備境界の不変条件は無保護** |
| 代役の数 | `misc3_stubs.c` は 57 個 | **関数 43 ＋ 変数 18 = 61 個**（うち 3 個は観測用。冒頭コメントの 57 は「代役で埋めた未解決シンボル数」で別の数え） |

**別名は形がばらけていなかった。** 記録は「`inven_type *` を渡す形・配列ごと
渡す形・添字を跨いで比べる形が混在するのでアクセサ 1 つでは済まない」としていたが、
実測では **85 件すべてが `&inventory[<添字>]` という同一の字面**で、違うのは
受け手だけ（引数に渡す 36／宣言つき局所ポインタ 28／既存ポインタへの代入 21）。
**`&inventory[x].member` を渡す形と配列ごと渡す形は 0 件**（`game_state.c:40` の
ポインタ写し 1 件のみ）。置きかえは店（#18-4）と同じく機械的にできる。

**1 本 2 役の実測——多いのは装備側だった。** `INVEN_WIELD` = 22
（`constant.h:198`）、要素数 34（`INVEN_ARRAY_SIZE`、装備は 22..33）。
`inventory[...]` の全 199 箇所は **持ち物側だけ 約 64／装備側だけ 約 112
（`INVEN_*` 定数添字 89 ＋ `INVEN_WIELD` 始まりのループ 23）／両方を跨ぐ 16／
定義・判別不能 7**。つまり `inventory` は「持ち物の配列に装備が生えている」
のではなく、**参照の過半が装備の側**にある。窓口を 1 本のままにするか
持ち物と装備に割るかは、この 16 件の跨ぎ（`moria1.c:1189` の `get_item`、
`misc4.c:24`、`spells.c:277`、`death.c:190`）をどう扱うかで決まる。
**ふるまいは変わらないが読み手にとっての意味が変わるので、ユーザーの判断を仰ぐ。**

**`sorted_objects` と `object_ident` は本当に独立だった**（記録の見立てどおり）。
`sorted_objects` は参照 6 件で、引く 4 箇所はすべて `t_list`（床の物）を作る関数。
持ち物 4 個と同じ関数内に同居する箇所は無い。`object_ident` は参照 11 件で、
`desc.c` 側はすべて `inven_type *` を引数に取るアクセサ——`inventory[]` を
添字で引かない。唯一の同居は `identify()`（`desc.c:216`）だが、そこも関数経由。
**どちらも持ち物 4 個から切りはなして扱える。**

**塊 E（#42）との関係が決まった。行き先は同じ `src/inventory.c`。**
`misc3.c:950-1159`（210 行・10 関数）を今そのまま動かすと**未解決シンボル 21 個**で、
そのうち 11 個は持ち物ではない依存（`cave` `t_list` `popt` `delete_object` =
床置き、`py` `char_row` `char_col` `pack_heavy` `weapon_heavy` `calc_bonuses`
`change_speed` = 体力再計算）。つまり**塊 E は「持ち物の芯」＋「床置き」＋
「体力再計算」の 3 つが混じった切り目**で、先に動かすと `inventory.c` が
最初から `cave` を抱えこむ。

**だから順番は「窓口（#18-5）が先、塊 E の移動（#42）が後」。** 先に窓口と
4 変数の所有を決めれば、塊 E を動かすときには未解決が 18 → 6 個（`inven_carry`
相当の芯）まで落ちた状態で切り目を選べる。参考値: `inven_carry` 単体
（`misc3.c:1126-1159`）は未解決 6 個。

#### 窓口を持ち物と装備に割る（2026-09-13、ユーザーの判断）

上記の「1 本 2 役」の問い合わせに対する答えは **「いったん分割して進める。あとで
まとめたほうがわかりやすければまとめる」**。この向きは戻しやすい——2 本の窓口を
1 本にまとめるのは後からでも機械的にできるが、1 本で作ってから割るのは
呼びだし側をもう一度全部触ることになる。

**所有は 1 つ、窓口は 2 つ。** 配列は 1 本（`inventory[INVEN_ARRAY_SIZE]`、
持ち物 0..21・装備 22..33）で、セーブファイルの並びもこの 1 本を前提にしている。
**配列を 2 本に割るのはふるまいの変更**（直列化の順序が変わりうる）なので採らない。
実体は `src/inventory.c` が 1 つ持ち、窓口だけを 2 つに分ける。

| 窓口 | 置き場 | 何を見るか |
|---|---|---|
| 持ち物 | `src/inventory.h` | 0..21 の枠、`inven_ctr`、`inven_weight` |
| 装備 | `src/equipment.h` | 22..33 の枠（`INVEN_*` 定数添字）、`equip_ctr` |
| 跨ぎ | 持ち物側に置き、**跨ぎ専用と明記する** | 添字空間の全域を走る 16 箇所（`moria1.c:1189` の `get_item`、`misc4.c:24`、`spells.c:277`、`death.c:190`、`save.c` の直列化） |

**跨ぎ窓口の呼びだし件数が、そのまま「まとめるべきか」の指標になる。** 16 件が
減らないなら分割は読み手の役に立っていないので、まとめる判断の根拠になる。減るなら
分割が効いている。**この数字を #18-5 の完了時に測って記録する。**

**ステップ B はファイル単位で 5 分割**（担当の推し）。① `moria1.c` 117 件
（`get_item` の跨ぎを含むので単独） ② `misc3.c` 52 件（塊 E と重なる）
③ `scrolls.c` 46 ＋ `desc.c` 21（装備の定数添字が主） ④ `save.c` 16 ＋
`files.c` 9 ＋ `death.c` 5（全域ループ・直列化） ⑤ 残り 18 ファイル。

#### 再検討の結論：#40 → #41 → #18-5

スキルの表（影響度 × 着手コスト）に実測値を入れると、3 者の位置がはっきり分かれた。

| 項目 | 影響度 | コスト | 根拠（実測） |
|---|---|---|---|
| **#40** 能力値の補正表 | Medium | **Low** | 116 行・外部依存 `py` 1 個・117 件で保護済み。抽出すると `stat_bonus_test` の依存が **8 → 3 コンパイル単位**（#38 を数字で減らす最初の実例） |
| **#41** 文字列への差しこみ | Low | **Low** | 67 行・外部依存 0。ただし被参照は 5 箇所だけで、効き目は #40 より小さい |
| **#18-5** 持ち物 | **High** | Medium〜High | 参照 379・28 ファイル・`externs.h` を 6 個減らす。ステップ B は 5 分割、跨ぎ 16 件、設計判断 1 件 |

**この順にする理由は 3 つある。**

1. **対象が重ならない。** #40 は能力値、#41 は文字列、#18-5 は持ち物。
   `misc3.c` の同じ塊を争わない（#40 は塊 C、#41 は塊 H、#18-5 が触るのは塊 E）。
2. **`misc3.c` を触る作法を安い題材で先に確立できる。** #40・#41 は合わせて
   183 行を `misc3.c` から出す。このとき通る道——`makefile` `makefile.win`
   `makefile.test` の 3 つのビルド定義、`misc3.o` を引く 5 実行形式のリンク行、
   `externs.h` からの宣言の移動——は **#18-5 と #42 でそのまま使う**。
   28 ファイル 379 箇所を触る作業でこの道を初めて通るのは高い。
3. **#18-5 のほうが判断待ちを含む。** 窓口を 1 本にするか持ち物と装備に割るかは
   ユーザーの判断が要る（上記「1 本 2 役の実測」）。#40・#41 は判断待ちが無い。

**#40 の着手には 1 つ前置きが要る。** `con_adj` はテスト 0 件・唯一テーブルを
持たない if/else 連鎖なので、**移動の前に数件で固定する**（ステップ A に相当。
移動だけの変更に「保護を足す」1 コミットが先につく）。

**#41 からは `concat` を外す。** 台帳は「同じ性質なので一緒に」としていたが、
`concat` は保護が無い（既存テストは代役に置きかわり本物を 1 度も通らない）。
一緒に動かすと「保護済みの移動」でなくなる。`concat` は別項目にする。

**#18-5 の段取りは実測で 2 つ変わった。** (1) `sorted_objects` と `object_ident`
は独立と確認できたので、**予定どおり先に切りだす**（どちらも数箇所で安い）。
(2) 別名の形が 1 つに収束しているので、記録が心配していた「アクセサ 1 つでは
済まない」は当たらない。**残る難所は跨ぎ 16 件と、装備境界が無保護なこと。**

**道具の欠陥を 1 つ見つけた（→ #45）。** `scripts/globals.py` の別名の
正規表現が `&&` とビット AND を address-of と誤検出する（`inventory` で 20 件、
105 対 85）。**この数字は着手順の判断に使ってきた指標**なので、直すと
`GLOBALS_INVENTORY.md` の別名の列が全体的に下がる可能性がある。道具の修正は
本体のふるまいを変えないが、**過去の記録の数字が変わる**ので扱いを決めてから。

### #40 能力値の補正表を `src/stats.c` に出した（2026-09-13 完了）

`misc3.c` の 11 責務のうち塊 C。**#33 の分割の 1 つめ**で、これが「`misc3.c` から
1 塊を出す」手順の実例になる。

| | 着手前 | 完了後 |
|---|---:|---:|
| `src/misc3.c` の行数 | 2090 | **1973** |
| テスト件数（全体） | 369 | **381** |
| `stat_bonus_test` の件数 | 117 | **129** |
| `stat_bonus_test` のリンク単位 | **8** | **3**（`src/` 側は 6 → 1） |
| 警告 | 0 | 0 |

**3 コミット。** `#40A` で `con_adj` に 12 件（`c0d78cb`）→ `#40B` で 116 行を
`src/stats.c` へ移動（`fd011a0`）→ `#40C` で `stat_bonus_test` のリンクを
3 単位に（`3574cbd`）。

**ステップ A が必要だった。** 台帳は「着手時点で保護済み」としていたが、7 関数の
うち **`con_adj` はテスト 0 件**だった。しかも 7 関数中唯一テーブルを持たない
if/else 連鎖（6 段、閾値 7/17/18/94/117）で、引数を取らず `py.stats.use_stat[A_CON]`
を直接読む——**塊の依存が `py` 1 個である理由がこれ**。閾値の両側を 12 件で固定した。

**保護の届かない等価変異が 1 つある**（記録のみ）。`if (con < 7)` → `if (con < 8)`
はレッド 0 件。最下段が `con - 7` なので `con == 7` では次段の 0 と同じ値を返し、
**原理的にテストで区別できない**。逆向きの `con < 6` は 1 件レッドになるので
閾値自体は守られている。

**`externs.h` を include しない形を選んだ**（`src/stats.c:17-23` に英語で理由）。
`py` の宣言が二重になるが、1 行の宣言のために ncurses まで引きこむ全域ヘッダを
取りこむのは手本（`panel.c` `stores.c` `options.c`）と逆行する。宣言は
`src/stats.h` を新設して `externs.h` から 7 行を移し、呼びだし側 10 ファイルに
`#include "stats.h"` を足した（`panel.h` などを受けている 14 ファイルと同じ形）。

**移動の正しさは字面で確かめた。** `git show 55ecad3:src/misc3.c` の削除分と
`src/stats.c:26-142` を `diff` して差分は末尾の空行 1 行だけ。`static` の
`lookup_stat_bonus` は `static` のまま移せた（`nm` で外部シンボルは 7 関数ちょうど。
公開が必要になったものは 0 個）。

**意図的な壊しは 4 通り・実測**（予測と全一致）。テーブル要素 `{15,2}` → `{15,3}`
で 3 件、閾値 `{117,5}` → `{116,5}` で 1 件、`con == 17` の返り値 `1` → `2` で 1 件、
**`lookup_stat_bonus` の継続条件 `>=` → `>` で 76 件**（`stat_bonus_test` 73 ＋
`put_misc3_test` 3 で、`src/stats.c` をリンクする上位のテストまで届く）。

#### #38 の成果指標は誤りだった（この作業で判明）

P3 #38 は「**`misc3.c` 単体の未解決シンボルは 83 個で、この数が減ることが
そのまま成果になる**」と書いていた。**成り立たない。** 実測すると

| | `nm -u` 基準 |
|---|---:|
| 着手前（`55ecad3`） | 85 |
| #40 完了後 | **87** |

**分割すると増える。** 塊が外に持っていた依存は `py` 1 個だけで、それは
`misc3.c` の他の場所でも使われているので減らない。逆に `stat_adj` と `con_adj` が
**ファイル内の呼びだしからモジュール間の呼びだしに変わって 2 個増えた**。

**正しい指標は「実行形式ごとのリンク単位」。** `stat_bonus_test` の 8 → 3 が成果で、
これは「1 実行形式 1 対象 module」に近づいた量をそのまま表す。#38 の数字の記述は
この基準に置きかえる（→ P3 #38 の欄も直した）。

### `makefile.win` は着手前から壊れている（2026-09-13 に判明、→ P3 #46）

#40 で 3 つめのビルド定義として `makefile.win` を触ったときに分かった。
**`SRCS` に 16 個のソースが未登録**で、`stats.c` を足しても Windows ビルドは
リンクできない（未登録: `abilities` `device` `game_state` `input` `input_ncurses`
`item_ident` `messages` `options` `panel` `platform` `render` `render_ncurses`
`signal_flags` `signals` `stores` `view_observer`）。**着手前からの状態**なので
このリファクタリングでは直さず記録する（→ P3 #46）。#40 では作法どおり 3 箇所を
更新した。

### #41 文字列への差しこみを `src/str_insert.c` に出した（2026-09-13 完了）

`misc3.c` の 11 責務のうち塊 H。**#33 の分割の 2 つめ。** #40 と同じ手順をなぞる
作業だったので、**手順が再現するかどうかの試験**にもなった（結論：再現した。
判断が要ったのは名前と `concat` の 2 点だけ）。

| | 着手前 | 完了後 |
|---|---:|---:|
| `src/misc3.c` の行数 | 1973 | **1904** |
| テスト件数（全体） | 381 | **403** |
| 実行形式 | 16 | **17** |
| `str_insert_test` のリンク単位 | 10 | **2**（`src/` 側は 8 → 1。**代役 0・フィクスチャ 0**） |
| 警告 | 0 | 0 |

**3 コミット。** `#41A` で `tests/str_insert_test.c` に 22 件（`82b1eed`）→ `#41B` で
67 行を `src/str_insert.c` へ移動（`6aeae73`）→ `#41C` で `str_insert_test` の
リンクを 2 単位に（`b1449bf`）。

**行番号と行数は記録とちがっていた。** 台帳の `1767-1835`／69 行は、#40 が 116 行を
抜いたことで **`1649-1716`／67 行**に動いていた（差の 2 行は境界の数えまちがい）。
**「記録された数字を前提にせず測りなおす」がまた効いた。**

**ステップ A が必要だった（#40 と同じ形）。** 台帳は「着手時点で保護済み」と
していたが、実際に通っていない分岐が 2 つあった。

- **`insert_str` の `insert` が非 NULL の経路。** `objdes_test` が `~` を含む雛形を
  通すのは食料 2 件だけで、そこでは `insert` が `CNIL`。**複数形化
  （`"ch~"→"ches"`）を守るテストは 1 件も無かった。** 実証：移動後に `if (insert)`
  を `if (0)` にすると `str_insert_test` は 6 件レッドになるが **`objdes_test` は
  0 件**
- **`insert_lnum` の `show_sign` が真の経路。** 呼びだし 2 箇所（`store2.c:145-146`）
  はどちらも `false` なので、`+` を付ける側は誰も通していなかった

**移動の正しさは字面で確かめた——差分 0 行。** 旧 `misc3.c:1649-1716`（68 行）と
`src/str_insert.c:26-93` が `diff` で完全一致（#40 は末尾の空行 1 行が差だった）。
`misc3.c` の減少 -69 行は、関数 68 行＋区切りの空行 1 行。**追加は 0 行。**

**`src/stats.c` より軽い手本になった。** 依存が**プロジェクト内シンボル 0 個**
（`nm -u` は libc だけ）なので、`stats.c` が必要とした手書きの `extern player_type py;`
すら要らない。`vtype`（`types.h:17`）**型**にだけ依存するので、include の並びは
`config.h` → `constant.h` → `types.h` → 自分のヘッダ（`stats.c` と同じ）。
`vtype` を `char[80]` に書きかえてしまえば型依存も消えるが、**それは純粋な移動で
なくなる**ので採らなかった。`makefile` の依存行は `$(HEADERS_FULL)` ではなく
`$(HEADERS_COMMON)`（`externs.h` を include しないので、こちらが正確）。

**意図的な壊しは 6 通り・すべてレッド。生き残った変異は 0。**（#40 では等価変異が
1 つ残った）`if (insert)` → `if (0)` で 6 件、`show_sign` の反転で 27 件、
内側の走査 `i < mtc_len` を 1 短くして 16 件、`strncmp` の長さを 1 短くして 9 件、
`out_val` の終端の添字を 1 ずらして 15 件、外側の走査 `pc <= bound` → `pc < bound`
で 9 件。

**「等価変異」の申告は誤りだった（検証で訂正）。** ステップ A の担当は
`pc <= bound` → `pc < bound` を「一致がある入力では結果が変わらない部分的な等価
変異」と報告したが、独立した検証で**殺されることが確認された**。一致が**無い**
入力では、走査が `pc == bound` で抜けたあとの `if (pc <= bound)` が真のまま残り、
検証していない位置を置きかえる（`"Arrow"` が `"Arros"` になる）。
**走査の終端と置きかえの可否が 2 つの独立した条件で書かれている**ことは臭いとして
実在するが、等価ではない（→ P3 #48）。**変異が生き残ったという報告は、それ自体を
独立に確かめる。**

#### #38 の指標がもう一段はっきりした（この作業で判明）

#40 で「成果は実行形式ごとのリンク単位で測る」と直したが、**その測りかたでも
片側しか見ていなかった。** #41 の前後を全実行形式で測ると：

| | `115308b`（#41 前） | 現在 |
|---|---:|---:|
| `str_insert_test`（新設） | — | **2** |
| `inven_stack_test` | 9 | **10** |
| `put_misc3_test` | 9 | **10** |
| `objdes_test` | 9 | **10** |
| `haggle_comment_test` | 11 | **12** |

**新しいテストは 2 本で済むようになったが、`misc3.c` を引く既存 4 本は 1 本ずつ
重くなった。** 切りだしたモジュールを追加でリンクする必要が出るためで、#40 でも
同じことが起きている（`stats.c` の追加）。**分割の効果は「新しく書くテストの
安さ」に出て、古い構成のテストにはいったん逆に働く。** 回収できるのは、この 4 本が
`misc3.c` を引く理由（`put_misc3` `prt_experience` `inven_carry` など）を剥がし
きったとき——つまり **#42（残る 10 塊）を終えるまで、この数字は増えつづける**。
着手順の判断材料にするときはこの二面性を前提にする。

**`tests/fixture.c:75` の空の代役は残した。** 使うのは `item_ident_test` だけで、
そこはリンカが `insert_str` を未解決にしない（代役で解決済み）。**実体と代役が
同居する構成は 1 つも無い**ので multiple definition は起きない。ただしこの構成は
本物の差しこみを 1 度も通らない（→ P3 #44 の 1 例）。

## P3：記録のみ（今回は着手しない）

将来その場所を触るときの手掛かりとして残す。

| # | 分類 | 場所 | 内容 | 着手しない理由 |
|---|---|---|---|---|
| 19 | 重複コード | spells.c 12箇所（511, 598, 673, 951, 984, 1024, 1065, 1113, 1153, 1224, 1264, 1460） | 「方向に飛ばして最初の対象を探す」骨格が12箇所コピペ。うち5関数はほぼ行単位で一致 | 終了条件に微妙な差異があり（fire_ball は fval 判定を else 側に置く）、テスト保護なしの抽出は危険。P1 でテストの足場ができてから再評価 |
| 20 | 重複コード | spells.c:1896-1954 | `lose_str/int/wis/dex/con/chr` の6関数が同型。`lose_str` と `lose_con` はメッセージまで完全一致 | テーブル駆動化で6→1にできるが、`py.flags` 依存でフィクスチャが必要 |
| 21 | 複雑な条件分岐 | creature.c:1039-1254 `mon_cast_spell` | switch 20ケース。ケース番号がビット位置の生数値で、魔法IDのマジックナンバーが前段分岐にハードコード | 変更頻度が低く（直近1年未変更）、テストコストが高い |
| 22 | 長すぎるメソッド | desc.c:275-589 `objdes` | 315行。被参照50箇所（最多）で表示文字列を全モジュールが依存 | 被参照が多く影響度は高いが、分割には広範なテストが必要。P1 完了後に再評価 |
| 23 | 不適切な責務配置 | misc3.c:206-254, 369-553 | `prt_stat`/`prt_lnum`/`prt_int`/`prt_hunger` 等の描画が計算ロジックと同居。UI層が活発なのに描画がここに残る | UI 抽象化の進行中作業と衝突する可能性。作業者の意図を確認してから |
| 24 | データの散在 | desc.c:138-211 ほか | アイテム鑑定状態が生ビット操作とアクセサで不統一 | 影響度 Low |
| 37 | 重複コード | tests/creature_stubs.c と tests/misc3_stubs.c | 代役ファイル 2 つが **19 シンボルを重複定義**。うち `distance` は本物（misc1.c）の写しが両方に置かれていて字面まで同一 | 現状は乖離なし（両方の写しが本物と一致、`distance_test.c` が本物を固定）。ただし写しの管理コストが 2 倍になっている。片方を共有ファイルに寄せるか、`src/misc1.c` を本物としてリンクできるか再検討する価値がある。テストの整理はふるまいを変えないので着手は安全だが、リンク構成の変更が 6 実行形式に及ぶ |
| 38 | 強すぎる依存関係 | makefile.test のリンク定義 | 古いテストが対象外の module まで芋づるでリンクしている。`abilities desc misc3 player tables treasure` の 6 連鎖が **4 ルールで反復**、`messages monsters options panel player stores tables treasure variable` の 9 連鎖が 2 ルール。対して窓口方式で作った 5 module（`stores` `panel` `messages` `device` `monsters`）は **1 module で完結** | 「1 実行形式 1 対象 module」に寄せると、`player.c` の変更で `put_misc3_test` が赤くなる状態を解消できる。ただし芋づるの根は `misc3.c` の責務同居（**実測 11 責務**。→ #40〜#42）なので、#40・#41 で 2 塊を出したあとに再計測する。~~**`misc3.c` 単体の未解決シンボルは 83 個**で、この数が減ることがそのまま成果になる~~ → **この指標は誤り**（2026-09-13、#40 で判明）。分割すると未解決シンボルは**増える**（`nm -u` 基準で 85 → 87。ファイル内の呼びだしがモジュール間の呼びだしに変わるため）。**成果は「実行形式ごとのリンク単位」で測る**——#40 で `stat_bonus_test` が 8 → 3（`src/` 側は 6 → 1）、#41 で `str_insert_test` が 10 → 2（`src/` 側は 8 → 1・代役 0）。**ただし 2 面ある**（2026-09-13、#41 で判明）。新しいテストは軽くなるが、**`misc3.c` を引く既存 4 本は切りだしたモジュールを足すぶん 1 本ずつ重くなる**（`inven_stack` 9→10、`put_misc3` 9→10、`objdes` 9→10、`haggle_comment` 11→12）。回収は #42 を終えたとき |
| 39 | 名前が意図を表さない | desc.c:143-185（`known1_p` `known2_p` `store_bought_p`） | 述語のはずが `int` でフラグ値そのものを返す（`return i_ptr->ident & ID_KNOWN2;`）。真のとき 1 ではなくビット値が返る。**`misc3.c:1052` は 2 つの戻り値を `==` で比べている**ので、今は両辺が同じビット位のため正しく動く | 実害は出ていない（呼びだし 34 箇所のうち等値比較は 1 箇所だけで、両辺が同じ述語）。危ういのは「述語なのだから 0/1 を返すべき」と片方だけ正規化する掃除をしたときで、そのとき `misc3.c:1052` が静かに壊れる。#24（鑑定状態の生ビット操作とアクセサの不統一）の具体形なので、#24 を動かすときに一緒に扱う |
| 43 | 肥大化クラス／モジュール | misc1.c misc2.c misc4.c moria1.c〜moria4.c（7 ファイル・6217 行） | `misc3.c` 以外の連番ファイル。**連番のペアは冒頭コメントまで同一**で、責務ではなくコンパイル単位で割った履歴。`misc1.c` は乱数・幾何・モンスター配置・物の一覧管理が同居し、うち乱数は既に `rnd.c` が存在する（合流先あり）。`misc2.c` は 916 行のうち 853 行が `magic_treasure` 1 関数（→ 棚上げ #26） | **#33（→ #40〜#42）を先に通す。** misc3.c で分割の手順とリンク構成の直しかたを確立してから、同じ型を残りに当てる。`misc1.c` の乱数を `rnd.c` に合流させるのが次に安いはず（新しい名前を考える必要がない）が、未計測 |
| 44 | （テストの穴） | tests/creature_stubs.c, misc3_stubs.c, save_stubs.c, fixture.c | **引数を無視して定数を返す非 void の代役が 55 個**（creature 25・misc3 13・save 11・fixture 6）。`in_bounds` は常に真、`los` は常に偽、`known2_p` `bit_pos` `damroll` `delete_object` は常に 0、`tfopen` は NULL。`refactoring-loop.md` が挙げる「モックが引数を無視している」の典型で、**この代役に守られた範囲はグリーンでも保護されていない**。とくに `tests/creature_stubs.c:182-184` の観測窓口が常に `""` / `0` を返すため、この構成ではメッセージ・画面を観測するテストは書いても中身を検証できない | 代役をまともにするだけでは意味がなく（本物を呼ぶべきか、そもそもその代役が必要かの判断が要る）、55 個をまとめて扱うと 1 コミット 1 変更にならない。**#38 で「1 実行形式 1 対象 module」に寄せると代役そのものが要らなくなる**ので、#38 の副産物として消えるのを待つのが筋。ただし**新しいテストを書くときに「この代役は引数を見ていない」ことを知らないと穴を作る**ので記録する |
| 45 | （道具の欠陥） | scripts/globals.py の alias 正規表現 | 別名（address-of）の数えかたが **`&&` とビット AND を誤検出**する。`inventory` で 20 件（105 対 85。`moria1.c` 8・`scrolls.c` 12）。`(?<![&|])` 相当の除外が無い | 本体のふるまいは変わらないので着手は安全だが、**この数字は #18 の着手順を決める指標として使ってきた**。直すと `GLOBALS_INVENTORY.md` の別名の列が全体的に下がりうるので、過去の記録との整合をどう取るかを決めてから直す |

| 46 | （ビルド定義の欠落） | makefile.win の `SRCS` | **16 個のソースが未登録**（`abilities` `device` `game_state` `input` `input_ncurses` `item_ident` `messages` `options` `panel` `platform` `render` `render_ncurses` `signal_flags` `signals` `stores` `view_observer`）。**着手前からこの状態**で、Windows ビルドはもともとリンクできない。`makefile.win` に他のソース列は無いので抜け道もない | 直すこと自体は機械的だが、**リンクが通るかを確かめる手段がこの環境に無い**（Windows でビルドできない）。「変更後にビルドが通ることを確認できる」という前提が満たせないので、#9 を保留したときと同じ理由で着手しない。ただし**新しい `.c` を足すたびに 3 箇所を更新する作法は守る**（#40 でも守った）——直す人が現れたときの差分を小さく保つため |

| 47 | 肥大化クラス／モジュール | misc4.c:95-108 `concat` ＋ externs.h:166 `CONCAT` | `insert_str` / `insert_lnum` と同じ性質（文字列の組みたて、型依存だけ）なので **#41 に一緒に寄せる予定だったが外した**。理由は**保護がゼロ**であること——`CONCAT` 経由の呼びだしは 88 件（`creature.c` 40・`spells.c` 34・`moria3.c` 7・`moria4.c` 7）あるのに、リンクされる場面ではすべて `tests/creature_stubs.c:166` の代役に置きかわり、既存テストは本物を 1 度も通らない | 混ぜると「保護済みの移動」でなくなる（#41 の値がそこで下がる）。**先に数件で固定してから、`src/str_insert.c` へ足すのが筋**。行き先は既にあるので、必要なのはステップ A だけ。`CONCAT` マクロ自身は `vtype` の複合リテラルを使うので `externs.h` に残す（移すと新モジュールに型依存が増える） |
| 48 | 理解しづらいロジック | str_insert.c:36-53（`insert_str` の走査） | **走査の終端（`for (… pc <= bound …)`）と置きかえの可否（`if (pc <= bound)`）が 2 つの独立した条件で書かれている。** 「見つかったか」を表す変数が無いので、ループを抜けた理由（一致した／端に達した）を条件の再評価で言いあてている。#41 の検証で `pc < bound` への変異が**一致が無い入力でだけ**レッドになったのは、この二重表現が原因（`"Arrow"` が `"Arros"` になる） | ふるまいを変えずに直せる（`char *found = NULL;` を置いて 1 つの条件にする）が、**#41 は純粋な移動に限る約束で進めた**ので混ぜなかった。保護は `str_insert_test` 22 件が既にあり、リンクも 2 単位なので**着手コストは P3 で最も低い部類**。`insert_lnum` 側は既に `string` が「見つかったか」を兼ねているので、揃える形になる |

| 49 | （テストの穴） | save.c:68-417 `sv_write` と save.c:452-990 `get_char` | **セーブファイルの項目の並びを見る単体テストが 1 本も無い。** `sv_write` の `wr_*` 150 件と `get_char` の `rd_*` 151 件が同じ順序であることを保証するものが コンパイラにも無い（型が合っていれば並びが入れちがっても通る）。#19B4 で意図的な壊しを 4 件試したところ、**4 件とも 514 件グリーンのまま生き残った**（書きだしの `turn` を 0 に する／読みこんだ `turn` を捨てる／種 2 つを入れちがえる／`character_is_saved()` の判定を 落とす）。既存の 2 本が見ているのは**部品**（`save_bool_test`：`wr_short` / `rd_bool` の バイト数と値）と**塊 1 つ**（`store_save_test`：`wr_store` / `rd_store`）だけ | **#19 が作った穴ではなく、もとから空いていた穴**。埋めるには `sv_write` / `get_char` を 一時ファイルへ通して往復させるテストが要るが、この 2 つは `py` ほか 100 個超の global を 読み書きするので、往復させる前に**状態を丸ごと組みたてる足場**が必要になる（`save_bool_test` の 構成に `variable.c` の本物が入っているのはそのため）。#19B4 では代わりに**実物のセーブ ファイルで両方向の読みかえ**を確かめた（1 つ前の binary と往復。歯があることは `wr_short(noscore)` を落とした mutant が弾かれることで確認）。**手で回す確認なので CI には乗らない。** `game_state.c`（状態の写しとり）が育てば、そこを経由して 組みたてられる可能性がある |

**#49 は 2026-09-15 に追加**（#19 の副産物）。#44 と同じく**コードの臭いでは
なく保護の穴**で、#19 の作業中に意図的な壊しが生き残ったことで**測って**分かった。

**#47・#48 は 2026-09-13 に追加**（#41 の副産物）。#47 は**#41 から意図的に外した
もの**で、#48 は**移動のついでに直したくなるが我慢したもの**。

**#46 は 2026-09-13 に追加**（#40 の副産物）。

**#44・#45 は 2026-09-13 に追加**（着手順の再検討の副産物）。#44 はコードの
臭いではなく**保護の穴**、#45 は**測る道具の欠陥**で、どちらもこれまで
「`src/` を見る」検出では出てこなかった種類。

**#37〜#39 は 2026-09-13 に追加。** リファクタリング用スキルを言語中立化した
うえで現在のコードベースに再適用して見つけたもの（→ 引きつぎ資料
`HANDOVER.md`）。#37 と #38 は**テストとビルド定義そのものの臭い**で、それまで
台帳が `src/` しか見ていなかったために漏れていた。#39 はスキル側に
「真偽型のない言語で述語が何を返すか」を見る検出項目を足したことで浮かんだ。

## 棚上げ（やらない）

| # | 分類 | 場所 | 内容 | 棚上げの理由 |
|---|---|---|---|---|
| 25 | 長すぎるメソッド | creature.c:249-854 `make_attack` | 605行。switch 4段重ね・計75ケース | 単関数605行を保護するテストを書くコストが極端に高い。グローバル（py, monsters, cave）と乱数への依存が全面的で、モック化に大規模な改造が必要。分割の価値を上回る。直近1年未変更で実害も小さい |
| 26 | 長すぎるメソッド | misc2.c:19-871 `magic_treasure` | 853行。switch 10重・case 119個。全アイテム生成の中枢 | 同上。乱数依存が全面的で、付呪ルールの網羅テストは現実的でない |
| 27 | 長すぎるメソッド | scrolls.c:18-486 `read_scroll` | 469行がファイル唯一の関数。switch 40ケース | 同上。ローカル変数 `j,k,l,tmp[6]` を用途を変えて再利用しており、分割前に変数の整理が必要でコストがさらに上がる |
| 28 | 重複コード | magic.c:17-209 vs prayer.c:17-204 | `cast()` と `pray()` が構造ごと同型。前提チェック7段、マナ不足時の気絶処理が同一 | 分岐本体・メッセージ・クラス判定の3点が異なり、共通化すると引数だらけの関数になる。差異3つ以上は抽出非推奨（`patterns.md`） |
| 29 | 重複コード | spells.c:598-672 vs moria4.c:800-874 | 飛翔ループが2箇所で再実装 | 命中判定・終了条件・外れ時処理の3点が違う。描画部分のみの切り出しなら可能だが効果が小さい |
| 30 | 重複コード | store2.c:435-590 vs 591-793 | 値切り交渉が購入/売却で並行実装 | 価格計算方向・insult上限・成立条件が逆。骨格が似ているだけで意味は反転しており、統合すると条件分岐が増える |
| 31 | データの散在 | 9ファイル約80関数の `(int y, int x)` | 座標が4形式（独立グローバル、fy/fx、バラの引数、oldy/oldx）で表現。Coord 型が無い | 置きかえ箇所が9ファイル80関数に及ぶ。テスト0件の状態で着手すべき規模ではない。将来 Coord 型を入れるなら、まず P1 でテストを充実させてから |
| 32 | 強すぎる依存関係 | player.c 全体 | 関数0個、530行が全部グローバルデータ。`py.` は全src で500回以上参照 | 500箇所の参照を持つグローバルの構造化は、このリファクタリングの範囲を超える。設計変更として別途扱うべき |
| 33 | 肥大化クラス／モジュール | misc3.c 全体 | ~~2212行に6責務~~ → **実測 2090 行に 11 責務** | **2026-09-13 に棚上げを解除。** 上記「#33 の精査」に分解を記録し、P1 の #40・#41（計測で独立が確認できた 2 塊）と P2 の #42（残る **10 塊**）に分けた。棚上げ時の理由「一括分割はしない」はそのまま維持する |
| 34 | **未接続の基盤**（旧 #3） | view_observer.c/.h 全体（296行） | Observer 基盤。全10関数の参照が宣言+定義のみで、登録する View 実装が存在しない | **参照ゼロだが削除しない。** 導入コミットが `Add Observer pattern foundation for Model-View separation`、直近も `Wrap Observer callback arguments in type-safe structures` で変更されている。これは残骸ではなく、Model-View 分離のために**先行して用意された基盤**。参照がないのは未完成だからで、消すと進行中の UI 抽象化作業を壊す。接続の予定がなくなった時点で再評価する |
| 35 | **未接続の基盤**（旧 #4） | game_state.c/.h 全体（277行） | GameState 構造体。`game_state_init` の呼び出し0件。externs.h の裸グローバル約90個と二重定義 | **参照ゼロだが削除しない。** 導入コミットが `Add GameState structure to encapsulate global state`。グローバル状態のカプセル化を意図した基盤。ただし「コピーした瞬間に元と乖離する」設計上の問題があるため、接続時に設計を見直す必要がある（→ バグ候補 B3） |
| 36 | **未接続の基盤**（旧 #5） | render.h:61-63, render_ncurses.c:225-254 | `get_char`/`check_input` が InputBackend の `get_key`/`check_available` と役割重複。`->get_char` 参照0件 | **参照ゼロだが削除しない。** 導入コミットが `Add rendering abstraction layer foundation`。ただしこの 2 関数は input 側に同等機能が既にあり、**3 項目のうち唯一「重複」の性質が強い**。input 側への統合が完了したと確認できれば削除候補に戻せる。作業者に意図を確認するのが最短 |

## バグ候補（このリファクタリングでは直さない）

| # | 場所 | 内容 | 見つけた経緯 |
|---|---|---|---|
| ~~B1~~ | ~~misc3.c:1162-1181 と 1242-1281~~ | **解消済み（#10-B）。** `items_can_stack(existing, incoming)` に抽出したので、2 箇所が「同一である保証」を人手のコメントに頼る状態そのものが無くなった。`tests/inven_stack_test.c` の 40 件が判定を固定している | #10 の調査中 → #10-B で解消 |
| B2 | spells.c:555-593 `get_flags` | 失敗時に `msg_print("ERROR in get_flags()\n")` でデバッグ文字列を画面に出力する | #8 の調査中 |
| B3 | game_state.c:22-112 | `game_state_init` が既存グローバル約50個をコピーする設計で、「コピーした瞬間に元と乖離する」。呼ばれていないので現状は無害だが、将来 externs.h を変えると同期漏れが起きる | #4 の調査中 |
| B4 | creature.c:75-86 `movement_rate` | 速度>0 で移動回数、速度<=0 で bool(0/1) を返す。単位が2種類混在しており、呼び出し側の解釈が正しいか未確認 | #14 の調査中 |
| B5 | staffs.c:39, wands.c:47 | **混乱すると魔法道具の成功率が上がる場合がある。** `chance` が負のとき `chance/2` が 0 方向に丸められて絶対値が縮み、救済抽選 `randint(USE_DEVICE - chance + 1)` の幅が狭くなる。幅が狭いほど 1 を引きやすく `USE_DEVICE` に引きあげられやすい。実測：`chance=-10` で非混乱 `randint(14)`（1/14）に対し混乱 `randint(9)`（1/9）。混乱がペナルティになっていない | #7 のステップ A（テスト保護）中。`device_chance_test.c` の `confusion_narrows_lucky_roll_range_when_chance_is_negative` に TODO で記録済み |
| B6 | staffs.c:45, wands.c:53 | **`chance == 1` では成功が原理的にありえない。** `randint(1)` は常に 1 を返し、判定は `randint(chance) < USE_DEVICE`(=3) が失敗条件なので必ず失敗する。直前の `if (chance <= 0) chance = 1;` は「わずかな成功機会を与える」意図に見えるが、実際の成功率は 0 | 同上。`device_use_always_fails_when_chance_is_one` で固定済み |
| **B15** | signals.c:88-104 と render_ncurses.c:109 | **セーブ後にサスペンド（^Z）が効かなくなる。** `nosignals()`（`signals.c:88`）が `signal(SIGTSTP, SIG_IGN)` で無効化するが、対になる `signals()`（`:97`）は `sigsetmask` を戻すだけで **`SIGTSTP` を再登録しない**。コメント `// SIGTSTP is handled by the rendering system` が理由だが、`render_ncurses.c:109` の `signal(SIGTSTP, suspend)` は初期化時に1回だけ実行されるので、`SIG_IGN` で潰されたあとは誰も戻さない。`nosignals()` は `save.c:406`, `save.c:480`, `death.c:445` から呼ばれる → **セーブすると以後 ^Z が無反応になる**。ncurses 導入後に判明。ふるまいの変更になるので直さない | 全体の再評価中（2026-08-28）。独立に確認済み |
| **B13** | creature.c:83 | **コメント `// speed must be negative here` が誤り。** この枝の条件は `speed > 0` の否定なので `speed == 0` も入る。`cspeed = r_ptr->speed - 10 + py.flags.speed`（`misc1.c:600`）なので 0 は容易に発生する。0 のときも `2 - 0 = 2` で周期 2 として正しく動くので、**コードは正しくコメントだけが誤り** | #14 のステップ A 中。独立に `cspeed` の範囲を確認済み |
| **B14** | creature.c:84 | **`turn` が負のとき「動かない」と判定される。** `turn` は `variable.c:69` で `-1` に初期化される。C の `%` は被除数が負なら 0 か負を返すので `-1 % 2 == -1` で `== 0` が成りたたない。ゲーム開始直後にこの枝を通る可能性があるが、**意図的かどうか判断できない**（実害は「最初の1ターン、遅い敵が動かない」程度で軽微）。ふるまいの変更になるので直さない | 同上。テストで現状を固定済み |
| **B12** | makefile.win | **`makefile.win` が本体の構成に追随していない。** 新規追加した `device.c` / `item_ident.c` / `abilities.c` が入っていないが、**このリファクタリング以前から放置されていた**（UI 抽象化で追加された `render.c` / `input.c` / `platform.c` / `view_observer.c` も未登録）。最終更新は `70ab060 Moves all unix.c code to io.c`。Windows ビルドは現状すでに通らないと考えられる。**私たちが壊したものではない**が、Windows ビルドを復活させるなら一括で追随が必要 | #11 のステップ B 中に判明。独立に確認済み |
| **B10** | misc3.c:973, files.c:236 | **コメント `// this results in a range from 0 to 29` が誤り。** `xfos = 40 - fos`（負なら0クランプ）なので、`fos` が 11 未満だと 29 を超える。作成時の `fos` は 11..43 に収まるが、探索つき装備が `py.misc.fos -= amount`（`moria1.c:48`）で下げるため実プレイ中に 11 を下まわる。実測：`fos=10` → `xfos=30`、`fos=0` → `xfos=40`。**コードは正しく、コメントだけが誤り**（読み手を誤解させる） | #11 のステップ A 中。独立に検算済み。`xfos_exceeds_twenty_nine_when_fos_falls_below_eleven` で固定 |
| **B11** | misc3.c:977, files.c:243 | **コメント `// this results in a range from 0 to 9` が誤り。** `xstl = stl + 1` なので最小値は 1（`stl` が 0 のとき）。範囲は 1..10 が正しい。**コードは正しく、コメントだけが誤り** | 同上。`xstl_is_one_when_stl_is_zero` で固定 |
| **B8** | misc3.c:1251-1272 `inven_carry` | **配列外読み（global-buffer-overflow）。** ループ `for (locn = 0;; locn++)` に**終了条件がない**。スタックも割りこみもできないアイテムを渡すと `inventory[]`（34 枠）を走りぬけて配列外を読む。**AddressSanitizer で実証済み**（全 34 枠を `tval=90` で埋め、`tval=10` を渡すと `misc3.c:1254` で `READ of size 1` の global-buffer-overflow）。`inven_check_num()` を先に呼ぶ約束に依存しており、破られたときの防御がない。**このリファクタリングの範囲を超える（ふるまいの変更）ので直さない** | #10 のステップ A 中。独立に ASan で再現を確認済み |
| **B9** | misc3.c:1251 vs 1164 | **`inven_carry` と `inven_check_num` の満杯判断が食いちがう。** `inven_check_num` は `inven_ctr < INVEN_WIELD`(=22) で満杯を判定して false を返すが、`inven_carry` は満杯でも挿入し `inven_ctr` を 23 にする。配列（34 枠）の範囲内なので即座には壊れないが、持ち物枠が装備欄（`INVEN_WIELD` 以降）を侵食する | 同上 |
| **B16** | str_insert.c:56-63（移動前は misc3.c:1672-1681） | **差しこみ結果が 80 バイトを超えると溢れる。** `insert_str` の作業領域は `char out_val[80]` 固定で、`strncpy` も 2 度の `strcat` も長さを見ない。呼びだし元の `desc.c` は `bigvtype`（160 バイト）を渡しているので、**呼びだし側の器のほうが作業領域より大きい**——長い名前で到達しうる。`insert_lnum` 側も `vtype str1, str2`（各 80 バイト）で同じ形。テストでは溢れさせていない（未定義動作は固定できない）。ふるまいの変更になるので直さない | #41 のステップ A 中に発見。移動前からの状態で、#41 では 1 行も変えていない。`tests/str_insert_test.c` の冒頭コメントに記録 |
| **B17** | spells.c:2124, magic.c:103, prayer.c:99 | **同じ「呪いを解く」効果で走る枠の範囲が 3 通りある。** `remove_curse()`（`spells.c:2124`）は 22..`INVEN_OUTER`(31) で **`INVEN_LIGHT`(32) と `INVEN_AUX`(33) を含まない**。`magic.c:103` は 22..33（装備の全枠）。`prayer.c:99` は 0..33 を `tval`（`TV_MIN_WEAR`..`TV_MAX_WEAR`）で絞る形なので、**袋の中の装備品も呪いが解ける**。3 つが同じ効果を別の範囲で実装している。意図的な差（効果の強さの違い）か取りこぼしかは判別できない。ふるまいは変えていない | #18-5 のステップ B（群 5・群 6）で装備の全域ループを窓口に置きかえる際に、範囲が揃っていないことに気づいた。**窓口ができたので 3 通りの差が字面に出るようになった** —— `spells.c` は `equipment_first_slot()` から始めて上端だけ `INVEN_OUTER`、`magic.c` は上端が `equipment_end_slot()`、`prayer.c` は跨ぎの窓口。3 箇所を並べれば範囲の違いが読める（`magic.c` の下端 22 は字面のまま残っている）。関連するコメントの誤り 2 件は `c25dce4` で直した |
| **B18** | save.c:914 | **`((!noscore) & 0x04)` は常に 0 なので、この `else if` の枝は 1 度も走らない。** `!noscore` は 0 か 1 にしかならないので `& 0x04` は必ず 0。意図は `!(noscore & 0x04)`（「まだ重複と印を付けていないなら」）で、括弧が 1 つ内側にずれている。**結果として `duplicate_character()` は呼ばれず、`noscore \|= 0x4` もここからは起きない。** そのため "This character is already on the scoreboard" のメッセージは出ず、`noscore & 0x4` を見る `prt_winner()`（`misc3.c:458`）の "Duplicate" の表示も**到達不能**になっている。`noscore` の他の 2 ビットは生きている（0x1 は `save.c:755` の蘇生、0x2 は `misc3.c:1661` の wizard 入り）。直すと「重複キャラはスコアに入れない」判定が動きだすので**ふるまいが変わる**——スコアボードの結果に影響するため、判断はユーザーの領分 | #19（セーブ／スコア／進行メタ）の着手前計測で `noscore` の全 15 参照を読んでいて気づいた。ビット演算としての `noscore` は 3 ビットあるが、`prt_winner()` が読む 3 通りのうち 1 通りが立たない |
| **B19** | files.c:28-35 `init_scorefile` | **開いた得点ファイルを誰も読まず、誰も閉じない。** `init_scorefile()` は `MORIA_TOP` を `"rb+"` で開いて `highscore_fp`（global）に入れる。setuid の権限があるうちに開いておく、という意図のコメントが付いており、`main.c:45` が `setuid(getuid())`（`main.c:323`）より前に呼ぶ。ところが**得点ファイルの読み書きをする 2 つの関数は自分で `fopen` する** —— `display_scores()` は `"rb"`、`highscores()` は `"rb+"`。以前はどちらもこの global に上書きしていたので「開いたものは捨てられていた」が、#19-5-1 で 2 関数を局所変数にしたので、**この handle は開かれたまま一度も読まれず、`exit(0)` まで閉じられない**（他の参照は `game_state.c:80` の写しとりだけで、`game_state_init()` は呼ばれていない → B3）。つまり setuid の意図は最初から果たされていない —— 権限を落としたあとに開きなおすので、権限の要る環境では `highscores()` 側が失敗する。**直すには「開いた handle を 2 関数に渡す」か「開かずに存在確認だけする」かの設計判断が要る**（前者は setuid の意図を活かす方向、後者は現状のふるまいをそのまま認める方向）。ふるまいが変わる — 得点ファイルの権限の扱いに影響するため、判断はユーザーの領分 | #19-5-1（`highscore_fp` の局所化）で 2 関数から global を外したときに、残った書きこみが 1 箇所だけになって見えた。局所化の前後で実物の `scores.dat` を両方向に読みかえて確認済み（`display_scores()` の表示も `highscores()` の追記も一致） |
| B7 | potions.c:323, eat.c:195, scrolls.c:470 | **`py.misc.lev == 0` でゼロ除算。** 経験値加算 `(i_ptr->level + (m_ptr->lev >> 1)) / m_ptr->lev` に防御がない。通常プレイでは到達しない（`create.c:86` が 1 で初期化、`lose_exp` は下限 1 を保つ）が、`save.c:655` の `rd_short(&m_ptr->lev)` はセーブファイルの値をそのまま読み、範囲検証をしていない。壊れた／改変されたセーブファイルでクラッシュする | #8 のステップ A 中。テストではゼロを渡していない（クラッシュするため）。`tests/item_ident_test.c` に TODO を記録 |

### バグ候補の 3 分類（2026-08-27 に整理）

作業を進めるうち、「バグ候補」に性質の違うものが混ざることがわかった。扱いが変わるので分けて記録する。

| 種類 | 例 | このリファクタリングでの扱い |
|---|---|---|
| **コードが誤っている** | B8（配列外読み）、B9（判断の食いちがい）、B7（ゼロ除算） | **直さない。** ふるまいの変更になる。報告して別作業にする |
| **コードは正しく、コメントが誤っている** | B10（`xfos` の range）、B11（`xstl` の range） | **直せる。** コメントの修正はふるまいを変えない。ただし 1 コミット 1 変更を守り、コード変更とは分ける |
| **設計上の疑問（正誤ではない）** | `xdev` が `disarm` ではなく `save` を基にしている | **直さない。** 意図が不明なものは記録して報告する。判断はユーザーの領分 |

2 番目は見落としやすい。**コメントを直すのはリファクタリングの範囲内**（外から見たふるまいが変わらない）なので、見つけたら直してよい。ただしコード変更と同じコミットに混ぜないこと。

### #12 の追加調査（2026-08-27）：a 系で片方の引数が捨てられるのは設計

ステップ A の担当が「`comment2a` の 3 件すべてが `%A1` を持たず、`comment3a` の 3 件すべてが
`%A2` を持たないので、a 系では片方の引数がつねに捨てられる」と報告した。事実を確認したうえで、
**これはバグではなく意図的な設計**と判断した。

```
comment2a（購入・最終）: "%A2 is my final offer; take it or leave it."
comment3a（売却・最終）: "I'll pay no more than %A1; take it or leave it."
```

`a` 系は「最終提示」のメッセージで、**店が示す額だけを表示すればよい**。
購入時は店の要求額（`%A2`）、売却時は店の提示額（`%A1`）。
呼びだし側の引数順の逆転（購入 `(offer, asking)` / 売却 `(asking, offer)`）と整合している。

ただし担当の指摘のうち次は妥当で、記録に値する。

- **プレースホルダの綴りを間違えても気づけない。** `insert_lnum` は置換対象がないと黙って
  数値を捨てる（`misc3.c:1907` の `strchr` が 0 を返す経路）。`%A1` を `%1A` と書いても
  エラーにならず、単に数値が表示されないだけ
- **要素数がリテラルで 2 箇所に書かれている。** 配列宣言（`comment2b[16]`）と `randint` の
  引数（`randint(16)`）。片方だけ増減させると壊れる。**現状は 4 配列すべて一致を確認済み**
  （宣言と実要素数も照合した）が、統合時にここを崩さないこと
- **`vtype`（`char[80]`）に境界検査がない。** 両プレースホルダを持つ最長テンプレートは
  `comment2b[9]` の 48 文字で、`int32_t` 最悪 11 文字 ×2 でも 64 文字に収まる。
  ただしテンプレートを 1 つ長くしただけで溢れる余地がある

**検証して却下したバグ候補**（記録しておくと再調査の手間が省ける）

| 場所 | 報告内容 | 却下の理由 |
|---|---|---|
| potions.c:322 ほかのコメント | 「`// round half-way case up` は `lev` が偶数のときだけ成りたつ。奇数では `lev >> 1` の切り捨てで切りあげにならない」 | **成立しない。** 奇数の `lev` では真の商が x.5 になりえない（分母が奇数だから）。`lev` が 3, 5 の全ケースで四捨五入と実際の値が一致することを確認済み。偶数でも .5 のケース（`lev=2, il=1` → 1、`lev=4, il=6` → 2）で切りあげが効いている。コメントは正確 |

## 作業ログ

| 日時 | 項目# | 何をしたか | テスト件数 | 結果 |
|---|---|---|---|---|
| 2026-08-26 | — | フェーズ0：`tests/minunit.h` 作成、疎通確認（グリーン/レッド両方を確認して失敗テスト削除） | 0 | OK |
| 2026-08-26 | — | フェーズ1：4区画で臭いを検出。デッドコード判定を grep で裏取り | 0 | OK |
| 2026-08-26 | — | `monster_name_indefinite` でテスト可能性を実証（レッド報告 `actual "an orc"` を確認） | 0 | OK |
| 2026-08-26 | #0,#1 | `makefile.test` 整備、`distance()` を10ケースで保護（`7b90f06`） | **0 → 10** | GREEN |
| 2026-08-26 | #2 | io.c から `curses.h` を削除（`758fcaf`） | 10 | GREEN |
| 2026-08-26 | #2 | io.c から `signal.h`/`sys/select.h`/`termios.h` を削除。io.c が ncurses 非依存でコンパイル可能になったことを確認 | 10 | GREEN |
| 2026-08-26 | #3,#4,#5 | git 履歴で導入意図を確認し、削除を中止。**未接続の基盤**として棚上げ（#34-36）に移動 | 10 | 差し戻し |
| 2026-08-26 | #6 | `check_time()` を削除（`8907a28`）。最初期からの残骸で本体は `return true;` のみ | 10 | GREEN |
| 2026-08-26 | #6 | `error_abort()` を削除（`d47a68a`）。UI 抽象化で呼び出し元が消えていた | 10 | GREEN |
| 2026-08-26 | #6 | `ignore_signals`/`default_signals`/`restore_signals` を削除（`798bf1b`）。ユーザー判断により削除。`shell_out()` 無効化にともなう残骸 | 10 | GREEN |
| 2026-08-27 | #7-A | **ステップ A（保護）**：`tests/device_chance_test.c` に24件追加（`1cfc0a5`）。`randint` を関数ポインタで受ける方式で乱数を切りはなし。**本体は無変更**。バグ候補 B5/B6 を発見 | **10 → 34** | GREEN |
| 2026-08-27 | #7-B | **ステップ B（変更）**：`src/device.c`/`device.h` に抽出（`836beca` 杖側 → `0aeca58` 棒側 → `ae09312` テストを実体に接続）。3コミットすべてグリーン、戻し 0 回 | 34 | GREEN |
| 2026-08-27 | #7-C | **ステップ C（検証）**：独立した担当が7項目を検証し **PASS**。計算式の同一性、assertion 不変（24件の期待値が完全一致）、`randint` 呼び出し回数・順序の保存、既知バグ2件の保存、makefile 統合をすべて確認 | 34 | **PASS** |
| 2026-08-27 | — | `tests/fixture.c`/`fixture.h` を追加（`2b2484d`）。リンクシームでグローバル状態依存のコードをテスト可能に。**本物のデータ（`tables.c`/`treasure.c`）をリンクし、`py` のみフィクスチャで定義、画面出力と乱数はスタブ**。本体は無変更 | 34 | GREEN |
| 2026-08-27 | #8-A | **ステップ A（保護）**：`tests/item_ident_test.c` に21件追加（`b20aef9`）。**フィクスチャ方式で本物の `known1_p`/`identify`/`sample` を検証**。本体は無変更。バグ候補 B7（ゼロ除算）を発見 | **34 → 55** | GREEN |
| 2026-08-27 | — | 報告された SUSPICIOUS 1件を検証して**却下**（`4cc1ba3`）。「`round half-way case up` が奇数レベルで不成立」は成立しない（分母が奇数だと真の商が x.5 にならない）。テストの誤コメントも修正 | 55 | GREEN |
| 2026-08-27 | #8-B | **ステップ B（変更）**：`src/item_ident.c`/`item_ident.h` に `learn_item_effect()` を抽出（`c66dc2f` potions → `1c58840` eat → `a8e1ffa` scrolls → `94ca85b` テストを実体に接続）。4コミットすべてグリーン、戻し 0 回 | 55 | GREEN |
| 2026-08-27 | #8-C | **ステップ C（検証）**：独立した担当が8項目を検証し **PASS**。とくに `i_ptr` の等価性を3ファイル個別に確認（`potions`/`eat` は戻り値を代入、`scrolls` は以降未使用）。`scrolls.c:463` の削除は「同一式だが同一値ではない」（`:161` のループが巻きもどす）ものの、到達時点では必ず同期しているため冗長と判定 | 55 | **PASS** |
| 2026-08-27 | — | 追加重複 **#8b** を発見・裏取り（`staffs.c:154`, `wands.c:152`）。同じ `ident` ブロックがさらに2箇所。`item_ident.c` が既にあるため着手コスト Low | 55 | — |
| 2026-08-27 | #8b | `staffs.c`（`a35cb7f`）と `wands.c`（`ae6d046`）を `learn_item_effect()` の呼びだしに置きかえ。**既存の21件で保護されているため新規テストは不要**。検証担当が警告した「直後の else 枝で `i_ptr` を読む」問題は、`if (i_ptr->p1 > 0)` の反対側で**排他**のため影響なしと確認（元コードの「never read after this」判断は正しかった）。`m_ptr` は `device_use_chance()` の引数で使い続けるため削除せず | 55 | GREEN |
| 2026-08-27 | — | **重複の完全解消を確認。** 経験値加算式 `m_ptr->exp += (i_ptr->level + ...)` の出現箇所は `item_ident.c` の1箇所のみ（着手前は5箇所） | 55 | GREEN |
| 2026-08-27 | #10-A | **ステップ A（保護）**：`tests/inven_stack_test.c` に40件追加（`b845644`）。**`misc3.c`（2212行・6責務同居）の実体をリンクできた** — 未解決シンボル57個を分類し、本物（`tables.c`/`treasure.c`/`player.c`/`desc.c`）＋代役（`tests/misc3_stubs.c`）で解決。本体は無変更。**実バグ B8（ASan で配列外読みを実証）と B9（2関数の判断が食いちがう）を発見** | **55 → 95** | GREEN |
| 2026-08-27 | — | 保護の実効性を確認。条件4を `<= 256` に壊すとレッド（`items_do_not_stack_when_number_total_reaches_two_hundred_fifty_six`）。`must be identical` が守りたかった不整合をテストが捕まえられる状態に。確認後は復元 | 95 | GREEN |
| 2026-08-27 | #10-B | **ステップ B（変更）**：`items_can_stack(existing, incoming)` に抽出（`7f2c2d5` check_num側 → `b555e6c` carry側）。引数名で「既存/新規」を明示。**条件3を「外側 else if」から「ループ内 AND」へ寄せる論理変換を含む**。`must be identical` コメントは実態に合わせて書きかえ。戻し0回 | 95 | GREEN |
| 2026-08-27 | #10-C | **ステップ C（検証）**：独立した担当が8項目を検証し **PASS**。6条件の保存（`(int)` キャスト含む）、**引数の既存/新規が取りちがえられていないこと**（呼びだし2箇所を変更前と個別に突きあわせ）、条件3の移動の等価性（`inven_ctr==0` と 条件3が偽 の2ケース）、B8/B9 の保存を確認。意図的な破壊でレッド（条件4→2件、条件6→3件）も確認 | 95 | **PASS** |
| 2026-08-27 | #11-A' | **担当が出力トークン上限（4096）を超えて中断。** 中途の成果に価値があったため救出してコミット（`baddf84`）：代役側で `put_buffer()` の出力を記録し `fixture_screen_text(row, col)` で読みとる仕組み。**計算結果を画面に書くだけの関数を、本体無変更で観測できるようになった** | 95 | GREEN |
| 2026-08-27 | #11-A | **ステップ A（保護）**：`tests/put_misc3_test.c` に24件追加（`8d73f05`）。分割指示（1回の書きこみで5件まで）により上限超過なく完了。`put_misc3()` を呼び画面出力を読みとって9式を観測。本体は無変更。**実装コメントの誤り B10/B11 を発見** | **95 → 119** | GREEN |
| 2026-08-27 | — | 誤ったコメントを修正（`09fbec7`）。`xfos` の `range 0 to 29`（`fos<11` で超える。探索装備が `fos` を下げる）と `xstl` の `range 0 to 9`（`+1` なので最小1）。**コードは正しくコメントだけが誤り → ふるまいを変えないのでリファクタリングの範囲内。**コード変更とは別コミットにした | 119 | GREEN |
| 2026-08-27 | #11-B | **ステップ B（変更）**：`src/abilities.c`/`abilities.h` の `calc_player_abilities()` に抽出（`e3afe41` 関数追加 → `76b474d` misc3.c側 → `86c62a0` files.c側）。9値を返すため構造体を導入。`xinfra` は書式化まで含めた（両呼びだし側の書式が完全一致だったため）。戻し0回 | 119 | GREEN |
| 2026-08-27 | #11-C | **ステップ C（検証）**：独立した担当が9項目を検証し **PASS**。8式の字面一致（`stat_adj` の `A_INT`/`A_WIS` の使い分け、`class_level_adj` の列、`/ 3` の有無をすべて個別確認）、**`xinfra` のバッファ縮小（`char[80]`→`char[24]`）の安全性**（`int16_t` の全範囲で最長13バイト、呼びだし側に幅指定なし）、`likert()` の除数8個が取りちがえられていないこと、`xdev` の設計上の疑問の保存を確認。意図的な破壊でもレッド（`A_INT`→`A_WIS` で2件、`/3` 追加で1件） | 119 | **PASS** |
| 2026-08-27 | #12-A | **ステップ A（保護）**：`tests/haggle_comment_test.c` に21件追加（`c21d7a7`）。`src/store2.c` を `#include` して `static` 関数を直接呼ぶ方式。`msg_print` の出力を記録する窓口も代役に追加。本体は無変更 | **119 → 140** | GREEN |
| 2026-08-27 | — | 報告された「a 系で片方の引数が捨てられる」を調査し**設計と判定**。`comment2a` は購入で `%A2`（店の要求額）、`comment3a` は売却で `%A1`（店の提示額）を使う。最終提示は店の額だけ表示すればよく、呼びだし側の引数順の逆転と整合する | 140 | — |
| 2026-08-27 | #12-B | **ステップ B（変更）**：`prt_haggle_comment()` に統合（`4a07ef1` 購入側 → `be868d1` 売却側）。`prt_comment2`/`prt_comment3` は薄いラッパーとして残しテストを無変更に。要素数は `sizeof` 自動計算にして二重管理を解消。戻し0回 | 140 | GREEN |
| 2026-08-27 | — | **担当が「意図的に壊してもレッドにならない」と正直に報告 → 保護の穴を発見。** 代役の `randint` が `maxval` を無視していたため、要素数の取りちがえが観測できなかった。代役に上限と呼びだし回数の記録を追加し、検証テスト4件を追加（`57b8813`）。**実証：要素数を 16→15 に取りちがえると即座にレッド** | **140 → 144** | GREEN |
| 2026-08-27 | #12-C | **ステップ C（検証）**：独立した担当が14項目を検証し **PASS**。統合の正しさ（`randint` 1回、`final > 0`、`insert_lnum` の順序、配列の対応、要素数）、呼びだし側の引数順の保存を確認。**穴埋めが機能することを独立に実証**（要素数の変異→1件レッド、`randint` 2回呼び→1件レッド）。`fixture.h` の宣言と `fixture.c` の実装の非対称を指摘 | 144 | **PASS** |
| 2026-08-27 | — | ステップ C の指摘に対応。`fixture.c` にも記録窓口を実装（`eb1bfca`）。宣言と実装の非対称を解消 | 144 | GREEN |
| 2026-08-28 | #14-A | **ステップ A（保護）**：`tests/movement_rate_test.c` に24件追加（`d580a07`）。`src/creature.c` を `#include` して `static` 関数を直接呼ぶ方式。**担当はトークン期限切れで中断したが、テストのコミットは完了していた**。周期性（`speed=0`→2、`-1`→3、`-2`→4）、負の `turn`、`rest` の影響範囲を固定。バグ候補 B13/B14 を発見 | **144 → 168** | GREEN |
| 2026-08-28 | #14-B | **ステップ B（変更）**：中断した担当から引き継ぎ、オーケストレーターが実施。`87cb4a9` 改名（`movement_rate` → `moves_this_turn`）→ `883b7f7` `? 1 : 0` で回数を返すことを明示 + B13 のコメント誤りを修正。**`if/else` の対称性は保持**（値を返す分岐は早期リターンにしない方針） | 168 | GREEN |
| 2026-08-28 | #14-C | **ステップ C（検証）**：独立した担当が8項目を検証し **PASS**。**`? 1 : 0` の等価性を実測で確認（`speed` × `turn` の 1,081万通りを網羅比較、不一致0件）**。演算子の優先順位（`(a==b) ? 1 : 0` と解釈されるか）も確認。意図的な破壊でレッド（`? 0 : 1` に反転 → 19件、`rest` の条件反転 → 4件）。コメントの重複を指摘 | 168 | **PASS** |
| 2026-08-28 | #15 | ヘッダの三重管理を解消（`2e24ccc` include を `backend_ncurses.h` に統一 → `7c1f8a7` 不要な2ヘッダを削除）。**`makefile` の依存記述も更新し、クリーンビルドで確認。** ncurses backend の公開窓口が1つになった | 269 | GREEN |
| 2026-08-28 | #9 | 端末モード設定5行を `set_terminal_mode()` に抽出（`296028e`）。`ncurses_init` と `suspend` で共有。ビルド・テスト・ゲーム起動で確認 | 269 | GREEN |
| 2026-08-28 | #17b-A | `chr_adj` にテスト16件追加（`9058631`）。22段すべての境界を押さえ、刻みが変わる点（3→4 で 5、14→15 で 1）と `switch` から `if` 連鎖に移る境界（18→19）を重点的に固定 | **269 → 285** | GREEN |
| 2026-08-28 | #17b-B | `chr_adj` をテーブル引きに書きかえ（`7760fea`）。`if` 連鎖5段 + `switch` 17ケースの混在を22段のテーブルに。**0..255 の全域で等価を確認**。`misc3.c` は 2124 → 2086 行（-38行）。境界を1つずらすとレッドになることも確認 | 285 | GREEN |
| 2026-08-28 | — | **`makefile.test` の警告フラグを本体に近づけた**（`5fe095c`）。12フラグのうち代役の性質と無関係な5つを追加。除外理由もコメントに明記。**この作業で不備2件が判明**：(1) `chr_adj` の宣言漏れ（暗黙宣言になっていた）、(2) **#12 で導入した引数名 `normal_table`/`final_table` が `externs.h:148` のグローバルと衝突**していた（`normal_comments` 等に改名）。テストコード由来の警告は 0 件に | 285 | GREEN |
| 2026-08-28 | — | ステップ C の指摘に対応。コメントの重複を整理（`fb4ccb5`）。設計背景の NOTE は残した | 168 | GREEN |
| 2026-08-28 | #16 | ラベル `inkey` → `retry`、`default` を後置（`b5107b8`）。**`case` の列挙は残した** — 受理する文字の集合を宣言的に書けており、条件式や `strchr` に変えると宣言性が失われる。`strchr` は `'\0'` にマッチする落とし穴もあり、`ESCAPE` マクロを文字列に埋めこめない。専用関数の抽出も 4 要素には過剰。**類型を横断調査したが、同種は `io.c:236` の1文字スキップのみで、一般化（配列＋線形探索）は割に合わない**（3件目が出たら再検討） | 168 | GREEN |
| 2026-08-28 | #17-A | **ステップ A（保護）**：`tests/stat_bonus_test.c` に101件追加（`a66fe08`）。5関数の全段の境界を「境界値-1 と 境界値」の対で網羅。本体は無変更。**全境界値の一覧を報告させ、それが当初の見立てを覆した** | **168 → 269** | GREEN |
| 2026-08-28 | — | **当初の検出「同じマジックナンバーが5箇所に散在」が不正確と判明。** 20個の境界値が6リストに分散し、全リストに共通するのは 0 と 18 のみ（実質 18 だけ）。共通化は達成できないと判断し、方針を「各関数が自分のテーブルを持つ」に変更（`671c5ca`） | 269 | — |
| 2026-08-28 | #17-B | **ステップ B（変更）**：6コミットで段階的にテーブル化（`48e8e61` 仕組み → `df18a5b` todam → `d761a02` toac → `f08ea50` todis → `5468436` stat_adj → `8d0a794` tohit）。単純な関数から着手し、最も間違いやすい `stat_adj`（降順→昇順）を後半に。要素数は `sizeof` 自動計算。**`misc3.c` が 2195 → 2124 行（-71行）** | 269 | GREEN |
| 2026-08-28 | #17-C | **ステップ C（検証）**：独立した担当が9項目を検証し **PASS**。**`use_stat` が `uint8_t` なので全数検査が可能**という性質を活かし、6リストすべてを 0..255 の全域で旧実装と照合（`tohit_adj` は 256×256 = 65,536 通りの全組みあわせも）。不一致0件。既知の不整合3件の保存、厳格な警告フラグでの警告数の同一性（旧335件 = 新335件）も確認 | 269 | **PASS** |
| 2026-08-28 | — | **ステップ B の報告の数値誤りを発見。** 「2テーブル入れかえで47件レッド」と報告されたが、ステップ C の実測は18件。オーケストレーターも独立に再現して**18件**を確認。結論（テーブルの取りちがえが検出される）は変わらないので差し戻さず、記録を正しい値に直した | 269 | GREEN |
| 2026-08-28 | — | #16 の議論から `wait_for_more` グローバルフラグの課題が浮上。代替案を3つ検討し、`get_check` での画面保存・復元が正しい方向と判断。ただし ncurses 依存・呼びだし元多数・ふるまいの変更を伴うため保留。**グローバル変数の削除という大きな枠組みで扱う事項**として記録 | 168 | — |
| 2026-08-29〜09-06 | 警告 | **コンパイラ警告を 8,338 件 → 0 件**（`fix/compiler-warnings` の 26 コミット）。種類ごとに 1 コミットで、件数の多さではなく危険度の順に扱った。`-Wstrict-prototypes` 5,530 → `externs.h` の 141 関数に引数を書く（`fa8cf63` `d504eac`）、`-Wdiscarded-qualifiers` 2,433 → 読みとり専用の文字列を `const char *` に（`af06422` `6479769`）、書式・書きこみ先の溢れ 36 件（`12ac459` `346572f` `6161196`）。詳細は「コンパイル警告の解消」節 | 285 | GREEN |
| 2026-09-06 | 警告 | **警告の陰にあった実際の不具合 9 件を修正。** `wizard.c:367` の `sscanf("%lx")` が 4 バイトの変数に 8 バイト書く（`3c8438e`）、`death.c:164` が `died_from` の配列外に書く（`40fdf20`）、`game_state.c:66` の終端しない `strncpy`（`cca6db9`）ほか。**警告を消すことではなく、これが目的だった** | 285 | GREEN |
| 2026-09-07 | 警告 | **最後の 2 件は「ふるまいが変わる」と見ていたが、変わらない直しかたがあった。** `monsters.c` の逆順走査を `std::reverse_iterator` と同じ持ちかたの反復子に包む（`1b5ac04` テスト → `6dd8df3` 本体）、`save.c` の真偽値を `uint16_t` で受けてから `bool` に詰める（`b98197b` テスト → `5d7ddb4` 本体）。どちらも先にテストで固定してから直した。`makefile.test` の本体由来の抑制も外した（`a5d05cf`） | **285 → 310** | GREEN |
| 2026-09-07 | — | `fix/compiler-warnings` を `develop` にマージ（`f54cb4c`）。警告 0 件・310 件グリーンを確認 | 310 | GREEN |
| 2026-09-09 | #18 | **着手前の計測。** `scripts/globals.py` で `externs.h` の 112 個を 11 区分に棚おろし（`b2199fb`、→ `GLOBALS_INVENTORY.md`）。**#18 の待ちの理由（`view_observer.c`）が成り立たないことが判明** — `view_observer.c` は 112 個を 1 個も参照していない。参照 3147 の 47% が 6 個に集中しており、区分ごとに分割して着手する。最初はオプション 11 個（既に `misc2.c:856` に登録表がある） | 310 | — |
| 2026-09-11 | #18-1A | **保護**：`src/options.c` に 11 個の表（表示名・値の置き場・セーブのビット）を置き、`tests/options_test.c` を追加（`7a0fafc`）。この時点では誰も使っていない（本体のふるまい不変）。書きこみ・読みこみは `sv_write()` / `rd_*` の中の if 文で関数として呼べないので、**#7 と同じ「テスト側に写す」方式**をとり、変更前の並びを写して **2^11 = 2048 通りで全数照合**。ビットを 2 つ入れかえると 2 件レッド、表示名を 1 つ変えると 1 件レッド（実証・復元済み） | **310 → 318** | GREEN |
| 2026-09-11 | #18-1B | **変更**：`save.c` の if 文 22 個（書く 11・読む 11）を `game_options_pack()` / `game_options_unpack()` の 2 行に（`94127a7`）。**save.c は 95 行減**。5.2.2 以前の互換だけは save.c に残した（ビットの対応ではなく「そのビットが無い形式」の話なので、表に持たせるところではない）。3 分岐から「入れたあと上書き」に形は変わるが最終の値は同じ | 318 | GREEN |
| 2026-09-11 | #18-1C | **変更**：`set_options()` が `misc2.c:856` の自前の表をやめ、同じ表を読む（`93a4cc9`）。走査の形は変えていない。これで表示名・値の置き場・ビットを知る場所が 1 つになった | 318 | GREEN |
| 2026-09-11 | #18-1D | **変更**：既定値も表の 4 列目に集め、`main.c` は `game_options_reset()` を呼ぶだけに（`f74ab29`）。着手前は 10 個が `variable.c` の初期値、1 個（`rogue_like_commands`）だけ `main.c` が `config.h` から入れる形で分かれていた。既定値の写しをテストに置き、1 つ変えるとレッドになることを確認。**キャラクター作成画面まで起動することも確認** | **318 → 321** | GREEN |
| 2026-09-11 | #18-2A | **保護**：`src/messages.c` にメッセージ履歴の輪を置き、`tests/messages_test.c` を追加（`5656624`）。呼びだし側（`io.c` / `dungeon.c` / `save.c`）は無変更。輪の回りかたは関数の中に直に書かれていて呼べないので、#18-1 と同じ「テスト側に写す」方式。押しこみ 0〜66 回（3 周ぶん）× 辿り 0〜21 個の全組みあわせで変更前の辿りかたと照合。**意図的に壊してレッドを 4 通り確認**（折りかえさない → 22 回目で検出、周期を 21 にする → 2 件、つなぎの空白を 1 個に → 1 件、枠の添字を 1 つずらす → 1 件） | **321 → 335** | GREEN |
| 2026-09-11 | #18-2B | **変更**：輪の回りかたを知る 3 箇所を窓口越しに書きかえ（`e270409`）。副産物 3 つ —— `do_command()` のローカル `j` が不要に、`save.c` から `(uint16_t *)&last_msg` のキャストが消え、`io.c` の `sprintf` が 1 つ減った。`^P` は添字の向きが逆になるので、行数 2〜22 × 押しこみ 0〜44 回の全組みあわせで (行, メッセージ) の対応が変わらないことを確認 | **335 → 336** | GREEN |
| 2026-09-11 | #18-2C | **変更**：`old_msg` / `last_msg` を `static` にし、`externs.h` から外した（`97173ce`）。**#18 で項目が実際に減ったのはここが最初（112 → 110）**。残っていた直接参照は `game_state.c` の 2 行だけで、輪を 1 つの塊として指す設計なので塊を渡す窓口を 1 つ足した。`scripts/globals.py --check` が「`externs.h` に無い」と教えるので `GROUPS` も更新 | 336 | GREEN |
| 2026-09-11 | #18-2D | **変更**：`msg_flag` / `wait_for_more` も `static` にし、意味を言う名前に（`msg_pending` / `msg_at_more_prompt`、`8c2837e`）。**メッセージ表示の区分 4 個がすべて片づき、`externs.h` は 108 個**。`store2.c` の「clear the msg flag just like we do in dungeon.c」という他所を見にいかせるコメントも、その場の理由に書きかえた。`make clean` からの全ビルドで警告 0 件 | **336 → 340** | GREEN |
| 2026-09-11 | #18-2E | **変更**：セーブファイルの言う添字を輪への入口で範囲へ畳む（`f47666f`）。変更前は検査なしの値で `old_msg[j]` を直に引いており、添字 40000 のセーブファイルで `^P` が配列外を読み `msg_print()` が配列外へ書いていた。正しい値（0〜21）は同じ値に畳まれるのでまともなセーブファイルのふるまいは変わらない。**寄せたから検査する場所が決まった**（変更前は入口が 3 箇所）。畳みを外すとレッド（添字 40000 で -25536）、来うる値 0〜65535 の全数でも確認 | **340 → 342** | GREEN |
| 2026-09-11 | — | **#18-2 のコミットメッセージ 3 通（`5656624` `e270409` `8c2837e`）に書いたテスト件数が 10 件ずつ少ない**（325 / 326 / 330 と書いたが、実際は 335 / 336 / 340）。集計時の読みまちがい。グリーン／レッドの結論は変わらないので履歴は書きかえず、正しい数をこの表に残す（2026-08-28 の数値誤りと同じ扱い） | 340 | GREEN |
| 2026-09-13 | — | **マージ**：`refactor/18-1-options` と `refactor/18-2-messages` を `develop` へ（`4bd3c52` `3086a9c`、どちらも `--no-ff`）。マージ後に全ビルド・全テスト・種族選択画面までの起動を確認 | 342 | GREEN |
| 2026-09-13 | #18-3A | **保護**：`src/panel.c` にパネルの計算（導出・移動の判定・範囲の判定）を置き、`tests/panel_test.c` を追加（`92acf54`）。値 10 個はまだ `variable.c` のまま。`get_panel` は `end_find()` を呼ぶので、副作用だけ `misc1.c` に残して `panel.c` を依存なしにした。変更前の 3 関数をテストに写し、出発点 28 通り × ダンジョン全域の外 4 マスまで × `force` 両方で総当たり照合。**意図的に壊してレッドを 3 通り確認**（`row_prt` の 1 → 2 で 2 件、`col_prt` の 13 → 12 で 3 件、移動の判定の余白 2 → 3 で 2 件） | **342 → 353** | GREEN |
| 2026-09-13 | #18-3B | **変更**：パネルにふれる 7 ファイル（`io.c` `misc1.c` `spells.c` `generate.c` `dungeon.c` `save.c` `game_state.c`）を窓口越しに書きかえ（`a41c421`）。数の重複が 2 つ消えた —— `generate.c` のパネル数の式（同じ 4 行が 2 回）と、地図の左上（`prt_map` が自分で数えていた行番号と 13）。`save.c` の `(uint16_t *)&max_panel_rows` も #18-2E と同じ形のキャストで、ローカル経由に直した | **353 → 354** | GREEN |
| 2026-09-13 | #18-3C | **変更**：10 個を `panel.c` の `static` にし、`externs.h` から外した（`cf75e58`）。**108 → 98 個・参照 3073 → 2974**。`panel.c` は `externs.h` も include しない。テストの出発点は窓口だけで作る形に直し、その過程で写しに**本体がなりようのない状態**が混ざっていたことがわかった。網は狭まっておらず、意図的な壊しは以前より多くの件数で落ちる（4 通り確認） | 354 | GREEN |
| 2026-09-13 | — | **マージ**：`refactor/18-3-panel` を `develop` へ（`47129ea`、`--no-ff`）。docs の書きもどし（`ff2c09a`）も含む。マージ後に全ビルド・全テスト・種族選択画面までの起動を確認 | 354 | GREEN |
| 2026-09-13 | #18-4A | **変更**：`last_store_inc` を `store2.c` の `static` に（`e90f7f6`）。15 年前から「なぜ `extern` なのか、`store2.c` しか使っていない」と FIXME が付いていた値。使うのは値切りの 3 関数だけ。`tests/haggle_comment_test.c` がリンクを通すために置いていた代役の定義も消した（`store2.c` を `#include` するテストなので二重定義になる）。この 1 個には写す相手がいない —— 0 初期化のまま同じ翻訳単位に閉じただけで、保証するのはリンカ。セーブファイルにも入っていないので外から観測できる差は無い | 354 | GREEN |
| 2026-09-13 | #18-4B | **変更**：`store[MAX_STORES]` を `src/stores.c` の `static` にし、窓口を `store_at()` / `store_count()` に（`ae161d1`）。**98 → 96 個・参照 2974 → 2886**。`store_type *s_ptr = &store[n]` で始まる 22 箇所（`store1.c` 10・`store2.c` 9・`save.c` 3）と `game_state.c:40` を置きかえ。**この区分には隠せる計算が無い**ので、`tests/stores_test.c`（8 件）は「置き場としてのふるまい」を押さえた —— 軒数、添字ごとに別の記録、記録が重なっていない、同じ添字は同じ記録、書いたものが残る、在庫 24 枠が収まる、走りだしが空。**意図的に壊してレッドを 4 通り確認**（常に 1 軒目を返す → 2 件、添字を 1 ずらす → 1 件、軒数を 5 に → 2 件、写しを返す → 4 件） | **354 → 362** | GREEN |
| 2026-09-13 | #18-4C | **変更**：セーブデータの店 1 軒の並びを `wr_store()` / `rd_store()` にまとめた（`a15953f`）。`save.c` の 3 箇所（書き 1・読み 2）に同じ項目列があり、**読む 2 箇所は字下げをのぞいて同一の 17 行**だった（いまの形式と 5.1.3 より前の形式が同じ並び）。品物 1 つは `wr_item` / `rd_item` になっていたのに店 1 軒はなっていなかったので同じ形に揃えた。在庫数の妥当性判定も関数の中に入り、投げだす合図だけが戻り値に。保護は「テスト側に写す」方式（`tests/store_save_test.c`、7 件）で、**バイト列が 1 バイトも違わないこと**と `memcmp` による全項目復元を両方向で照合。**意図的に壊してレッドを 4 通り確認**（`good_buy`/`bad_buy` の順を入れかえ → 3 件、在庫数を 2 バイトに → 4 件、上限を 1 甘く → 1 件、読む枠を 1 つ少なく → 2 件） | **362 → 369** | GREEN |
| 2026-09-13 | — | **マージ**：`refactor/18-4-stores` を `develop` へ（`4edb913`、`--no-ff`）。docs（`6ba8c14`、所見 12 と #18-4 の節）も含む。マージ後に全ビルド・全テスト・種族選択画面までの起動を確認。**#18 は 4 区分（オプション 11・メッセージ 4・パネル 10・店 2）が片づき、`externs.h` は 112 → 96 個** | 369 | GREEN |
| 2026-09-13 | — | 引きつぎ資料 `HANDOVER.md` を追加。次の担当（#18-5 持ち物 6 個）に渡す前提・作法・地雷をまとめた。この作業ログの追随漏れ 5 行も同時に補った | 369 | GREEN |
| 2026-09-13 | — | リファクタリング用スキルを言語中立化（言語固有の内容を `lang/c.md` などのパックへ分離）し、そのスキルを現在のコードベースに再適用。**P3 に #37〜#39 を追加**——代役ファイル 2 つの 19 シンボル重複、`makefile.test` の 6〜9 module 連鎖（窓口方式で作った 5 module は 1）、`int` でフラグ値を返す述語 3 つ。いずれもコードは未変更 | 369 | GREEN |
| 2026-09-13 | #33 | **記録のみ**：スキルの「肥大化クラス」をモジュール単位に一般化して再適用し、**#33 の棚上げを解除**。`misc3.c` の 87 関数を 11 塊に分け、行き先と既存の保護を表にした（記録は 2212 行 6 責務だったが**実測 2090 行 11 責務**）。切り目はリンカに聞いた——**`misc3.c` 全体は未解決シンボル 83 個、能力値まるごとは 8 個、補正表だけなら `py` の 1 個、`insert_str`/`insert_lnum` は 0 個**。この 2 塊を P1 の #40・#41 に、残る 9 塊を P2 の #42 に、`misc1/2/4` と `moria1〜4`（6217 行）を P3 の #43 に置いた。**名前が何も約束していないファイルは 8 個・8307 行で `src` の 29%**。連番のペアは冒頭コメントまで同一だった。コードは未変更 | 369 | GREEN |
| 2026-09-13 | 着手順 | **記録のみ**：`HANDOVER.md` 第 5 節（次は #18-5）と P1 に後から入った #40・#41 が食いちがっていたので、スキルの「保護しやすく効果が大きいものから」に照らして 3 者を測りなおした（→ 上記「着手順の再検討」）。作業役 4 名に読みとりだけを委譲。**結論は #40 → #41 → #18-5**。台帳の数字は **11 箇所ずれていた** —— #40 は 118→116 行で `con_adj` が無保護、#41 は 69→67 行で `concat` に保護なし・`insert_lnum` の保護は `haggle_comment_test`、ビルド定義は 2 個ではなく 3 個（`makefile.win`）、#18-5 の実体は `variable.c` ではなく `treasure.c`・別名 105→**85**・触るファイル 27→28・`moria1.c` 64→117、`misc3.c` の未解決シンボルは 83／57／**76** の 3 つの数字が併存。新事実として **#40 の抽出で `stat_bonus_test` の依存が 8→3 コンパイル単位**、**`inventory` の参照は装備側 112 が持ち物側 64 より多い**、**別名 85 件は `&inventory[x]` の 1 形に収束**、**`sorted_objects` と `object_ident` は持ち物と独立**。P3 に #44（引数を無視する代役 55 個＝保護の穴）と #45（`globals.py` の別名正規表現の誤検出）を追加。コードは未変更 | 369 | GREEN |
| 2026-09-13 | #40A | **保護**：`con_adj` に 12 件（`c0d78cb`）。台帳は「#40 は着手時点で保護済み」としていたが、**7 関数のうち `con_adj` だけテスト 0 件**だった。しかも唯一テーブルを持たない if/else 連鎖（6 段、閾値 7/17/18/94/117）で、引数を取らず `py.stats.use_stat[A_CON]` を直接読む——塊の依存が `py` 1 個である理由がこれ。閾値の両側を固定。**意図的な壊しを 2 通り確認**（閾値 94 → 95 で 1 件、`return 4` → `5` で 2 件）。**保護の届かない等価変異が 1 つある**（`con < 7` → `con < 8` はレッド 0 件。最下段が `con - 7` なので `con == 7` は次段の 0 と同値で原理的に区別できない） | **369 → 381** | GREEN |
| 2026-09-13 | #40B | **変更**：能力値の補正表 116 行を `src/misc3.c` から `src/stats.c` へ移した（`fd011a0`）。**`misc3.c` は 2090 → 1973 行**。`externs.h` の 7 宣言を新設 `src/stats.h` へ移し、呼びだし側 10 ファイルに `#include "stats.h"` を足した。`stats.c` は手本（`panel.c` `stores.c` `options.c`）に倣い **`externs.h` を include しない**——`py` の宣言は二重になるが、1 行のために ncurses まで引きこむ全域ヘッダは取りこまない（理由は `src/stats.c:17-23` に英語で記載）。移動の正しさは字面で確認（削除分と `stats.c:26-142` の `diff` が末尾の空行 1 行だけ）。`static` の `lookup_stat_bonus` は `static` のまま。ビルド定義 3 つ（`makefile` `makefile.win` `makefile.test`）を更新 | 381 | GREEN |
| 2026-09-13 | #40C | **変更**：`stat_bonus_test` のリンクを **8 → 3 コンパイル単位**に（`3574cbd`）。`src/misc3.c desc.c tables.c treasure.c player.c abilities.c` ＋ `misc3_stubs.c` を引くのをやめ、`src/stats.c` ＋ 新設 `tests/stats_fixture.c`（`py` と `fixture_reset` だけ、**代役 0 個**）だけになった。**#38 を数字で減らした 1 件目。** 未解決は `py` と `fixture_reset` の 2 個のみ | 381 | GREEN |
| 2026-09-13 | #40 検証 | **独立した検証**：別の担当が `55ecad3..HEAD` を 8 項目で点検し、件数の推移（369 → 381 → 381 → 381）・16 実行形式すべての存在・assertion 不変（差分は手書き `extern` 7 行 → `#include "stats.h"` と冒頭コメントだけ）・移動 116 行の字面一致を確認。**ミューテーションは 4 通り実測して予測と全一致**（テーブル要素で 3 件、閾値で 1 件、`con_adj` の返り値で 1 件、`lookup_stat_bonus` の継続条件 `>=` → `>` で **76 件**＝`stat_bonus_test` 73 ＋ `put_misc3_test` 3）。**テストの穴は無**。副産物として **#38 の成果指標が誤りだと判明**（`misc3.c` の未解決シンボルは分割で 85 → **87** と増える。正しい指標は実行形式ごとのリンク単位）と、**`makefile.win` に 16 個のソースが着手前から未登録**（→ P3 #46） | 381 | GREEN |
| 2026-09-13 | — | **マージ**：`refactor/40-stats` を `develop` へ（`8ac9d10`、`--no-ff`）。docs（`702b10e`、#40 の節と #38 の成果指標の訂正、P3 #46）も含む。マージ後に全ビルド（警告 0）・全テスト 381 件・`globals.py --check`・種族選択画面までの起動を確認。**#33 の 11 塊のうち 1 つめ（塊 C）が片づき、`misc3.c` は 2090 → 1973 行** | 381 | GREEN |
| 2026-09-13 | #41A | **保護**：`tests/str_insert_test.c` を新設して 22 件（`82b1eed`）。台帳は「#41 は着手時点で保護済み」としていたが、**通っていない分岐が 2 つ**あった —— `insert_str` の `insert` が非 NULL の経路（`objdes_test` が通す `~` 入りの雛形は食料 2 件だけで、そこは `CNIL`。つまり**複数形化 `"ch~"→"ches"` を守るテストは 0 件**）と、`insert_lnum` の `show_sign` が真の経路（呼びだし 2 箇所はどちらも `false`）。行番号も記録とずれていた（#40 が 116 行抜いたため `1767-1835` → **`1649-1716`**、69 → **67 行**）。実体はまだ `misc3.c` にあるので、リンクは `objdes_test` と同じ一時的な重い構成にし、その旨をコメントに明記。**意図的な壊しを 6 通り実測**（6／9／27／8／9／16 件）。うち `strncmp` の長さを 1 短くする変異は最初の 20 件では**レッド 0 件**で、`"%A1"` と `"%A2"` を見分ける 2 件を足して塞いだ。バグ候補 **B16**（`out_val[80]` の溢れ）を発見 | **381 → 403** | GREEN |
| 2026-09-13 | #41B | **変更**：`insert_str` / `insert_lnum` の 67 行を `src/misc3.c` から `src/str_insert.c` へ移した（`6aeae73`）。**`misc3.c` は 1973 → 1904 行**（-69 = 関数 68 行＋区切りの空行 1 行、追加は 0 行）。`externs.h` の 2 宣言を新設 `src/str_insert.h` へ移し、呼びだし側 2 ファイル（`desc.c` `store2.c`）に include を足した。**名前を `strings.c` にしない**（`-Isrc` が `<strings.h>` を横取りしうる）。依存は**プロジェクト内シンボル 0 個**なので、`stats.c` が要した手書き `extern` すら不要 —— `vtype` 型にだけ依存するので include の並びだけ揃えた（`vtype` を `char[80]` に書きかえれば型依存も消えるが、純粋な移動でなくなるので採らない）。移動の正しさは字面で確認して**差分 0 行**（#40 は空行 1 行の差があった）。リンクに `src/str_insert.c` が要る実行形式は**リンカに聞いて 5 つ**。ビルド定義 3 つを更新（`makefile` の依存行は `$(HEADERS_FULL)` ではなく `$(HEADERS_COMMON)`。`externs.h` を include しないため） | 403 | GREEN |
| 2026-09-13 | #41C | **変更**：`str_insert_test` のリンクを **10 → 2 コンパイル単位**に（`b1449bf`）。`src/` 側は 8 → 1（`misc3.c stats.c desc.c tables.c treasure.c player.c abilities.c` を引くのをやめた）、`tests/` 側は 2 → 1（`misc3_stubs.c` が不要に）。**代役 0 個・フィクスチャ 0 個**で、集合全体の未解決シンボルは自前 0 個（全部 libc）。**#38 を数字で減らした 2 件目**で、`stat_bonus_test`（8 → 3）より軽い最小構成。テスト本体は 1 行も変えていない | 403 | GREEN |
| 2026-09-13 | #41 検証 | **独立した検証**：別の担当が `115308b..refactor/41-str-insert` を 7 項目で点検し PASS。`115308b` の worktree を作って**保護前 381 件を自分で実測**し、`grep -c "RUN_TEST("` = 403 と実行件数の一致まで確認。既存テストの assertion は 1 行も触られていない（`tests/` の差分は新規 1 ファイルのみ）。`src/` の差分は移動・宣言・include だけ。字面一致（差分 0 行）と行数の推移（1973 → 1904）も測りなおして一致。**ミューテーション 6 通りすべてレッド、生き残りは 0**（#40 では等価変異が 1 つ残った）。**ステップ A の「部分的な等価変異」の申告は誤りで、`pc <= bound` → `pc < bound` は殺される**（一致が**無い**入力で `"Arrow"` → `"Arros"`。`str_insert_test` 1 件＋`objdes_test` 8 件）——**変異が生き残ったという報告は独立に確かめる。** 副産物として **#38 の指標に 2 面あることが判明**（新テストは 2 本で済むが、`misc3.c` を引く既存 4 本は 1 本ずつ重くなる。9→10・9→10・9→10・11→12）。P3 に **#47**（`concat` は保護ゼロなので #41 から外した）と **#48**（`insert_str` の走査の終端と置きかえの可否が二重表現）を追加 | 403 | GREEN |
| 2026-09-13 | — | **マージ**：`refactor/41-str-insert` を `develop` へ（`5b321ef`、`--no-ff`）。docs（`8524c02`、#41 の節・#38 の指標の 2 面性・P3 #47・#48・バグ候補 B16）も含む。マージ後に全ビルド（警告 0）・全テスト 403 件（17 実行形式）・`globals.py --check`・種族選択画面までの起動を確認。**#33 の 11 塊のうち 2 つめ（塊 H）が片づき、`misc3.c` は 1973 → 1904 行**（着手時 2212 から -308 行）。残る 9 塊は P2 の #42 | 403 | GREEN |
| 2026-09-14 | #18-5A | **保護**：`src/inventory.c` 85 行と `src/inventory.h` 55 行・`src/equipment.h` 45 行を置き、`tests/inventory_test.c` に 25 件（`0b96757`）。**呼びだし側は 1 箇所も変えず、実体も `treasure.c` に残したまま**で、窓口が `extern` 越しに読み書きする＝ふるまい不変。この区分にも隠せる計算が無いので #18-4 の `stores_test.c` と同じ「置き場としてのふるまい」を押さえる形にした —— 走りだしが空、枠 22 と装備 22..33 の境界、添字ごとに別の枠、枠が重なっていない、同じ添字はいつでも同じ枠、書いたものが残る、個数と重量の出し入れ（負の個数も `int16_t` の折りかえしも現状のまま固定）、跨ぎの窓口が持ち物側・装備側と同じ枠を指し全 34 枠をちょうど 1 度ずつ覆うこと。**窓口は範囲を検査しない**ので、持ち物の窓口に 22 を渡すと装備に届くことも仕様として固定した（検査を足すとふるまいが変わる）。足場は `tests/inventory_fixture.c` 31 行 | **403 → 428** | GREEN |
| 2026-09-14 | #18-5B 群 1 | **変更**：`moria1.c` の 98 件を窓口越しに（`76e5f5f..f6b813c` の 6 本）。単独の群にしたのは件数が最多で、`get_item`（`moria1.c:1189`）が跨ぎの中枢だから。実測では跨ぎは 1 件だけで、残りは持ち物側と装備側にきれいに分かれた。`inven_command` が `moria4.c` ではなく**この群にある**ことも判明（偵察の誤り） | 428 | GREEN |
| 2026-09-14 | #18-5B 群 2 | **変更**：`misc3.c` の 41 件を窓口越しに（`b15ce9d` `0f78f3c` `97931d4` `c8372b5` `9c67e02` の 5 本）。#42 の塊 E（`misc3.c:950-1159`、持ち物の芯）と重なる範囲で、**先に窓口を通す**ことで塊 E を動かすときの未解決シンボルが落ちる。跨ぎ 1 件。偵察の 52 件との差 11 件は `object_ident` など窓口の対象外 | 428 | GREEN |
| 2026-09-14 | #18-5B 群 3 | **変更**：`scrolls.c` 46 件 ＋ `desc.c` 13 件を窓口越しに（`e5595ae` `bb5597b` `aac2842` `11038d3` の 4 本）。装備の定数添字（`INVEN_*`）が主。`desc.c` は偵察の 21 件ではなく **19 件**で、うち `object_ident` の 6 件は窓口の対象外なので置きかえは 13 件。この群で `item_ident_test` のリンクに `src/inventory.c` が加わった。跨ぎ 1 件 | 428 | GREEN |
| 2026-09-14 | #18-5B 群 4 | **変更**：`moria4.c` 24 件 ＋ `moria2.c` 9 件 ＋ `moria3.c` 6 件を窓口越しに（`fbb5392..154daed` の 5 本）。**跨ぎ 0 件**。`moria4.c` の 24 件は偵察が予想した `inven_command` ではなく `tunnel` / `facts` / `throw_object` / `py_bash` に散在していた | 428 | GREEN |
| 2026-09-14 | #18-5B 群 5 | **変更**：`save.c` `dungeon.c` `files.c` `death.c` `main.c` の 46 件を窓口越しに（`0dbaf85..5a3affc` の 10 本。1 コミット 1 ファイルを基本にした）。**跨ぎ 10 件がここに寄った**（全域ループと直列化）。ただし**偵察の「`save.c` の直列化は跨ぎ」は誤り** —— 持ち物と装備で 2 本の別ループだったので分けられ、`save.c` の跨ぎは 0。1 件だけ字面の置きかえにならなかった：`rd_short((uint16_t *)&inven_ctr)` は窓口に設定関数しか無いので、局所の `uint16_t` に読んでから `inventory_set_count((int16_t)tmp)` に渡す形にした（`msg_history_set_newest_slot()` と同じ先例）。**0..65535 の全域でビット直書きとキャストの一致を独立に確認（mismatch=0）。** 型を越えたアドレス操作（strict aliasing 違反）が消え、セーブ形式は不変。この群で `save_bool_test` と `store_save_test` のリンクに `src/inventory.c` が加わった | 428 | GREEN |
| 2026-09-14 | #18-5B 群 6 | **変更**：残り 11 ファイル ＋ `game_state.c` の 36 件を窓口越しに（`56bef1d..646a52c` の 12 本）。**跨ぎ 10 件**。`creature.c` が窓口を呼ぶので `movement_rate_test` のリンクが 2 → 3 単位になったが、代役に足したのは `inven_weight` と `equip_ctr` の 2 行だけで `externs.h` は引いていない。最後の 1 件（`646a52c`）は割りあての一覧に無かった `game_state.c` の配列写しで、**跨ぎの窓口で受けた**（配列の先頭を控える形なので添字空間の全域を指す）。バグ候補 **B17**（呪いを解く 3 通りの枠）はこの群と群 5 で装備の全域ループを触るときに気づいた | 428 | GREEN |
| 2026-09-14 | #18-5C | **変更**：実体 4 個を `treasure.c` から `src/inventory.c` へ移して `static` にし、`externs.h` の 4 行を削除（`e96f504`）→ 足場 2 つを片づけて `inventory_test` を 3 → 2 単位・**代役 0** に（`b028c17`）→ `scripts/globals.py` の `GROUPS` から 4 個を外す（`31c29d7`）。**96 → 92 個・参照 2886 → 2527。** `treasure.c` は 564 → **565 行**（定義 4 行を消し、行き先を指すコメント 4 行を置いた）ので、**`treasure.c` を引く 7 実行形式のリンク行は 1 つも変わらずに済んだ**。`e96f504` では `tests/` に残っていた直接参照 41 件（`inven_stack_test.c` 22・`item_ident_test.c` 6・`misc3_stubs.c` 3・`fixture.c` 2、`creature_stubs.c` の重複定義 4）も窓口越しにした。**意図的な壊しを 3 通り確認**（`inventory_at()` を 1 ずらす → 7 件、`equipment_first_slot()` を 22 → 23 → 3 件、`inventory_set_count()` を空実装 → 16 件。3 通りとも復元してグリーン復帰を確認）。**実動作も突きあわせた** —— 変更前の実行形式を別に作り、同じ入力列で町まで降りて `i` を押し、`Rations of Food` / `Wooden Torch` / `Dagger` / `Cloak` と `"You are carrying"` が一致（差は能力値のロール＝乱数の種のみ） | 428 | GREEN |
| 2026-09-14 | — | 誤ったコメントを修正（`c25dce4`）。`prayer.c:102` の「装備しているものだけ旗を消す」（実際は全枠を走り `tval` で絞るので袋の中の装備品も解ける）と、`remove_curse()`（`spells.c:2121`）の「持ち物の呪いを解く」（実際は装備の 22..`INVEN_OUTER` だけで持ち物には触らない）の 2 件。**コードは正しくコメントだけが誤り**なのでふるまいは変わらない。B10/B11 と同じ扱いで、コード変更とは別コミットにした（範囲の食いちがい自体はバグ候補 **B17** として記録し、直していない） | 428 | GREEN |
| 2026-09-14 | — | **#18-5 の報告の数値誤りが 2 件。**（1）`11038d3`（#18-5B15）の本文は `identify()` の呼びだしを「4 箇所・`desc.c` 内にも」と書いたが、実際は **3 箇所**（`spells.c:279` / `item_ident.c:38` / `store2.c:954`）で `desc.c` 内には無い（跨ぎ 1 件・置きかえ 1 件という結論は正しい）。（2）4 群目の報告で `equipment_at` の件数を 134 と書いたが、正しくは **119** —— `grep -oF` が `inventory_and_equipment_at(` の中の `equipment_at(` を重複して数えていた。**窓口の件数を数えるときは跨ぎの窓口が部分文字列になることに注意する。** どちらも結論は変わらないので履歴は書きかえず、正しい値をこの表に残す（2026-09-11 の #18-2 と同じ扱い） | 428 | GREEN |
| 2026-09-14 | #18-5 docs | **記録のみ**：`GLOBALS_INVENTORY.md`（所見 13・見立ての表・合計 92 個）、`REFACTORING_PLAN.md`（この #18-5 の節、#18 の行、バグ候補 **B17**、この作業ログ）、`HANDOVER.md`（第 0・4・5・6・7 節。次の作業は**セーブ／スコア／進行メタ 14 個**）を更新。`scripts/globals.py` は `31c29d7` で済んでいるので変更なし。コードは 1 行も触っていない | 428 | GREEN |
| 2026-09-14 | #18-5 マージ | `refactor/18-5-inventory`（48 コミット）を `develop` へ `--no-ff` でマージ（`c0590a1`）。マージ後に `make clean && make`（警告 0 件）、`make -f makefile.test`（428 件・failed=0）、`python3 scripts/globals.py --check`（92 個）、起動確認を実施。**`externs.h` の global は 96 → 92 個、参照は 2886 → 2527 件。** #18 は 9 区分のうち 5 区分が片づき（オプション 11・メッセージ 4・パネル 10・店 2 が区分ごと、持ち物・アイテムは 6 個のうち 4 個）、次は**セーブ／スコア／進行メタ 14 個** | 428 | GREEN |
| 2026-09-14 | — | **記録のみ**：引きつぎ時の点検で**文書の不備 2 件**を直した。（1）`HANDOVER.md` 第 6 節の「#18-5 で二面性の 2 例目が出た」の段落が**丸ごと 2 回**書かれていた（10 行。1 文字も違わない）ので 1 回にした。（2）**`misc3.c` の残る塊は 9 ではなく 10**。「11 責務 − 完了 2」で数えていたが、その 11 は塊 C と C' を「能力値」1 責務として数えた数で、#40 が出したのは**補正表（C）だけ**、値を書きかえる側（C'・115 行）は残っている。**責務で数えるか塊で数えるかが混ざって 1 ずれていた。** #42 の行の内訳 10 個の合計 1874 行が `misc3.c` 1906 行と合うことで裏をとった。直したのは `HANDOVER.md` 1 箇所と `REFACTORING_PLAN.md` 4 箇所（台帳・精査・#38 の節・棚上げ #33）。**作業ログの過去の行（2026-09-13 の 2 件）は書きかえていない**——履歴は残して正しい値をこの行に置く運用（2026-09-11 の #18-2、2026-09-14 の #18-5 と同じ扱い）。あわせて着手前の数字を測りなおし、**警告 0・テスト 428 件 18 実行形式・global 92 個 2527 参照・`misc3.c` 1906 行・起動 OK** が記録どおりであることを確認（今回はずれ 0 件）。コードは 1 行も触っていない | 428 | GREEN |
| 2026-09-15 | #19A | **保護**：3 つの窓口と 86 件のテストを置いた（`a8754d6` `215e38f` `09a1688`）。`src/progress.c` 78 行・`progress.h` 54 行（`turn` と種 2 つと wizard 2 つ、テスト 27 件）、`src/score_death.c` 71 行・`score_death.h` 56 行（`death` `died_from` `birth_date` `noscore`、27 件）、`src/save_state.c` 83 行・`save_state.h` 75 行（`savefile` と旗 3 つ ＋ **述語 3 つ**、32 件）。**呼びだし側は 1 箇所も変えず、実体も `variable.c` に残したまま**で窓口が `extern` 越しに読み書きする＝ふるまい不変（#18-5A と同じ二段構え）。区分を 3 本に割ったのは、**`src/` 全体で 3 群を混ぜる式が `signals.c:199` の 1 つだけ**だと実測できたから。跨ぐ 2 つの問いは `save_state.c` に置いて名前を付けた（矢印は一方向）。述語 3 つは `if` 文の中の字面だったので #7 / #18-1 と同じ「テスト側に写す」方式で、旗 2 つは 4 通り・3 つは 8 通りを総当たり。`turn` の境目（`>= 0` と `> 0` が `turn == 0` で分かれる）も固定した。足場は `tests/*_fixture.c` 3 本 88 行 | **428 → 455 → 482 → 514** | GREEN |
| 2026-09-15 | #19B1 | **変更**：進行 5 個の呼びだし側 24 箇所を窓口越しに（`c95cc03`、`save.c` 以外の 14 ファイル）。ミューテーション 29/5/3 件レッド。`wizard` と `to_be_wizard` の使い分け（前者は入ったあと、後者はコマンド行で頼まれた状態）が呼びだし側で目に見えるようになった | 514 | GREEN |
| 2026-09-15 | #19B2 | **変更**：結末 4 個の呼びだし側 33 箇所を窓口越しに（`ae7168b`）。ミューテーション 9/8/10/6 件レッド。`died_from` はアドレスを返す窓口（`strcpy` の受け先になるので写しを返すと届かない。`inventory_at()` と同じ形） | 514 | GREEN |
| 2026-09-15 | #19B3 | **変更**：セーブの旗 4 個の呼びだし側を窓口越しに（`dec8fd4`。直接 17 箇所 ＋ **述語に畳んだ 6 箇所**）。ミューテーション 5/6/6/4 件レッド。`signals.c` / `death.c` / `io.c` に同じ字面で 3 回あった `character_generated && !character_saved` が 1 つの名前になった | 514 | GREEN |
| 2026-09-15 | #19B4 | **変更**：`save.c` の 55 箇所を窓口越しに（`705dd60`）。この区分でいちばん重い 1 本なので独立コミットにした。**別名 6 件の `rd_*` を局所変数経由にし、型を越えたポインタのキャストが 4 件消えた**（`&turn` `&noscore` `&birth_date`、`bool` に 2 バイト書いていた `panic_save`）。**セーブ形式は 1 バイトも変えていない。** B18（`save.c:914` の括弧のずれ）は字面のまま移した。**単体テストのミューテーション 4 件が全部生き残った**ので、実物のセーブファイルで両方向の読みかえを確認（歯があることは `wr_short(noscore)` を落とした mutant が弾かれることで確かめた）。この穴は P3 **#49** に記録 | 514 | GREEN |
| 2026-09-15 | #19C | **変更**：実体 13 個を `variable.c` から 3 module へ移して `static` にし、`externs.h` の 13 行を削除（`e5d70e7`）→ 用の済んだ足場 3 本 88 行を消す（`bc8f814`）→ `globals.py` の `GROUPS` から 13 個を外す（`e286501`）。**92 → 79 個・参照 2527 → 2253。** `src/` と `tests/` の直接参照を**両方 0 にしてから**移したので**リンカのエラーが 1 件も出なかった**（#18-5C1 では 41 件出た）。**足場は二重定義にもならずに残る**ことを実測で確かめた（足場の `turn` を `-1` → `12345` にしても failed=0 ＝ もう本体に届いていない。本体側の同じ壊しは 1 件レッド）。意図的な壊しは C1 で 4 通り・C2 で 2 通り確認。`progress_test` と `score_death_test` は **2 単位・代役 0**（`inventory_test` と同じ最小構成）、`save_state_test` は跨ぐ述語の代金として 4 単位 | 514 | GREEN |
| 2026-09-15 | #19 docs | **記録のみ**：`GLOBALS_INVENTORY.md`（**所見 14**・見立ての表・合計 79 個）、`REFACTORING_PLAN.md`（この #19 の節、#18 の行、P3 **#49**、この作業ログ）、`HANDOVER.md`（第 0・1・5・6・7 節）を更新。`scripts/globals.py` は `e286501` で済んでいるので変更なし。コードは 1 行も触っていない。**`highscore_fp` 1 個は残している** —— 局所変数化は `init_scorefile()`（`files.c:28`、setuid のうちに開いておく）と `game_state.c:80`（状態の写しとり）に触るので、ふるまいの判断がユーザーの領分 | 514 | GREEN |
| 2026-09-16 | #19-5-1 | **変更**：区分の 14 個目 `highscore_fp` を `death.c` の 2 関数の局所変数に（`c50804e`）。ユーザーが選んだのは 3 通りのうち**ふるまいを変えない 1 番**。38 箇所を `FILE *score_fp` に（**名前を変えたのは `-Wshadow` が global と同名の局所変数を拾うから**）。参照 43 → 7・書き 3 → 1 だが **`externs.h` には残るので 79 個は動かない**。かわりに `init_scorefile()` の handle が「開いたまま誰も読まず誰も閉じない」ことが字面に出たので、バグ候補 **B19** として記録（setuid の意図は最初から果たされていなかった）。**`death.c` は単体テストが 1 件も届かない**ので、`git worktree` で変更前を建てて実物の `scores.dat` を両方向に読みかえた（76 → 149 バイト、`-s` の表示が一致）。pty の罠 2 つ（キーは 1 文字ずつ／終了は `^K` で `Q` ではない）は `HANDOVER.md` 第 7 節に写した | 514 | GREEN |
| 2026-09-16 | #19-5-1 docs | **記録のみ**：`REFACTORING_PLAN.md`（「#19-5-1」の節、#19 の節の残り 1 個の扱い、#18 の行、バグ候補 **B19**、この作業ログ）、`GLOBALS_INVENTORY.md`（所見 14 に結果を追記・見立ての表・合計の参照 2253 → 2215）、`HANDOVER.md`（第 0・1・3・5・7 節。**第 5 節の 5-1 は「問い合わせ待ち」から「済んだ 1 個」の記録に書きかえ、次の作業はプレイヤー状態 13 個**）、`scripts/globals.py`（区分のコメントを現状に）を更新。コードは 1 行も触っていない | 514 | GREEN |
| 2026-09-16 | — | **マージ**：`refactor/19-savemeta`（14 コミット）を `develop` へ（`8c2af3f`、`--no-ff`）。マージ後に確認 5 つ —— `make clean && make` 警告 **0**、テスト **514 件 failed=0**（**21 実行形式**）、`globals.py --check` **79 個**、起動確認 **1**（種族選択画面まで）。**ユーザーの指示でブランチも削除**したので、ローカルは `develop` 1 本だけになった（`origin` への push はしていない。`origin/develop` は `38ec3c3` のまま）。これで `externs.h` は **9 区分のうち 6 つ完了**、次は**プレイヤー状態 13 個** | 514 | GREEN |
| 2026-09-23 | #18-6A | **保護**：居場所 2 個の窓口 `src/player_pos.c` 44 行・`player_pos.h` 44 行とテスト 17 件を置いた（`e5bfc5e`）。窓口は 4 つ（読み 2・`player_place`・`player_pos_forget`）。**呼びだし側は 1 箇所も変えず、実体も `player.c` に残したまま**で module が `extern` 2 行を自分で書く（`stats.c` が `py` に対してやっている形。1 行のために ncurses まで引く `externs.h` は取りこまない）。テストは置き場としてのふるまい（そろって入る・片方が他方を壊さない）、**範囲を検査しないこと**（ダンジョンの外・負・`int16_t` の端。窓口が窓を広げないことを 32768 → −32768 で固定）、`-1` の置きなおしを押さえた。意図的な壊し 2 通り（行と列の入れかえ 13 件レッド、`-1` を `0` に 2 件レッド）。ビルド定義は 3 本とも更新。足場 `tests/player_pos_fixture.c` 25 行 | **514 → 531** | GREEN |
| 2026-09-23 | #18-6B1 | **変更**：別名のある 2 ファイルを先に片づけた（`201ce54`）。`generate.c` の `new_spot(&char_row, &char_col)` 2 箇所を局所変数経由に、`-1` の組を `player_pos_forget()` に。`save.c` は読み 2 件を `uint16_t` の局所変数経由にして、**ポインタ型を偽るキャスト 2 件を消した**（#19B4 と同じ形）。**セーブ形式は 1 バイトも変えていない。** P3 **#49** のとおり単体テストはセーブの並びを守らないので（意図的な壊しを入れても 531 件グリーンのまま）、実物で確かめた —— 変更前の binary が書いた 4835 バイトを両方の binary で読み、画面が 1 バイトも違わないこと／変更後が再保存したものも同じく一致すること／その壊しは画面比較なら検出できること。**セーブファイル同士のバイト比較は使えない**（4 バイト目が XOR の種で、中身が同じでも全バイトが違って見える）。`save_bool_test` と `store_save_test` に `src/player_pos.c` を足した | 531 | GREEN |
| 2026-09-23 | #18-6B2 | **変更**：移動と視界（`moria3.c`・`moria4.c`）の読み 36 箇所・書き 2 組を窓口越しに（`ea84400`）。書きは `move_char()` の 2 組（歩いた先／落石で 1 歩もどる）。**この 2 本には単体テストが 1 件も届かない**ので実物で確かめた —— 変更前の binary が書いたセーブを両方で読み、同じキー列（南 2・東 2・北・西 2・南・東・探索 2）を流した画面 **2858 バイト**が一致。読みこみだけの画面と違うので、キー入力が効いていることも確認している | 531 | GREEN |
| 2026-09-23 | #18-6B3 | **変更**：呪文と効果（`spells.c`・`magic.c`）の読み 37 箇所・書き 1 組を窓口越しに（`5cc2f48`）。読みは「プレイヤーを中心にした 3x3」「見えるか（`los`）」「効果の発生点」の 3 通りだけ。**実物の突きあわせが使えない経路を見つけた** —— ウィザードモードの `^T`（テレポート）は**同じ binary で 2 回流しても画面が違う**（3083 / 3120 バイト。乱数の種を時計から取る）。**実物で比べるなら乱数を引かない経路を選ぶ**というのがこの 3 ステップで分かったこと（歩く・探索する・セーブを読みこむ経路は決定的）。この 1 組の書きは diff の目視と型で守っている | 531 | GREEN |
| 2026-09-23 | #18-6B4 | **変更**：主ループとモンスター（`dungeon.c`・`creature.c`）の読み 32 箇所を窓口越しに（`bd6d354`）。**書きは 1 つも無い**（どちらもプレイヤーがどこにいるかを聞くだけ）。`movement_rate_test` は `src/creature.c` を取りこむのでリンクが 5 → 6 単位になった（代役を書かず本物の窓口を通す形は #18-5B31 と同じ理由）。歩く手順で 2858 バイトの画面が一致 | 531 | GREEN |
| 2026-09-23 | #18-6B5 | **変更**：残る 10 ファイルの 54 箇所（読み 52・書き 1 組）を窓口越しに（`bc87732`）。これで **`src/` の呼びだし側の直接参照は 0**。内訳は `misc1.c` 4・`misc3.c` 10・`misc4.c` 6・`moria1.c` 3・`moria2.c` 6・`prayer.c` 4・`scrolls.c` 8・`staffs.c` 6・`wands.c` 3・`wizard.c` 3。`src/misc3.c` を取りこむ 4 本（`inven_stack_test` `put_misc3_test` `haggle_comment_test` `objdes_test`）に `src/player_pos.c` を足した。歩く手順で 2858 バイトの画面が一致 | 531 | GREEN |
| 2026-09-23 | #18-6C | **変更**：実体 2 個を `player.c` から `src/player_pos.c` へ移して `static` にし、`externs.h` の 2 行を削除（`9556d24`）→ 用の済んだ足場を消す（`46847d2`、`player_pos_fixture.c` 25 行と `creature_stubs.c` の代役 2 個）→ `globals.py` の `GROUPS` から 2 個を外す（`98bebfe`）。**79 → 77 個・参照 2215 → 1913。** 区分「プレイヤー状態」は 13 個・960・別名 106 → **11 個・658・別名 100**。#19C と同じく **`src/` と `tests/` の直接参照を両方 0 にしてから**移したのでリンカのエラーは 1 件も出ていない。**足場が死んだことを実測**（足場の `char_row` を 12345 にしても 531 件グリーン、本体の `static` を同じく壊すと 1 件レッド）。`player_pos_test` は **2 単位・代役 0**。初期化子は `player.c` にも無かったので付けていない（位置は読む前に必ず書かれる） | 531 | GREEN |
| 2026-09-23 | — | **#18-6 の報告の数値誤り 1 件。** 着手前の偵察で「読みは 302 箇所のうち 258 件」と書き、その数字が A〜C のコミットメッセージと `src/player_pos.h` のコメントに入った。**正しくは 284 件**（302 = 読み 284 ＋ 定義 2 ＋ 書き 10 ＋ 別名 6）。窓口の呼びだしを数えて食いちがいに気づいた。`src/player_pos.h` のコメントだけ直し（コメントのみ・ふるまい不変）、**コミットメッセージの履歴は書きかえていない**（2026-09-11 の #18-2、2026-09-14 の #18-5 と同じ運用）。結論（「読みがほぼ全部」「窓口 4 つで足りる」）は変わらない | 531 | GREEN |
| 2026-09-23 | #18-6 docs | **記録のみ**：`GLOBALS_INVENTORY.md`（**所見 15**・見立ての表・合計 77 個 1913 参照）、`REFACTORING_PLAN.md`（この #18-6 の節、#18 の行、この作業ログ）、`HANDOVER.md`（第 0・4・5・7 節。**次の作業はプレイヤー状態の残り 11 個**で、行き先の判断がユーザーの領分）を更新。`scripts/globals.py` は `98bebfe` で済んでいるので変更なし | 531 | GREEN |
| 2026-09-24 | #18-6 マージ | **マージ**：`refactor/18-6-player`（11 コミット）を `develop` へ（`c3395e5`、`--no-ff`）。マージ後に確認 5 つ —— `make clean && make` 警告 **0**、テスト **531 件 failed=0**（**22 実行形式**）、`globals.py --check` **77 個**、起動確認 **1**（種族選択画面まで）。**ユーザーの指示でブランチも削除**したのでローカルは `develop` 1 本（`origin` への push はしていない）。これで #18 は **9 区分のうち 6 つ完了 ＋ 7 区分目に着手**、次は**プレイヤー状態の残り 11 個**（ユーザーの指示で `py` 以外の **10 個をまとめて** 1 ブランチで進める） | 531 | GREEN |
| 2026-09-24 | #18-7-1 | **変更**：勝ちと最高得点 2 個（`total_winner`・`max_score`）を既にあった `src/score_death.c` へ（`f011359` `af751f4` `fa46530` `3dbf6d0` `75c9e3c` `481584d`）。新しい module は作らず、窓口 5 つを足しただけ（`player_has_won` / `set_player_has_won` / `best_score` / `set_best_score` ほか）。**77 → 75 個。** 足場と代役を消して `score_death_test` は代役 0 に | **531 → 545** | GREEN |
| 2026-09-24 | #18-7-2 | **変更**：レベルごとの HP 表 1 個（`player_hp`）を新設 `src/hp_table.c` へ（`0afa088` `5df35fa` `90e5435` `53015b6` `170e6a8` `d1add7c` `9db1f77`）。**1 起点で読む表**（古い添字は `player_hp[lev - 1]`）で、段を 1 つずらしても値が「それらしく」出てしまう。**テストが 1 件も届いていなかった `calc_hitpoints` に 13 件足してから** B に入った（`#18-7-2A2`。本体は 1 行も変えていない）。**75 → 74 個** | **545 → 575** | GREEN |
| 2026-09-24 | #18-7-3 | **変更**：明かりの有無 1 個（`player_light`）を新設 `src/player_light.c` へ（`7845da5` `64eff05` `e931e0b` `24434db` `32e072f` `d75685b`）。**導出（灯りの欄の燃料を見る）に替えなかった** —— `dungeon()` が明るい／暗いの**変わり目**でモンスターを出しなおすので、覚えた答えを返す置き場のままにした。実物で確かめた（松明を装備したまま保存したセーブを読ませる走行を別に作った）。**74 → 73 個** | **575 → 588** | GREEN |
| 2026-09-24 | #18-7-4 | **変更**：重さに負けているか 2 個（`weapon_heavy`・`pack_heavy`）を新設 `src/burden.c` へ（`b42d446` `2a5c01a` `67ca862` `1b28d27` `46cc2ba` `36d3dd7` `ef48c6d`）。荷の側は旗ではなく**段数**で、速度は `change_speed(新しい段数 − 覚えた段数)` という**差**で動く。**テストが 1 件も届いていなかった `check_strength` に 24 件足してから** B に入った（`#18-7-4A2`。本体は 1 行も変えず、代役の `change_speed`・`calc_bonuses` に記録を足して観測した）。変異 4 通りで検出を実測。**重い荷・重すぎる武器は実物では作れず**（店で 2000 単位ぶんの鉄杭が要る）、実物は「ふつうの道が変更前と同じ」ことの確認に使った（2981 バイト一致）＋ 窓口が生きていることは実物の変異で見た。**73 → 71 個** | **588 → 612** | GREEN |
| 2026-09-24 | #18-7 マージ | **マージ**：`refactor/18-7-player-rest`（26 コミット）を `develop` へ（`85e6b51`、`--no-ff`）。マージ後に確認 5 つ —— `make clean && make` 警告 **0**、テスト **612 件 failed=0**（**27 実行形式**）、`globals.py --check` **71 個**、起動確認 **1**（種族選択画面まで）。これで #18 は **9 区分のうち 6 つ完了 ＋ 7 区分目の 8 個**、次は**呪文 4 個**（`spell_learned` `spell_worked` `spell_forgotten` `spell_order`）。`py` は最後 | 612 | GREEN |
| 2026-09-24 | #18-8-A1 | **保護**：呪文 3 個を書きかえるのは `calc_spells()`（`misc3.c:1220`）1 か所だけなのに、テストが 1 件も届いていなかった。`tests/calc_spells_test.c` を 17 件（`502a348`。**`src/` は 1 行も変えていない**）。押さえたのは 5 つ —— レベルを超えた呪文は上の位から下りて忘れる／覚えていられる数は段 × レベル数（段 0 で 0 個・段 4 以上で 1.5 倍）／思いだすのは覚えた順・忘れるのはその逆／`spell_order` の 99 は「まだ」の印だが**読み飛ばしではなく走査の枠を 1 つ食う**／職業で言いかたが変わる。変異 3 通りでレッド。3 つめ（99 を `1L << (j & 31)` に丸める）は**最初に生き残った** —— 99 & 31 == 3 だが、丸めた添字で段の表を引くと表の外を拾い、レベル 7 未満では「まだ届かない」側に落ちて差が出ない。レベル 7 の 1 件を足して殺した | **612 → 629** | GREEN |
| 2026-09-24 | #18-8-A2 | **保護**：もう 1 か所の書きかえ元 `gain_spells()`（`misc3.c:1374`）に `tests/gain_spells_test.c` を 13 件（`0b8555d`。ここも `src/` は無変更）。覚えた印と覚えた順が**必ず対で動く**こと（積む位置は「99 でない要素の次」で、覚えた数を数える変数は無い）、MAGE 系は本が要り足りない分を `new_spells` に残すこと、PRIEST 系は本が要らず選べもしないこと、学べる数 0 なら手番を使わないことを固定。MAGE 側はキー入力を待つので**代役に窓口を 1 つ足した**（`fixture_set_get_com_keys`）—— 既定では `get_com` が 0 を返し、**繰りかえしに 1 度も入らず学ぶ経路に触れていなかった**。変異 4 通りでレッド。最後（すでに覚えた呪文を候補から外さない）は**最初に生き残った**（覚えていた呪文が本に載っていない条件だった）ので、載っている 1 件を足して殺した | **629 → 642** | GREEN |
| 2026-09-24 | #18-8-A3 | **保護**：呪文 4 個（参照 43）の窓口を 1 本の module `src/spells_known.c` に置き、`tests/spells_known_test.c` を 43 件（`90f5d07`）。実体はまだ `player.c`、呼びだし側は 1 つも変えていない。**4 つで 1 つの問いに答えている**から 1 本にした ——「何を知っていて、どういう順で知ったか」。覚えた印と忘れた印（`calc_spells`）、覚えた印と覚えた順（`gain_spells`）はいつも対で動くので、対を窓口 1 つにすれば置き場の側で保証できる。**ビット演算（`1L << spell`）も内側に入れた** —— 3 か所が自分でシフトを書き、2 か所が「99 でシフトすると結果が決まらない」ことを覚えていなければならなかった。古い形と**意図して違えたのは 2 つ**（履歴が満杯なら順への積みを落とす／`spell_learned_nth(負)` は `SPELL_NONE`）。変異 8 通りでレッド | **642 → 685** | GREEN |
| 2026-09-24 | #18-8-B1 | **変更**：`misc3.c` の外にある 13 行を窓口越しに（`b6be3b4`）。`magic.c` と `prayer.c` が**同じシフトを 2 度書いていた**のが消え（`spell_has_worked` / `spell_mark_worked`）、`main.c` の 3 行は `spell_order_forget_all()` の 1 行に（**99 という数が `main.c` から消えた**）。`moria3.c` は `spells_learned_among(flags)`。`save.c` はファイルの形が固定なので生の窓口を通し、読みこみは局所変数に受けてから渡す（`progress` の種と同じ形）。`makefile.test` は `save.c` をリンクする 2 本に `src/spells_known.c` を足した（実体はまだ本物の `player.c` なので足場は要らない） | 685 | GREEN |
| 2026-09-24 | #18-8-B2 | **変更**：残る `misc3.c` の 25 行とテスト 2 本を窓口越しに（`73bd5b0`）。**消えた重複が 3 つ** —— `calc_spells` の「99 なら遮蔽を 0 に」の 6 行が 2 か所（走査の内側だったので遮蔽を持ちまわる必要も消え、`for (int i = 31; i >= 0; i--)` になった）、覚えている数の 32 回の走査は `learned_spell_count()` の 1 行、`gain_spells` の覚えた順の末尾を探す 6 行（`last_known`）はまるごと。**覚えた印を立てる行と順に積む行の対（2 か所）がそれぞれ 1 行になり、対が離れうる形が残っていない**——これが今回のいちばんの変化。テスト側の「覚えている呪文を 1 つ足す」は `spell_learn()` そのもの、「忘れている呪文を 1 つ足す」は `spell_learn()` + `spell_forget()`、つまり**ゲームでの唯一の道すじ**になった。`makefile.test` は `misc3.c` をリンクする 8 本に足した | 685 | GREEN |
| 2026-09-24 | #18-8-C | **変更**：実体 4 行を `player.c:307-310` から `src/spells_known.c` へ移して `static` に、`externs.h:84-87` の宣言 4 行を消した（`dd50206`）。足場（`tests/spells_known_fixture.c`）も消して**対象 1 本＋テスト 1 本の 2 単位**に。覚えた順の走りだしは 0 埋めのまま残した（開始時に `main.c` が `SPELL_NONE` で埋め、続きからはセーブファイルが上書きするので**初期値そのものは誰も読まない**）。**検証でつまずいた 1 点** —— 初期値を `0xFFFFFFFF` に変えても 685 件すべてグリーンだった（どのテストも先に空にし、ゲームも起動時に上書きするので**誰も初期値を読まない**）。**幅を `uint32_t` → `uint16_t` に狭める**変異に替えて `spells_known_test` と `calc_spells_test` の 5 件レッドを確認し、`nm` で 4 つとも局所記号（`b`）になっていることも見た。導出する形にはしていない（何を覚えたかは履歴で、記録がなければ復元できない）。**71 → 67 個** | 685 | GREEN |
