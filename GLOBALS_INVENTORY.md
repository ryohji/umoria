# グローバル変数の棚おろし

臭い #18「データの散在」に手をつける前に、散らばっているものの全体像を
数字で押さえておくための資料。`src/externs.h` が宣言する 112 個を、
「誰のものか」で 11 区分に分けて数えあげた。

計測日: 2026-09-09（`develop`）

## 読みかた

再現はコマンド一本。手で数えると見落とすし、直すたびに数えなおすので
道具にしてある（`scripts/warnings.sh` と同じ位置づけ）。

```sh
python3 scripts/globals.py          # 区分ごとの集計（下の表）
python3 scripts/globals.py --full   # 112 個すべての一覧（付録）
python3 scripts/globals.py --check  # 分類の網羅性だけ確認（CI 向け）
```

数えているもの:

| 列 | 意味 |
| --- | --- |
| 参照 | `src/*.c` に名前が現れる回数（定義そのものを含む） |
| 書き | 代入・`++`・`--`・`strcpy` 系の第 1 引数に渡る形 |
| 別名 | `&x` や、配列名を裸で関数に渡す形の回数 |

「書き」からは 2 種類を除いてある。定義の置き場（`variable.c` `player.c`
`tables.c` `treasure.c` `monsters.c`）の初期値つき定義と、`game_state.c` の
`state->x = x`（状態の写しとりで `x` 自身は書かない）。どちらも数えると
「どこから状態が変えられるか」が見えなくなる。

**「書き」は下まわる見積り**であって正確な数ではない。局所ポインタ経由の
書きこみが数えられないため。たとえば `store` は

```c
store_type *s_ptr = &store[store_num];   // ← ここは「別名」として数える
s_ptr->store_ctr++;                      // ← この書きこみは数えられない
```

の形をとるので、`store` の「書き」は 0 だが実際には書かれている。
だから別名の列を併記している。**別名がとられている変数は「書き」を
信用してはいけない**という合図として読む。カプセル化で厄介なのもここで、
参照を機械的に置きかえるだけでは足りない箇所にあたる。

## 区分ごとの集計

| 区分 | 個数 | 参照 | 書き | 別名 | 最多参照 |
| --- | ---: | ---: | ---: | ---: | --- |
| 定数表（読みとり専用データ） | 20 | 190 | 0 | 19 | `class`(48) |
| 起動時に一度書きかえる表 | 7 | 61 | 13 | 2 | `object_list`(22) |
| オプション（ユーザー設定） | 11 | 120 | 26 | 13 | `rogue_like_commands`(19) |
| プレイヤー状態 | 13 | 957 | 167 | 106 | `py`(549) |
| ダンジョンとその中身 | 12 | 652 | 105 | 248 | `cave`(257) |
| 持ち物・アイテム | 6 | 379 | 32 | 113 | `inventory`(251) |
| 店 | 2 | 88 | 11 | 23 | `store`(68) |
| 画面の見えている範囲（パネル） | 10 | 99 | 22 | 2 | `panel_row_min`(13) |
| メッセージ表示 | 4 | 41 | 10 | 2 | `msg_flag`(13) |
| コマンド入力・実行中のフラグ | 13 | 241 | 123 | 2 | `free_turn_flag`(80) |
| セーブ／スコア／進行メタ | 14 | 319 | 54 | 8 | `turn`(54) |
| **合計** | **112** | **3147** | **563** | **538** | |

横断的な見かた:

- 書きこみも別名もない（実質定数）: **16 個**
- 3 ファイル以上から書かれる: **24 個**
  （`py` `free_turn_flag` `cave` `died_from` `t_list` `death`
  `command_count` `dun_level` …）
- 別名がとられる（数えもれあり）: **54 個**
  （`cave` `inventory` `py` `t_list` `m_list` `store` `magic_spell`
  `spell_learned` …）

## わかったこと

1. **数は 112 でも、重さは偏っている。**
   参照 3147 のうち `py`(549) `cave`(257) `inventory`(251) `char_row`/
   `char_col`(151 ずつ) `t_list`(113) の 6 個で 1472 ＝ 47% を占める。
   「112 個を片づける」ではなく「重いものを個別に、軽いものをまとめて」に
   なる。

2. **`py` は最大だが、いちばん厄介ではない。**
   参照 549・別名 88 と最大だが、`py` はすでに 1 個の構造体で、書きこみは
   `py.flags.*` `py.misc.*` のようにメンバ単位。散在しているのは「`py` が
   グローバルであること」ではなく、それ以外の 99 個が `py` の外に置かれて
   いることのほう。`char_row`/`char_col`（各 151 参照）が `py.misc` に
   入っていないのがその典型。

3. **`cave` の別名 145 が最大の障壁。**
   `cave_type *c_ptr = &cave[y][x];` の形が全域に散っている。ここを
   アクセサに寄せるのは #18 の中でもっとも高くつく。逆に言えば、#18 を
   「全部やる」にすると事実上 `cave` の書きかえプロジェクトになる。

4. **オプション 11 個にはすでに登録表がある。**
   `misc2.c:856` の `options[]` が `{表示名, bool*}` の対を持ち、
   `set_options()` はこの表を回すだけ。散らばっているのは
   *この表の外*の 2 箇所だけ ——
   - `save.c:596-640` セーブ時にビットへ詰め、ロード時に戻す
   - `main.c:35,183` 起動時の既定値と `-o` 相当の上書き
   つまり 11 個 × 参照 120 に対して、書きこみは save/load と起動の
   3 箇所に閉じている。**カプセル化がいちばん安く済む区分。**

5. **実質定数の 16 個は #18 の対象ではない。**
   `copyright` `days` `player_title` `blows_table` … いずれも書きこみ 0・
   別名 0。`const` を付けて閉じるのは意味があるが（→ 別の臭い）、
   「散在」の問題ではない。

6. **「定数表に見えて起動時に書きかえる 7 個」は罠。**
   `colors` 系 6 個は `desc.c:34 magic_init()` が `randes_seed` を種に
   シャッフルする（未鑑定アイテムの見た目をゲームごとに変える仕掛け。
   `randes_seed` がセーブされるのは再現のため）。`object_list` は
   `main.c:308 price_adjust()` が `cost` を一括で割りもどす（`COST_ADJ`
   が 100 なので現状 `#if` で消えているが、変えると生きる）。
   **`const` 化しようとすると壊れる。**

7. **`free_turn_flag` は変数ではなく制御フローの成れの果て。**
   参照 80 に対し書きこみ 67、14 ファイルから書かれる。「この行動でターンを
   消費したか」を大域変数で伝えている。#18 の枠で直すものではなく、
   戻り値に変えるべきもの（→ #18 とは別立てにする）。

8. **`game_state.c` は真実の源ではない。**
   `game_state_init()` は 40 個ほどのグローバルを `state->` にコピーする
   だけ。しかも `game_state_init` / `g_game_state` の参照は `game_state.c`
   の外に 1 件もない（`src/` `tests/` を `grep` して 0 件）。つまり
   **現状は動いていない雛形**で、既存グローバルと並存させれば写した瞬間に
   乖離する（→ バグ候補 B3）。#18 で `GameState` を本体にするなら
   「新しく作る」ではなく「この雛形を実体にして写しを消す」作業になる。

9. **`view_observer.c` は #18 とほぼ無関係。**
   `externs.h` の 112 個のうち、この 129 行が参照するものは 0 個
   （`grep` が拾う `py` は `Copyright` の一部）。#18 の前提として
   「先にグローバルを整理しないと `view_observer.c` の変更が過大になる」と
   していたが、**その前提は成り立たない**。両者は切り離して進められる。

## 区分ごとの見立て

| 区分 | 見立て | 目安 |
| --- | --- | --- |
| 定数表 20 | `const` 化のみ。#18 の対象外 | 小・独立 |
| 起動時に書きかえる表 7 | 触らない。`const` 化は不可と注記だけ残す | 注記のみ |
| オプション 11 | `misc2.c:856` の表を正とし、save/load と起動を表経由に。**最初の一歩に向く** | 小 |
| メッセージ表示 4 | `msg_flag` `old_msg` `last_msg` `wait_for_more` を `msg_print` 側に閉じる | 小 |
| パネル 10 | 相互に決まる値（`panel_row_min` は `panel_row` から計算）。1 構造体＋再計算関数に | 小〜中 |
| 店 2 | 別名 23 で見積り不能。`store_type` へのアクセサが要る | 中 |
| 持ち物 6 | `inventory` 別名 105。`inven_ctr` `inven_weight` と不変条件で結ばれている | 中〜大 |
| セーブ／進行メタ 14 | `save.c` に集約する余地は大きいが `death` `turn` は全域から読まれる | 中 |
| プレイヤー状態 13 | `char_row`/`char_col` を `py` に寄せるのが最も効く一手 | 中 |
| コマンド／実行中フラグ 13 | `free_turn_flag` は戻り値化（別課題）。残りは呼び出しの引数へ | 大 |
| ダンジョン 12 | `cave` 別名 145。**最後にやる**。単独で 1 プロジェクト | 特大 |

## 付録: 112 個の一覧

`python3 scripts/globals.py --full` の出力（2026-09-09 時点のスナップ
ショット）。数字は上の表と同じ意味。

```
名前                       参照   参照f   書き   書きf   別名  区分
copyright                 1     1    0     0    0  定数表（読みとり専用データ）
days                      1     1    0     0    0  定数表（読みとり専用データ）
player_title              2     2    0     0    0  定数表（読みとり専用データ）
race                     41     7    0     0    2  定数表（読みとり専用データ）
background                4     2    0     0    1  定数表（読みとり専用データ）
player_exp                7     4    0     0    0  定数表（読みとり専用データ）
rgold_adj                 3     3    0     0    0  定数表（読みとり専用データ）
class                    48    14    0     0    5  定数表（読みとり専用データ）
class_level_adj          18     8    0     0    0  定数表（読みとり専用データ）
magic_spell              12     6    0     0    9  定数表（読みとり専用データ）
spell_names              20     4    0     0    0  定数表（読みとり専用データ）
player_init               2     2    0     0    0  定数表（読みとり専用データ）
owners                   12     3    0     0    2  定数表（読みとり専用データ）
store_choice              2     2    0     0    0  定数表（読みとり専用データ）
store_buy                 2     2    0     0    0  定数表（読みとり専用データ）
special_names             2     2    0     0    0  定数表（読みとり専用データ）
blank_monster             3     3    0     0    0  定数表（読みとり専用データ）
syllables                 2     2    0     0    0  定数表（読みとり専用データ）
blows_table               2     2    0     0    0  定数表（読みとり専用データ）
normal_table              6     2    0     0    0  定数表（読みとり専用データ）
object_list              22     9    1     1    2  起動時に一度書きかえる表
colors                    8     3    2     1    0  起動時に一度書きかえる表
mushrooms                 6     2    2     1    0  起動時に一度書きかえる表
woods                     6     2    2     1    0  起動時に一度書きかえる表
metals                    6     2    2     1    0  起動時に一度書きかえる表
rocks                     6     2    2     1    0  起動時に一度書きかえる表
amulets                   7     3    2     1    0  起動時に一度書きかえる表
rogue_like_commands      19     7    4     2    1  オプション（ユーザー設定）
find_cut                  9     5    2     1    1  オプション（ユーザー設定）
find_examine             10     5    2     1    1  オプション（ユーザー設定）
find_prself              14     7    2     1    1  オプション（ユーザー設定）
find_bound                8     5    2     1    1  オプション（ユーザー設定）
prompt_carry_flag         8     5    2     1    2  オプション（ユーザー設定）
show_weight_flag         12     5    2     1    1  オプション（ユーザー設定）
highlight_seams          11     7    2     1    2  オプション（ユーザー設定）
find_ignore_doors         8     5    2     1    1  オプション（ユーザー設定）
sound_beep_flag          10     5    3     1    1  オプション（ユーザー設定）
display_counts           11     5    3     1    1  オプション（ユーザー設定）
player_light             11     6    4     1    1  プレイヤー状態
weapon_heavy             12     6    5     3    0  プレイヤー状態
pack_heavy               11     4    3     2    0  プレイヤー状態
py                      549    31  126    18   88  プレイヤー状態
player_hp                10     4    3     1    0  プレイヤー状態
char_row                151    19    5     4    3  プレイヤー状態
char_col                151    19    5     4    3  プレイヤー状態
spell_learned            17     4    5     1    6  プレイヤー状態
spell_worked              8     5    2     2    1  プレイヤー状態
spell_forgotten           9     3    3     1    2  プレイヤー状態
spell_order               9     4    3     2    0  プレイヤー状態
total_winner             13     8    2     2    1  プレイヤー状態
max_score                 6     3    1     1    1  プレイヤー状態
hack_monptr              15     5   10     1    0  ダンジョンとその中身
dun_level                45    11   11     5    1  ダンジョンとその中身
cur_height               30     7    2     1    1  ダンジョンとその中身
cur_width                32     8    2     1    1  ダンジョンとその中身
cave                    257    16   47     9  145  ダンジョンとその中身
t_level                  16     3    3     1    0  ダンジョンとその中身
t_list                  113    17   13     7   52  ダンジョンとその中身
tcptr                    14     4    3     2    1  ダンジョンとその中身
m_list                   73    12    4     3   45  ダンジョンとその中身
m_level                  20     4    3     1    0  ダンジョンとその中身
mfptr                    28     9    3     3    1  ダンジョンとその中身
mon_tot_mult              9     5    4     3    1  ダンジョンとその中身
object_ident             11     5    4     1    0  持ち物・アイテム
inventory               251    28    8     3  105  持ち物・アイテム
sorted_objects            6     4    1     1    0  持ち物・アイテム
inven_ctr                65    18    4     2    1  持ち物・アイテム
inven_weight             22     7   12     5    1  持ち物・アイテム
equip_ctr                24     6    3     2    6  持ち物・アイテム
store                    68    18    0     0   22  店
last_store_inc           20     2   11     1    1  店
max_panel_rows           11     5    2     1    1  画面の見えている範囲（パネル）
max_panel_cols           11     5    2     1    1  画面の見えている範囲（パネル）
panel_row                11     4    4     3    0  画面の見えている範囲（パネル）
panel_col                10     4    4     3    0  画面の見えている範囲（パネル）
panel_row_min            13     4    2     2    0  画面の見えている範囲（パネル）
panel_row_max            11     4    2     2    0  画面の見えている範囲（パネル）
panel_col_min            13     4    2     2    0  画面の見えている範囲（パネル）
panel_col_max            11     4    2     2    0  画面の見えている範囲（パネル）
panel_col_prt             4     3    1     1    0  画面の見えている範囲（パネル）
panel_row_prt             4     3    1     1    0  画面の見えている範囲（パネル）
msg_flag                 13     5    5     3    1  メッセージ表示
old_msg                  10     5    2     1    0  メッセージ表示
last_msg                 12     5    1     1    1  メッセージ表示
wait_for_more             6     4    2     1    0  メッセージ表示
new_level_flag           16     6    9     4    0  コマンド入力・実行中のフラグ
teleport_flag             7     5    3     3    0  コマンド入力・実行中のフラグ
eof_flag                 11     6    2     1    0  コマンド入力・実行中のフラグ
find_flag                25     8    6     3    0  コマンド入力・実行中のフラグ
free_turn_flag           80    16   67    14    1  コマンド入力・実行中のフラグ
doing_inven              15     5    4     1    0  コマンド入力・実行中のフラグ
screen_change            10     5    4     3    0  コマンド入力・実行中のフラグ
command_count            47     9   20     5    0  コマンド入力・実行中のフラグ
default_dir               6     4    2     1    0  コマンド入力・実行中のフラグ
missile_ctr               9     4    2     1    1  コマンド入力・実行中のフラグ
closing_flag              3     2    0     0    0  コマンド入力・実行中のフラグ
last_command              4     2    1     1    0  コマンド入力・実行中のフラグ
light_flag                8     3    3     1    0  コマンド入力・実行中のフラグ
died_from                35     9   14     7    0  セーブ／スコア／進行メタ
savefile                 40     5    4     2    0  セーブ／スコア／進行メタ
birth_date               16     5    2     2    1  セーブ／スコア／進行メタ
character_generated      12     8    3     2    2  セーブ／スコア／進行メタ
character_saved          11     6    2     2    0  セーブ／スコア／進行メタ
highscore_fp             43     4    3     2    0  セーブ／スコア／進行メタ
noscore                  17     5    3     2    1  セーブ／スコア／進行メタ
randes_seed               6     5    1     1    1  セーブ／スコア／進行メタ
town_seed                 8     5    1     1    1  セーブ／スコア／進行メタ
death                    36    12    9     6    0  セーブ／スコア／進行メタ
turn                     54    15    6     3    1  セーブ／スコア／進行メタ
wizard                   25     9    2     2    0  セーブ／スコア／進行メタ
to_be_wizard              7     3    2     2    0  セーブ／スコア／進行メタ
panic_save                9     6    2     2    1  セーブ／スコア／進行メタ
```
