# 引きつぎ資料

次にこのリファクタリングを続ける担当へ。**この資料 → `REFACTORING_PLAN.md` →
`GLOBALS_INVENTORY.md`** の順に読めば、前提と作法と次の一手が揃う。

作成日: 2026-09-13（`develop` の `a89c97d` 時点）
更新: 2026-09-24（呪文 4 個の完了時点。第 0・4・5・6 節を差しかえた）
以前の更新: 2026-09-24（プレイヤー状態の 4 群 6 個のマージ時点。第 0・4・5・6・7 節を差しかえた）
以前の更新: 2026-09-24（居場所 2 個のマージ時点。第 0 節を差しかえた）
以前の更新: 2026-09-23（居場所 2 個の完了時点。第 0・4・5・7 節を差しかえた）
以前の更新: 2026-09-16（`highscore_fp` 完了時点。第 0・5・7 節を差しかえた）
さらに以前: 2026-09-15（#19 完了時点。第 0・5 節を差しかえた）
さらに以前: 2026-09-14（#18-5 完了時点。第 0・4・5・6・7 節を差しかえた。**同日の
履歴書きかえで全 SHA が変わったので、第 0・3 節と作成日の ID を差しかえた**）

## 0. 現在地

| | |
|---|---|
| ブランチ | **`refactor/18-8-spells-known`（4 コミット）で作業中。マージはまだ**（ユーザーの承認が要る）。`refactor/18-7-player-rest`（26 コミット）は `85e6b51` で `--no-ff` マージ済み・ブランチはまだ消していない（削除の指示は出ていない）。それ以前のブランチはすべてマージ済み・削除済み。**`origin/develop` は `2db44bd`（最後の fetch 時点）で、そこから `develop` が大きく先行している。push はしていない** |
| 警告 | **0 件**（`make clean && make`） |
| テスト | **685 件グリーン**（30 実行形式。`calc_spells_test` 17・`gain_spells_test` 13・`spells_known_test` 43 を追加） |
| `externs.h` の global | **67 個・参照 1808**（着手時 112・3147。2026-09-24 に呪文 4 個が `src/spells_known.c` の `static` になった） |
| `src/misc3.c` | **1906 行**（着手時 2212 → #33 の計測時 2090 → #40 で 1973 → #41 で 1904 → #18-5B の窓口置きかえで 1906。#19 では触っていない） |

**`refactor/18-8-spells-known` は A → B → C が終わり、記録の更新だけが
残っている**（2026-09-24）。**マージはユーザーの承認を得てから**（第 3 節）。
確認 4 つ（第 2 節）は各コミットで通している —— 警告 0・
**685 件 failed=0（30 実行形式）**・**67 個**・起動 1。
**`origin` への push はしていない**（第 3 節。push はユーザーが自分で行う）。

**区分「プレイヤー状態」に残るのは `py` 1 個だけ**（552 参照）。これは問い単位の
窓口で別に進める話で、1 本の module には収まらない。**#18 の次の一手は
この区分の外**（持ち物・アイテムに残る `object_ident` と `sorted_objects`、
または #33 の `misc3.c` 分割）→ 第 5 節。

**進めている作業は 2 本ある。**

1. `REFACTORING_PLAN.md` の **#18「データの散在」**。4 区分（オプション 11・
   メッセージ 4・パネル 10・店 2）が区分ごと片づき、**持ち物・アイテムは
   6 個のうち 4 個**（`inventory` `inven_ctr` `inven_weight` `equip_ctr`）が
   片づいた。**この区分は区分としては未完了で、`object_ident` と
   `sorted_objects` の 2 個が残っている**（どちらも鑑定と床の物の側なので、
   持ち物の窓口とは別の作業になる）。**セーブ／スコア／進行メタも 14 個のうち
   13 個が片づいた**（#19。`src/progress.c` `src/score_death.c`
   `src/save_state.c` の 3 本に割った）。**残っていた `highscore_fp` 1 個は
   2026-09-16 に `death.c` の 2 関数の局所変数にした**（窓口ではなく
   「global である必要が無い」話だった。`externs.h` には `init_scorefile()` の
   ために残るので **79 個は動かず、参照だけ 43 → 7** に減った。開いたまま誰も
   読まない handle はバグ候補 **B19** として記録 → 第 5 節）。
   **7 区分目「プレイヤー状態 13 個」に着手し、2026-09-23 に居場所 2 個
   （`char_row` `char_col`）を `src/player_pos.c` に閉じた**（79 → 77 個・
   参照 2215 → 1913）。**つづけて 2026-09-24 に 4 群 6 個**（勝ちと最高得点 →
   既存の `src/score_death.c`、レベルごとの HP 表 → `src/hp_table.c`、
   明かりの有無 → `src/player_light.c`、重さ 2 個 → `src/burden.c`。
   77 → 71 個・参照 1913 → 1850）。**さらに 2026-09-24 に呪文 4 個
   （`spell_learned` `spell_worked` `spell_forgotten` `spell_order`）を
   `src/spells_known.c` に閉じた**（71 → 67 個・参照 1850 → 1808）。
   **区分に残るのは `py` 1 個**（552 参照）で、これは問い単位の窓口で別に
   進める（→ 第 5 節）。
2. **#33 の `misc3.c` 分割**（11 責務）。**#40（能力値の補正表 → `src/stats.c`）と
   #41（文字列への差しこみ → `src/str_insert.c`）が完了**。残る **10 塊**は P2 の
   #42（**2026-09-14 に数えなおした。それまで「9 塊」と書いていたのは誤り**——
   #40 が出したのは責務「能力値」のうち補正表（塊 C）だけで、値を書きかえる側
   （塊 C'・115 行）が残っている）。
   **持ち物の窓口ができたので、塊 E（`misc3.c:950-1159`）の行き先は
   `src/inventory.c` に決まっている。**

**着手順は 2026-09-13 に測りなおして #40 → #41 → #18-5 と決め、3 つとも
終わった**（理由は `REFACTORING_PLAN.md`「着手順の再検討」、#18-5 の結果は
「#18-5 持ち物 4 個」の節）。#18-5 では窓口を 1 本にするか持ち物と装備に
割るかをユーザーに問い合わせ、**2 本に割って進めた**。跨ぎの呼びだしは
354 件中 22 件（6%）にとどまったので、**割ったままにする**と決めている。

P1 台帳（#0〜#9）は全項目完了、#40・#41 も完了。P2 台帳は #13 をのぞき完了。
コンパイラ警告は 8,338 → 0 件（実バグ 9 件の修正を含む）。

## 1. 資料の役割分担

| 文書 | 何が書いてあるか |
|---|---|
| `REFACTORING_PLAN.md` | 台帳（P1/P2/P3/棚上げ）、区分ごとの経過、**バグ候補 18 件**（B1 は #10-B で解消）、末尾に**作業ログ**（1 作業 1 行、テスト件数の推移つき） |
| `GLOBALS_INVENTORY.md` | #18 の棚おろし。着手前の姿（凍結）＋**所見 17 件**＋区分ごとの見立て |
| `scripts/globals.py` | `externs.h` の global を区分に分けて数える道具。手で数えない |
| `CHANGELOG.md` | 上流の履歴。この作業では触っていない |

`patterns.md`（臭いの型と手順のカタログ）はリポジトリ外。文中で参照されるが
無くても進められる。

## 2. 動かしかた

この 5 つを毎ステップの終わりにやる。**1 つでも欠けたらコミットしない。**

```sh
# 1. 本体（ncurses が要る。警告は 0 でなければならない）
make clean && make 2>&1 | grep -E "warning:|error:" | wc -l

# 2. 全テスト（ncurses 非依存。末尾に RESULT: GREEN が出る）
make -f makefile.test 2>&1 | grep -E "^[0-9]+ tests" \
  | awk '{s+=$1; f+=$5} END {print s" tests, failed="f}'

# 3. global の分類の網羅性（区分から外した名前を消し忘れると落ちる）
python3 scripts/globals.py --check

# 4. 起動確認（種族選択画面まで行けば OK）
touch scores.dat
printf '\n\n\n\n' | timeout 10 script -qc "TERM=xterm ./umoria /tmp/probe.sav" /dev/null \
  | tr -d '\r' | grep -c "Choose a race"
rm -f scores.dat /tmp/probe.sav

# 5. 意図的に壊してレッドを確認（→ 第 4 節）
```

`awk` の列に注意。`8 tests, 8 passed, 0 failed` なので**失敗数は `$5`**。
`$4` を見ると常に 0 に見えて、壊してもレッドに気づけない（実際に一度やった）。

## 3. ユーザーとの約束

- **破壊的変更・ふるまいの変わる変更は、やる前に問い合わせる。**
  そうでないものは「修正前後で結果が変わらないこと」をテストで守りながら進める。
- **ブランチを切る。**`refactor/18-5-inventory` のように区分ごと。作業は
  `develop` では行わない。
- **コミットは適切な単位で分ける。**1 コミット 1 変更。各コミットが単体で
  グリーンであること。
- **`origin` に push しない。**push と force-push はユーザーが自分で行う
  （2026-09-14 の履歴書きかえのあと、ユーザーが `develop` を force-push した）。
- **ブランチは自分では消さない。**ただし**消さずに残す約束は 2026-09-14 に
  解けた**——ユーザーの指示でマージ済みの 14 本を削除した。**2026-09-16 にも
  指示を得て `refactor/19-savemeta` を消している**（マージ後に確認 5 つを
  通してから消す）。区分ごとにブランチを切る作法は変わらないので、次の区分でも
  同じように切って、マージまで終わったらユーザーに消してよいか確かめる。
  **同じ日に `docs/fix-chunk-count` と `docs/history-rewrite`（2026-09-14 の
  削除で残っていた 2 本）も指示を得て消したので、ローカルは `develop` 1 本
  だけ。** どちらも `git branch --merged develop` に出る＝完全にマージ済みで、
  `-d` で消えた（`-D` は使っていない）。
- **バグ候補（B2〜B19）は直さない。**見つけたら記録して報告する。判断は
  ユーザーの領分。
- 文書・コミットメッセージ・テストのコメントは日本語。**ソースコードの
  コメントは英語**（周囲に合わせる）。
- コメントの誤りを見つけたら直してよい（ふるまいが変わらないので範囲内）。
  ただしコード変更と同じコミットに混ぜない。

## 4. 確立した進めかた

6 区分を通して形が固まっている。踏襲すること。

**ステップ A / B / C。** A =保護（新しい module とテストを置くが、本体は
呼ばない＝ふるまい不変）→ B =呼びだし側を窓口越しに書きかえ → C =`static`
化して `externs.h` から外す → docs。区分の性質によって A が無いこともある
（#18-4 は写す相手が無かった）。

**「テスト側に写す」方式。** 変更前のロジックが関数の中に直に書かれていて
外から呼べないとき、その並びをテストに `legacy_*` として写しとり、新しい実体と
総当たりで突きあわせる。#7 が最初で、#18-1（2^11 通り）、#18-3（出発点 28 通り
× 全域）、#18-4C（バイト列の一致）で使った。**写しは「変更前の字面」であって
理想の実装ではない**——変更前になりようのない状態を写しこんでいたことが
#18-3C で判明した例がある。

**意図的に壊してレッドを確認する。** 保護が効いていることの証拠。件数を
コミットメッセージに書く（例:「`row_prt` の 1 → 2 で 5 件」）。壊した後は必ず
復元し、グリーンに戻ったことを確認する。

**新しい module は依存を持たせない。** `panel.c` `messages.c` `options.c`
`stores.c` はどれも `externs.h` を include しない。おかげでテストが対象 1 本
だけをリンクできる。移せない副作用は呼びだし側に残す（`get_panel()` の
`end_find()` がその例）。

**既にある窓口モジュール**（真似する手本として）:

| module | 中身 | テスト |
|---|---|---|
| `src/device.c` | 魔法道具の成功判定 | `device_chance_test.c` 24 件 |
| `src/item_ident.c` | 効果が判明したときの経験値と鑑定 | `item_ident_test.c` 21 件 |
| `src/abilities.c` | 能力値 9 式 | `put_misc3_test.c` 24 件 |
| `src/options.c` | オプション 11 個の表（名前・置き場・ビット・既定値） | `options_test.c` |
| `src/messages.c` | メッセージ履歴の輪 | `messages_test.c` |
| `src/panel.c` | 見ている範囲の座標 10 個 | `panel_test.c` 12 件 |
| `src/stores.c` | 店 6 軒の記録 | `stores_test.c` 8 件 |
| `src/stats.c` | 能力値の補正表（`misc3.c` から出した 116 行） | `stat_bonus_test.c` 129 件 |
| `src/str_insert.c` | 文字列への差しこみ（`misc3.c` から出した 67 行） | `str_insert_test.c` 22 件 |
| `src/inventory.c` | 持ち物 22 枠と装備 12 枠（1 本の配列・2 つの窓口。`inventory.h` と `equipment.h`） | `inventory_test.c` 25 件 |
| `src/progress.c` | ターン数と乱数の種と wizard の 5 個 | `progress_test.c` 27 件 |
| `src/score_death.c` | 結末の 6 個（`death` `died_from` `birth_date` `noscore` ＋ #18-7-1 で足した `total_winner` `max_score`） | `score_death_test.c` 37 件 |
| `src/save_state.c` | セーブの旗 4 個と**跨ぐ述語 3 つ** | `save_state_test.c` 32 件 |
| `src/player_pos.c` | プレイヤーの居場所 2 個（読み 2・置く 1・忘れる 1） | `player_pos_test.c` 17 件 |
| `src/hp_table.c` | レベルごとの HP の段 1 個（表そのもの。`py.misc.mhp` は別の値） | `hp_table_test.c` 15 件＋`calc_hitpoints_test.c` 13 件 |
| `src/player_light.c` | 明かりの有無 1 個（**導出に替えず置き場のまま**。要るのは変わり目） | `player_light_test.c` 7 件 |
| `src/burden.c` | 重さに負けているか 2 個（武器は旗・荷は段数） | `burden_test.c` 12 件＋`check_strength_test.c` 24 件 |
| `src/spells_known.c` | 覚えている呪文 4 個（覚えた／成功した／忘れた の 3 つのビット列＋覚えた順 32 個。**ビット演算は窓口の内側**） | `spells_known_test.c` 43 件＋`calc_spells_test.c` 17 件＋`gain_spells_test.c` 13 件 |

**`src/str_insert.c` は「依存が 0 個の module」の手本。** リンクは
**`tests/str_insert_test.c` ＋ `src/str_insert.c` の 2 本だけ**（それまでは 10
コンパイル単位）で、**代役もフィクスチャも 1 つも要らない**。umoria のシンボルに
1 つも依存しないので、手書きの `extern` すら書かなくてよい。`vtype`（`types.h:17`）
**型**にだけ依存するため、include の並びは `stats.c` と同じ（`config.h` →
`constant.h` → `types.h` → 自分のヘッダ）。**`vtype` を `char[80]` に書きかえれば
型依存も消えるが、それをやると「純粋な移動」でなくなる**ので採らなかった。

**`src/stats.c` は「依存が 1 個ある module」の手本。** `panel.c` などは依存ゼロ
だが `stats.c` は `py` を要る。`externs.h` を include せず `extern player_type py;`
を自分で書く形を選んだ（理由は `src/stats.c:17-23` に英語で記載）。おかげで
`stat_bonus_test` は **`stats.c` ＋ `tests/stats_fixture.c` の 2 本だけ**を
リンクする（それまでは 8 コンパイル単位）。**代役は 1 つも要らない。**

**`src/inventory.c` も 2 単位・代役 0 になった（#18-5C）。** `inventory_test` の
リンクは **`tests/inventory_test.c` ＋ `src/inventory.c` の 2 本だけ**で、
`str_insert_test` と並ぶ最小構成。ただし軽い理由が違う ——
`str_insert.c` は**依存する相手がいない**からで、`inventory.c` は
**依存する相手（実体）を自分が持っている**から。ステップ A の時点では実体が
まだ `treasure.c` にあったので 3 単位＋足場 1 本（`tests/inventory_fixture.c`
31 行）で、**ステップ C で実体を引きこんだ結果として 2 単位・代役 0 になった。**
**カプセル化はテストの足場を減らす方向に効く** —— #41 の分割は既存テストを
1 本ずつ重くしたが、#18-5 では足場 31 行と `tests/creature_stubs.c` の代役
4 個が消えた（第 6 節の #38 の二面性を参照）。

**`src/spells_known.c` は「対で動く 2 つを 1 つの窓口にする」手本（#18-8）。**
4 個のうち `spell_learned` と `spell_forgotten` はいつも対で動き
（`calc_spells`）、`spell_learned` と `spell_order` もいつも対で動く
（`gain_spells`）。その対を `spell_forget()` `spell_remember()` `spell_learn()`
という 1 つの窓口にすると、**対が離れうる形が残らない**。覚えた順に積む位置を
探す 6 行（`last_known`）も、99 でシフトしないための 6 行×2 も呼びだし側から
消えた（`misc3.c` で 25 行 → 窓口の呼びだしだけ）。**ビット演算
（`1L << spell`）を窓口の内側に置くかはユーザーに問い合わせて「隠す」と決めた**
—— 呼びだし側が持っているのは呪文の番号（`magic_spell[]` の添字）で、
3 か所が同じシフトを書いていた。

**`src/player_pos.c` は「窓口の名前を呼びだし側で決める」手本（#18-6）。**
module の接頭辞にそろえるなら `player_pos_row()` だが、読みが 284 箇所ある
ので `los(player_pos_row(), player_pos_col(), y, x)` の雑音が 284 回出る。
**読みだけ短い名前**（`player_row()` / `player_col()`）にし、書きこむ側
（`player_place()` `player_pos_forget()`）は接頭辞を保った —— 「誰が
プレイヤーを動かすのか」は名前で探せる必要があるから。判断の理由は
`src/player_pos.h` に英語で書いてある。**窓口の数が少ない群では #38 の
二面性が損の側に出る**ことも分かった（既存 7 本が 1 単位ずつ重くなり、
`tests/creature_stubs.c` は他の代役が残るので消せず、減ったのは足場 25 行と
代役 2 行だけ）。

## 5. 次の作業: 「プレイヤー状態」の外へ（区分に残るのは `py` だけ）

**#19 は終わった**（結果は `REFACTORING_PLAN.md`「#19 セーブ／スコア／進行メタ
13 個」の節）。**`highscore_fp` 1 個も 2026-09-16 に片づいた**（下の 5-1 は
その記録）。**プレイヤー状態 13 個のうち居場所 2 個も 2026-09-23 に片づいた**
（5-2）。**さらに 4 群 6 個も 2026-09-24 に片づいた**（5-3）。次は**同じ区分に
残る 5 個のうち呪文 4 個**（5-4。`py` は最後）。**この資料の数字は着手時に必ず
測りなおす**（`python3 scripts/globals.py --full`。過去 3 回、記録が 6 箇所・
11 箇所・6 箇所ずれていた）。

### 5-1. 済んだ 1 個: `highscore_fp`（2026-09-16、`c50804e`）

3 通りの行き先を並べてユーザーに問い合わせ、**1 番（`death.c` の 2 関数だけ
局所変数にする）** を選んでもらった。`display_scores()` と `highscores()` は
どちらも 1 つの関数の中で `fopen` して `fclose` するので、handle を global に
置く理由が無い。38 件を `FILE *score_fp` にした（**名前を変えたのは
`-Wshadow` が global と同名の局所変数を拾うから**）。参照 43 → 7・書き 3 → 1。

**`externs.h` にはまだ残っているので 79 個は動かない。** 残った書きこみは
`init_scorefile()`（`files.c:28`）1 箇所だけで、これは setuid の権限が残って
いるうちに得点ファイルを開いておくためにある。**その handle は誰も読まず、
誰も閉じない** —— 以前は 2 関数が上書きしていたので「捨てられている」と
読めたが、いまは上書きする人もいない。setuid の意図は最初から果たされて
いなかった（権限を落としたあとに 2 関数が開きなおす）。**バグ候補 B19 として
記録し、直していない**（`REFACTORING_PLAN.md`）。

**この 1 個を `externs.h` から消すときに残っている判断**（ふるまいが変わるので
ユーザーの領分。第 3 節）:

- `init_scorefile()` を「開いて `NULL` 検査だけして閉じる」にするか、開くのを
  やめるか（**79 → 78 個**。setuid の意図を捨てる方向）。
- あるいは開いた handle を 2 関数に渡す形にそろえるか（setuid の意図を活かす
  方向。ファイル位置の共有が起きるので、`fseek` の入れかたを考える必要がある）。
- どちらでも `game_state.h:65` の項目と `game_state.c:80` の写しが消える
  （`game_state_init()` は呼ばれていない → バグ候補 B3）。

**確認のしかたが特殊だったので第 7 節にも書いた。** `death.c` をリンクする
テスト実行形式が無いので、単体テストは 1 件も届かない。実物の `scores.dat` を
変更前後の binary で両方向に読みかえて確かめた。

### 5-2. 済んだ 2 個: `char_row` / `char_col`（2026-09-23、`src/player_pos.c`）

区分の 1 群目。**3 通りの行き先を並べてユーザーに問い合わせ、「位置だけの
module を作る」を選んでもらった**（`py.misc` に入れる案と、`py` 全体の窓口に
混ぜる案は採らなかった）。おかげで **`py` の粒度の決定と独立に進められた。**

窓口は 4 つ（`player_row()` `player_col()` `player_place(row, col)`
`player_pos_forget()`）。302 参照のうち **284 件が読み**で、書きは 5 組 10 件
（行と列を別々に動かす箇所は 1 つも無い）。**79 → 77 個・参照 2215 → 1913**、
区分は **13 個・960 → 11 個・658**。詳しくは `REFACTORING_PLAN.md`
「#18-6 プレイヤー状態のうち居場所 2 個」の節。

**着手前の見立ては外れていた。** 表の「難所」は「`char_row`/`char_col` を
`py` に寄せるのが最も効く一手」だったが、効いたのは `py` に寄せることでは
なく**読みを 1 つの問いに畳むこと**だった。残る 5 個でも、見立てより
「参照の中身がどうなっているか」を先に数えること。

### 5-3. 済んだ 4 群 6 個（2026-09-24、`refactor/18-7-player-rest`）

**着手前の問い合わせ 4 つはすべて答えが出た**（下の 5-4 の表から消えた 6 個）。

| 群 | 個数 | 行き先 | 判断 |
|---|---:|---|---|
| #18-7-1 | `total_winner` `max_score` | 既にあった `src/score_death.c` | 見立てどおり合流させてよい、と承認を得た |
| #18-7-2 | `player_hp` | 新設 `src/hp_table.c` | `py.misc.mhp` との二重化は**残した** —— 表は「レベルごとの段」、`mhp` は「いまの最大 HP」で別の値。`calc_hitpoints` が表から `mhp` を作る関係も変えていない |
| #18-7-3 | `player_light` | 新設 `src/player_light.c` | **導出に替えず置き場のまま。** `dungeon()` が明るい／暗いの**変わり目**でモンスターを出しなおすので |
| #18-7-4 | `weapon_heavy` `pack_heavy` | 新設 `src/burden.c` | 同じく置き場のまま。速度は `change_speed(新しい段数 − 覚えた段数)` という**差**で動くので、覚えた数が要る |

**旗 3 個の「導出にするか」は『しない』が答えだった。** 3 個とも他の状態から
計算できるが、呼びだし側が要るのは値ではなく**変わり目**だった。詳しくは
`GLOBALS_INVENTORY.md` の所見 16 と `REFACTORING_PLAN.md` の #18-7 の節。

**テストが 1 件も届いていない関数は、窓口に替える前に押さえる**という手が
この 4 群で 2 回要った（`calc_hitpoints` 13 件・`check_strength` 24 件）。
戻り値が無い関数は、代役に渡された値を記録させて `tests/fixture.h` の窓口から
読む（`change_speed`・`calc_bonuses` に足した。`msg_print` の延長）。

### 5-4. 済んだ 4 個: 呪文（2026-09-24、`src/spells_known.c`）

**行き先とビット演算の扱いはユーザーに問い合わせて決めた**（第 3 節の約束。
「1 本の module にまとめる」「ビット演算は窓口の内側に隠す」）。

| 段 | 何をしたか |
|---|---|
| A1 | `calc_spells`（`misc3.c:1220`）にテスト 17 件。**単体テストが 1 件も届いていなかった** |
| A2 | `gain_spells`（`misc3.c:1374`）にテスト 13 件。同じく 0 件だった |
| A3 | `src/spells_known.c` ＋ `.h` ＋ テスト 43 件。実体はまだ `player.c`（足場 `tests/spells_known_fixture.c`） |
| B1 | `misc3.c` の外の 5 ファイル（`magic.c` `prayer.c` `main.c` `moria3.c` `save.c`）13 行 |
| B2 | `misc3.c` 25 行とテスト 2 本。**`src/` から直接参照が消えた** |
| C | 実体 4 行を `player.c` から module の `static` に移し、`externs.h` の 4 行を削除。足場を削除 |

**得たものは「対が離れうる形が残らない」こと。** 覚えた印と忘れた印
（`calc_spells`）、覚えた印と覚えた順（`gain_spells`）はいつも対で動いていた
が、対であることはコードのどこにも書かれていなかった。`spell_forget()`
`spell_remember()` `spell_learn()` にしたので、片方だけ動かす書きかたが
できない。

**消えた雑音**: 覚えた順に積む位置を探す 6 行（`last_known`）／99 で
シフトしないための 6 行 × 2 か所／32 回まわして覚えている数を数える 5 行／
同じ `1L << i` を書いていた 3 か所。

**記録していた見立ては外れていた。** 5-4 に「`magic.c` `prayer.c` `save.c`」と
書いてあったが、実際の集まりは **`misc3.c` の 25 行**（43 参照のうち）。
`magic.c` と `prayer.c` は 2 行ずつしかない。**見立ては着手時に測りなおす**
（第 5 節の冒頭にそう書いてあるとおり）。

**意図して古い形と違えたところが 3 つある。** どれもまともなゲームでは
起きない道すじで、コメントに英語で理由を書いた。
- 覚えた順が満杯なら積みを落とす（古い形は `spell_order[32]` に書いた）
- `spell_learned_nth(負)` は `SPELL_NONE`（古い形は配列の手前を読んだ）
- 走りだしの 0 埋めは**そのまま**にした（`main.c` が `SPELL_NONE` で埋め、
  セーブから戻すときは 4 つとも上書きされるので、初期値は誰も読まない）

### 5-5. 次: この区分の外

**区分「プレイヤー状態」に残るのは `py` 1 個だけ**（552 参照）。**これは
1 本の module にはならない** —— 既に構造体で、**#19 方式の「問い単位の窓口」**で
進めるとユーザーと決めている（`py` 全体を返す窓口は作らない）。まとまった
1 作業ではなく、問いごとに切り出す長い列になる。

**なので次の一手は 2 つの候補から選ぶ**（どちらを先にするかはユーザーに
問い合わせる。作業の順番の判断なので第 3 節にあたる）。

| 候補 | 中身 | 手ざわり |
|---|---|---|
| 持ち物・アイテムの残り 2 個 | `object_ident`（11 参照）`sorted_objects`（6 参照） | **安い。** どちらも鑑定と床の物の側で、`src/inventory.c` からは独立している。区分を 1 つ閉じられる |
| #33 の `misc3.c` 分割 | 残る 10 塊（P2 #42） | **高い。** ただし塊 E（`misc3.c:950-1159`）の行き先は `src/inventory.c` に決まっている |

**`py` を始めるなら、最初の問いを何にするかから決める。** 552 参照の内訳を
測りなおし、いちばん独立した問い（例: `py.flags.confused` のような状態の旗）
から切るのが安い。**この判断はまだユーザーに問い合わせていない。**

どれを選んでも段取りは #18-5 / #19 / #18-6 / #18-7 / #18-8 と同じ A → B → C
（第 4 節）。**直接参照が `src/` と `tests/` の両方で 0 になってから** C で実体を
移す（#19C・#18-6C・#18-7C・#18-8C はこの順序を守ったのでリンカのエラーが
1 件も出ていない）。

- 手本は 6 つある。依存 0 の `str_insert.c`（2 単位）、依存 1 の `stats.c`
  （3 単位）、**実体を自分で持つ `inventory.c`（2 単位・代役 0）**、
  **#19 の 3 本**（`progress.c` `score_death.c` は 2 単位・代役 0、
  `save_state.c` は跨ぐ述語 2 つのために 4 単位）、
  **`player_pos.c`（2 単位・代役 0。窓口 4 つの最小形）**、そして
  **`spells_known.c`（2 単位・代役 0。窓口 20 個で、対で動く操作を 1 つに
  まとめた形）**。**「跨ぎに名前を付けて片方向の依存にする」形が要るなら
  `save_state.c` が近い。**
- **P3 #49 を忘れない。** `sv_write` / `get_char` の項目の並びを見るテストが
  無いので、セーブファイルを読み書きする global を触るときは**単体テストの
  グリーンを信用しすぎない**（#19B4 では意図的な壊し 4 件が全部生き残り、
  #18-6B1 でも 1 件も落ちなかった）。実物で確かめるやりかたと**その限界**
  （乱数を引く経路は比べられない／セーブファイルのバイト比較は使えない）は
  第 7 節にまとめた。**#18-8-B1 の `save.c` は 3 語＋32 バイトの並びを
  変えていない**（生の窓口を通すだけ）。

## 6. その先の順番

`GLOBALS_INVENTORY.md` の「区分ごとの見立て」が正。安い順に並べてある。

| 次 | 区分 | 個数 | 参照 | 別名 | 難所 |
|---|---|---:|---:|---:|---|
| 済 | 持ち物・アイテム | 6 → **2** | 379 → **17** | 113 → **0** | **持ち物 4 個は完了**（2026-09-14、`src/inventory.c`）。窓口は持ち物・装備・跨ぎの 3 系統で、跨ぎの呼びだしは 354 件中 22 件。**残る 2 個**（`object_ident` `sorted_objects`）は鑑定と床の物の側で、持ち物 4 個から独立している |
| 済 | セーブ／スコア／進行メタ | 14 → **1** | 320 → **45** | 8 → **0** | **13 個は完了**（2026-09-15、`src/progress.c` `src/score_death.c` `src/save_state.c` の 3 本）。見立てどおり 3 用途に割れた（3 群を混ぜる式は `signals.c:199` の 1 つだけ）。別名 6 件は予想どおり `save.c` の `rd_*(&x)` で、局所変数経由にしてキャスト 4 件も消えた。**残る 1 個 `highscore_fp`** は窓口ではなく局所変数化（→ 第 5 節） |
| 済※ | プレイヤー状態 | 13 → **1** | 957 → **552** | 106 → **88** | **12 個は完了**（居場所 2 個は 2026-09-23 の `src/player_pos.c`、旗と控え 6 個は 2026-09-24 の `src/hp_table.c` `src/player_light.c` `src/burden.c` と既存 `src/score_death.c`、呪文 4 個は 2026-09-24 の `src/spells_known.c`）。**旗 3 個は導出に替えず置き場のまま**にした —— 呼びだし側が要るのは値ではなく**変わり目**（→ 5-3）。※**残る `py` 1 個で 552 参照・別名 88** を占める。1 本の module にはならず、問い単位の窓口で別に進める（→ 5-5） |
| **1** | 持ち物・アイテムの残り 2 個 | 2 | 17 | 0 | `object_ident` 11 参照・`sorted_objects` 6 参照。**いちばん安い**。区分を 1 つ閉じられる（→ 5-5） |
| 2 | コマンド／実行中フラグ | 13 | 241 | 2 | `free_turn_flag`（書き 80／14 ファイル）は**別枠**。置き場ではなく戻り値の問題 |
| 3 | ダンジョンとその中身 | 12 | 652 | 248 | `cave` 別名 257。**最後**。単独で 1 プロジェクト |
| — | オプション | 11 | 87 | 13 | #18-1 で表は 1 つになったが**値は `externs.h` に残っている**。`static` にするには 11 個ぶんの窓口か「設定の塊」が要る |
| — | 定数表 20 / 起動時に書きかわる表 7 | 27 | 251 | 21 | #18 の対象外。前者は `const` 化の話、後者は `const` にすると壊れる |

**#18 に混ぜないと決めたもの**（`REFACTORING_PLAN.md` に理由あり）:
`free_turn_flag` の戻り値化、`wait_for_more` → `get_check` の画面復元、
実質定数 16 個の `const` 化、`GameState`（#35）の接続。

**#18 以外に残っている台帳項目**: P2 **#13**（シグナル責務の分裂。**バグ候補
B15 と同一原因**なので直すなら同時）、P3 **#19〜#24**（テストの足場ができた
ので #19 と #22 は再評価の条件を満たしている）、棚上げ **#25〜#36**。

**P3 #37〜#39 は「テストとビルド定義自身の臭い」**（2026-09-13 追加）。
`src/` だけを見ていたので漏れていた領域で、#38（`makefile.test` の 6〜9
module 連鎖）は**次に新しいテストを足すときの足場の重さに直に効く**。
持ち物（#18-5）は `tests/misc3_stubs.c` の上に足すことになると見ていたが、
**実際は `inventory_test` を 2 単位・代役 0 で新設できた**（下記「#18-5 で
二面性の 2 例目」）。`misc3_stubs.c` に手が入ったのは代役を足すためではなく、
**そこに残っていた直接参照 3 件を窓口越しにするため**。#37（代役 2 ファイルの
19 シンボル重複）はまだ残っているので、新しいテストを足すときは #37・#38 を見る。

**P3 #47・#48 は #41 の副産物**（2026-09-13 追加）。**#47 は #41 から意図的に
外したもの**——`concat`（`misc4.c:95-108`）は `insert_str` と同じ性質だが、
`CONCAT` 経由の呼びだし 88 件がすべて `tests/creature_stubs.c:166` の代役に
置きかわるので**本物を通るテストが 0**。行き先（`src/str_insert.c`）は既にあるので、
必要なのはステップ A だけ。**#48 は移動のついでに直したくなったが我慢したもの**
（`insert_str` の走査の終端と置きかえの可否が 2 つの独立した条件で書かれている）。
保護 22 件・リンク 2 単位が揃っているので、**P3 で最も着手が安い部類**。

**P3 #44〜#46 も同じ領域**（2026-09-13 追加）。**#44 は保護の穴**——代役 4
ファイルに**引数を無視して定数を返す非 void の代役が 55 個**ある（`in_bounds` は
常に真、`los` は常に偽、`tfopen` は NULL）。**新しいテストを書くときにこれを
知らないと穴を作る。** とくに `tests/creature_stubs.c:182-184` の観測窓口は
常に `""` / `0` を返すので、**あの構成ではメッセージや画面を見るテストは
書いても中身を検証できない**。#45 は `globals.py` の誤検出、#46 は
`makefile.win` の 16 個未登録。

**#38 の成果指標は 2026-09-13 に置きかえた。** 「`misc3.c` 単体の未解決
シンボルが減ることが成果」は**誤り**。分割すると増える（#40 で 85 → 87。
ファイル内の呼びだしがモジュール間の呼びだしに変わるため）。**成果は
「実行形式ごとのリンク単位」で測る**——#40 で `stat_bonus_test` が 8 → 3、#41 で
`str_insert_test` が 10 → 2（代役 0）。**ただしこの指標には 2 面ある**（#41 で判明）。
**新しく書くテストは軽くなるが、`misc3.c` を引く既存 4 本は切りだした module を
足すぶん 1 本ずつ重くなる**（`inven_stack` `put_misc3` `objdes` 9→10、
`haggle_comment` 11→12）。回収できるのは #42 を終えたとき。**着手順の判断に使う
ときはこの二面性を前提にする。**

**#18-5 で二面性の 2 例目が出た。** 新設の `inventory_test` は **2 単位・代役 0**
で済んだ一方、**`item_ident_test` は `src/inventory.c` を引くようになった**
（`desc.c` が持ち物の窓口越しに `inventory` を触るため。同じことが
`movement_rate_test` 2 → 3、`save_bool_test` と `store_save_test` にも起きている）。
つまり**窓口を作る作業でも既存テストのリンクは重くなる。** ただし #41 と違って
**軽くなる側にも動いた** —— 実体が `inventory.c` の `static` になったので、
`tests/inventory_fixture.c` 31 行と `tests/creature_stubs.c` の代役 4 個が消えた。
**分割（#42）は代役を減らさないが、カプセル化（#18）は減らす。**

## 7. 地雷とつまずきどころ

前の担当が実際に踏んだもの。

**ビルド・リンクまわり**

- `src/types.h` に多重取りこみの番人が無い。**テストから重ねて include できない**。
  module のヘッダは `types.h` を include せず、「先に `config.h` / `constant.h` /
  `types.h` を include すること」を前提にする（`externs.h` 自身がそう）。
- include の順は `headers.h` → `config.h` `constant.h` `types.h` →
  `externs.h` と module のヘッダ。`externs.h` は単体ではコンパイルできない。
- `makefile.test` は **1 実行形式 1 対象 module**。追加するときは `TESTS` に
  足し、依存と link 行を書く（コメントに「なぜこの .c をリンクするか」も書く
  慣習になっている）。
- `static` 関数を検証したいときはテスト側が `.c` を丸ごと `#include` する
  （`save_bool_test` `store_save_test` `haggle_comment_test`
  `movement_rate_test`）。この場合、**代役側の同名定義と衝突する**ので注意
  （#18-4A で `haggle_comment_test.c` の `last_store_inc` を消した）。
- **`static` な実体と、代役側の非 `static` な定義は multiple definition に
  ならない。** 別の翻訳単位なのでリンカは黙って両方を置く。足場
  （`tests/*_fixture.c` `tests/*_stubs.c`）を消し忘れると、**窓口越しの
  読み書きが届かない代役**が生き残り、テストは通るのに何も検証しない状態に
  なる。#18-5C ではたまたま `undefined reference` 41 件が先に出たので
  気づけたが、順序が違えば見逃していた。**実体を `static` にしたら、同じ
  名前を定義している代役を必ず探して消す。**
  **#19C で実測した**（2026-09-15）。`src/` と `tests/` の直接参照を両方 0 に
  してから 13 個を `static` にしたので、**リンカは 1 件も文句を言わず、足場
  3 本はそのまま残った**。足場の `turn` を `-1` → `12345` に変えても
  `failed=0`（＝もう本体に届いていない）、本体側の同じ壊しは 1 件レッド。
  **この 2 つを並べて走らせるのが、足場が死んでいることの確認になる。**
- `externs.h` が `fopen` を `tfopen` に置きかえる。テストで実ファイルを開く
  なら `tmpfile()` を使う。
- 新しい `.c` は `makefile` の `SRCS` と `OBJS` の**両方**に足し、依存行
  （`foo.o: $(SRCDIR)/foo.h $(HEADERS_FULL)`）も書く。
- **ビルド定義は 3 つある。`makefile.win` を忘れる**（`SRCS` / `OBJS` / 依存行の
  3 箇所）。ただしこのファイルは**着手前から壊れていて、16 個のソースが未登録**
  （→ P3 #46）。それでも新しい `.c` を足すときは 3 箇所を更新する慣習にしている
  ——直す人が現れたときの差分を小さく保つため。
- **新しい module に `externs.h` を include させたくなったら、要る宣言が
  いくつかを数える。** `stats.c` は `py` の 1 個だけだったので
  `extern player_type py;` を自分で書いた（二重宣言になるが、1 行のために
  ncurses まで引きこむ全域ヘッダは取りこまない）。理由は `src/stats.c:17-23` に
  英語で書いてある。
- **`src/strings.h` という名前を作らない。** `-Isrc` があるので
  `#include <strings.h>` を横取りしうる（`src/curses.h` で同じ横取りが既に
  起きている）。

**テストの書きかた**

- `tests/minunit.h`: `TEST` / `ASSERT_EQ_INT` / `ASSERT_EQ_STR` /
  `ASSERT_TRUE` / `ASSERT_FALSE` / `RUN_TEST` / `TEST_SUMMARY`。
  `MU_SETUP()` / `MU_TEARDOWN()` を `#include` 前に定義すると毎テストで走る。
- **1 テスト 1 assertion。**総当たりは `mismatch` を溜めて最後に 1 回
  assert する形にしている（どの入力で落ちたかが件数ではなく値で分かる）。
- テストは 1 プロセスで状態を共有する。**書きこむテストと初期状態を見る
  テストがあるなら、後者を `main()` の先頭に置く**（`stores_test.c` の
  `every_store_starts_out_empty`）。順序依存はコメントに書く。

**git まわり**

- マージは必ず `--no-ff`。長い日本語のメッセージはファイル経由で渡す
  （`git merge --no-ff -F /tmp/msg.txt <branch>`）。**heredoc の `-F -` は
  効かない。**コミットも同じく `git commit -F /tmp/msg.txt`。
- コミットメッセージの体裁: 1 行目に `#18-4B: 何をしたか`、本文に**なぜ・
  何が消えたか・保護のとりかた・意図的な壊しの件数・テスト件数と警告数**。

**その他**

- **2026-09-14 に履歴を書きかえたので、それより前の記録にある SHA は
  もう存在しない。** ユーザーの指示で `Ryohji.Ikebe@sony.com` を
  `Ryohji.Ikebe@gmail.com` に差しかえた（`git filter-branch`、対象 158
  コミット）ため、`develop` の全 440 コミットのうち書きかえ範囲以降の SHA が
  すべて変わり、ユーザーが `origin/develop` に force-push した。**中身は
  1 バイトも変わっていない**（書きかえ前との `git diff` が空であることを確認済み）。
  `REFACTORING_PLAN.md` の作業ログには**書きかえ前の SHA が数百件残っている**。
  コミット ID で追えないときは**題名で探す**（`git log --format='%h %s' | grep ...`）。
  題名は 1 文字も変わっていないので確実に見つかる。**古い ID を新しい ID に
  一括で置きかえる作業はしていない**——記録としての価値は題名と数字のほうに
  あり、数百件の機械置換で誤りを混ぜるほうが損だと判断した。
- **ステップ B の完了判定を `src/` だけで測らない。** `tests/` にも直接参照が
  ある。#18-5 では `src/*.c` の参照が 0 になった時点で「実体を `static` に
  できる」と判断したが、`tests/` に **41 件**残っていた（`inven_stack_test.c`
  22・`item_ident_test.c` 6・`misc3_stubs.c` 3・`fixture.c` 2、
  `creature_stubs.c` の重複定義 4）。**`grep` の対象は `src/` と `tests/` の
  両方にする。**
- `save.c` の `SAVE_LOG(...)` は空に展開されるデバッグ用。増やしてもセーブ
  ファイルには影響しない。
- **`death.c` には単体テストが 1 件も届かない**（リンクしている実行形式が
  無い）。得点ファイルまわりを触るなら**実物で確かめる**しかない。
  `highscore_fp`（2026-09-16）でやったのはこの手順:

  ```sh
  git worktree add /tmp/wt-before <変更前の SHA> && make -C /tmp/wt-before
  # キーは 1 文字ずつ間をあけて渡す（まとめて流すと端末の用意が終わる前に
  # 食われて、種族選択の手前で止まる）
  #   \n 導入画面 → a 種族 → m 性別 → ESC 能力値決定 → a 職業 →
  #   名前 \r → \n pause_exit → ^K 終了 → y 確認 → ESC 記録出力を飛ばす →
  #   \n 上位得点表のあとの pause_line
  ( sleep 2; for k in '\n' a m '\033' a P r o b e '\r' '\n' '\013' y '\033' '\n'; do
      printf '%b' "$k"; sleep 0.6; done; sleep 3 ) \
    | timeout 40 script -qc "TERM=xterm ./umoria /tmp/t.sav" /dev/null > /tmp/out.txt
  # 表示だけ見るなら得点ファイルを読んで終わる -s（display_scores() だけ通る）
  printf '\n' | timeout 15 script -qc "TERM=xterm ./umoria -s" /dev/null
  ```

  **終了キーは `^K`（`\013`）で、`Q` ではない。** 既定の（rogue 風でない）
  割りあてでは `original_commands()`（`dungeon.c:824`）が `^K` を `'Q'` に
  訳す側なので、`Q` を直に送っても効かない。1 人ぶんの記録は **76 バイト**、
  2 人ぶんで **149 バイト**。変更前と変更後で「書いた側・読んだ側」を入れ
  かえて 2 通り走らせ、`-s` の出力が 1 バイトも違わないことを見る。
- **実物で突きあわせるなら「乱数を引かない経路」を選ぶ**（#18-6 で分かった
  手順の限界）。移動・視界・呪文・主ループには単体テストが 1 件も届かないので
  実物の画面を比べるしかないが、比べられる経路とそうでない経路がある。

  | 経路 | 決定的か |
  |---|---|
  | セーブを読みこんで表示する | **はい**（#18-6B1） |
  | 読みこんだあと歩く・探索する | **はい**（同じ binary 2 回で 2858 バイトが一致） |
  | キャラクタを作る | いいえ（能力値のロール） |
  | ウィザードモードの `^T`（テレポート） | **いいえ**（同じ binary 2 回で 3083 / 3120 バイト） |

  種は起動時に時計から取るので、乱数を引く経路は**同じ binary でも 2 回で
  違う**。手順は「変更前の binary でキャラクタを作って保存 →
  その 1 つのセーブを両方の binary で読み、同じキー列を流して画面を比べる」。
  キー列は `#18-6B2` で使った南 2・東 2・北・西 2・南・東・探索 2。
- **セーブファイル同士のバイト比較は使えない。** `sv_write` の 4 バイト目が
  XOR の種で、保存ごとに変わる（中身が同じでも全バイトが違って見える）。
  比べるのは**復元した画面**のほう。#18-6B1 で 4846 / 4849 バイトが全相違に
  なって気づいた。
- **実物で「窓口がその道で生きている」ことを示すには、変異させた binary を
  並べる**（#18-7-4 で分かった手）。同じセーブを 3 つの binary
  （変更前・変更後・**窓口の中身を壊した変異**）に読ませ、変更前と変更後の
  画面が一致し、**変異だけが違う**ことを見る。#18-7-4 では変異 binary が
  読みこんだ直後に "You move more easily under the weight of your pack." を
  出して差が現れた（覚えた段数が壊れると、荷が変わっていないのに「楽に
  なった」と言う）。一致するだけの比較は「その道を通っていない」ことと
  区別できないので、**差が出る側を 1 つ用意する**。
- **盲打ちのキー列で作れない状態がある。** #18-7-4 で重い荷（STR 17・体重 166
  なら上限は 2376、持ち物は 302）を作るには店で 2000 単位ぶんの鉄杭を買う必要
  があり、画面を見ずに組めなかった。`moria4.c:83`（掘るときに旗を読む）も
  "Empty air?!?" で手前で戻る。**実物で届かない分岐は単体テスト側で押さえる**
  （`check_strength_test.c` の 24 件がその 4 通りの分岐を持っている）。
- **変異の刺す場所を間違えると「生き残った」と誤読する。** #18-8-A3 で
  `bit_of()` の範囲検査を確かめようとして、検査の**後ろ**にあるシフトの式だけ
  丸めた（`1L << (spell & 31)`）。検査が先に 0 を返すので変異は届かず、
  43 件そのままグリーンになった。**「生き残った」と読む前に、変異が本当に
  実行される位置にあるかを確かめる**（`grep` で当てた 1 行だけを見て済ませ
  ないこと）。刺しなおしたら 3 件が落ちた。
- **初期値が誰にも読まれない置き場がある。** #18-8-C で 4 個を `static` に
  移したとき、実体を `0xFFFFFFFF` に変えても全 685 件がグリーンだった。
  どのテストも最初に置き場を空にするうえ、本体でもゲーム開始時に `main.c` が
  埋め、セーブから戻すときは 4 つとも上書きされる —— **初期値は本当に誰も
  読まない**。C の検証はそこではなく、**実体の幅を狭める**（`uint32_t` →
  `uint16_t`）ほうで取った（module のテストと `calc_spells_test` の両方が
  落ちる）。`nm` で 4 つとも局所記号（小文字の `b`）になっていることも見る。
- **`git add -A` は実行時の生成物を拾う。** ゲームを起動して確認すると
  作業ディレクトリに `scores.dat` ができる。#18-6B1 で 1 度コミットに混ぜて
  しまい、`git rm --cached scores.dat` ＋ `git commit --amend -C HEAD` で
  外した。**staging は明示したパスだけにする**（`.gitignore` には入れていない
  ——ゲームを動かすのに必要なファイルなので、追跡しないまま置いてある）。
- `scripts/globals.py` の `GROUPS` は手で維持する表。区分が片づいたら**行ごと
  消して**コメントで `GLOBALS_INVENTORY.md` を指す（片づいた記録は inventory
  側に残す）。消し忘れると `--check` が「`externs.h` に無い」と教えてくれる。
- 過去に**報告した数値の誤り**が 4 回ある（テスト件数の読みまちがい、レッド
  件数の誤り、`awk` の列ちがい、**偵察で数えた「読みの件数」を 258 と書いたが
  正しくは 284**——#18-6。窓口の呼びだしを数えるまで気づかなかった）。
  **数字は自分で測って書く。**履歴は書きかえず、作業ログに正しい値を残す運用に
  している。
  **2026-09-13 の再検討では、台帳に書かれていた数字のうち 11 箇所がずれていた**
  （行数の境界、実体の置き場、別名の数、ファイルごとの件数、未解決シンボルの
  数えかた）。**引きつぐときは「記録された数字」を前提にせず測りなおす。**
- **検証（ステップ C）を読みとり専用の担当に割りあてない。** 意図的に壊して
  レッドを確認する工程が実行できず、検証が未完了で返ってくる（実際に一度
  やった）。`Explore` のような読みとり専用の役は**計測と点検まで**で、
  ミューテーションは書きこみできる役に渡す。
- **変異が生き残ったという報告は、それ自体を独立に確かめる。** #41 では作業役 2 名が
  `pc <= bound` → `pc < bound` を「部分的な等価変異」と報告したが、**検証で殺される
  ことが分かった**（一致が**無い**入力で `"Arrow"` → `"Arros"`。`objdes_test` も 8 件
  レッド）。「レッドにならなかった」は**入力が足りないだけ**のことが多い。等価だと
  結論するには「原理的に区別できない」ことを示す必要がある（#40 の `con < 7` は
  最下段が `con - 7` なので本当に等価だった）。
- **「テストの件数」と「保護の強さ」は別物。** #40 では `stat_bonus_test` が
  117 件あったのに、7 関数のうち `con_adj` は 0 件だった。**件数ではなく
  関数ごとの被覆を見る。**

## 8. 終わったら更新する文書

区分を 1 つ片づけるごとに、**docs だけの独立したコミット**を作る。

1. `GLOBALS_INVENTORY.md`——所見（`## わかったこと` の連番）を 1 件追加、
   「区分ごとの見立て」の該当行を**完了**に、集計節の「現在の合計」を更新。
   凍結した表（着手前の姿）と付録の一覧は**書きかえない**。
2. `REFACTORING_PLAN.md`——`#### #18-N` の節を追加、#18 の行の個数と完了
   一覧を更新、**末尾の作業ログに 1 ステップ 1 行**（テスト件数の推移つき）。
3. `scripts/globals.py`——`GROUPS` から片づいた名前を消す。
4. `HANDOVER.md`（この資料）——第 0 節の数字と第 5 節の「次の作業」を差しかえる。

マージまで終わったら、マージの行も作業ログに足す（`--no-ff` のコミット ID と
「マージ後に全ビルド・全テスト・起動を確認」）。

**#33 系（#40〜#42、`misc3.c` の分割）のときは 1 と 3 が要らない。** global を
減らす作業ではないので `GLOBALS_INVENTORY.md` と `scripts/globals.py` は
そのまま。かわりに **`REFACTORING_PLAN.md` の「#33 の精査」の塊の表**（どの塊が
どこへ行ったか）と、**この資料の第 4 節の module 一覧**を更新する。
